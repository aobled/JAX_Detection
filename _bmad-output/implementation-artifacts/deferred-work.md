## Deferred from: discussion "too_much_boxes" avec Amelia (bmad-agent-dev) (2026-07-18)

**Contexte** : l'utilisateur a déplacé, par principe, toutes les images à plus de 20 boîtes (`max_boxes` de `JAX_DETECTOR`/`FIGHTERJET_DETECTION`) dans un répertoire séparé, `/home/aobled/Downloads/Aircraft_DATASET/too_much_boxes` — **242 images, 7 167 boîtes au total** (~29,6 boîtes/image en moyenne, probablement des scènes de tarmac/formations à forte densité). Question posée : les inclure dans le dataset de détection, les laisser à part, ou baisser `max_boxes` de 20 à 10 ?

- **[Discussion][Decision] Répertoire `too_much_boxes` laissé à part, exclu du train/val de détection — décision motivée, pas juste "on verra plus tard".** Raison technique précise : `encode_detection_targets` (Story 7.1) ne rejette pas une image à plus de `max_boxes` — elle garde les N plus grandes boîtes (par aire) et **étiquette silencieusement le reste comme fond** (aucun pic de heatmap à ces positions). Pour une image à 70 avions, ~50 d'entre eux resteraient visuellement présents mais étiquetés "fond" par la vérité terrain — un vrai bruit d'étiquetage **contradictoire** (le même motif visuel, avion, tantôt positif tantôt négatif selon un critère d'aire sans rapport avec la tâche), pas juste une perte d'information neutre. Jugé trop risqué à introduire pendant que le modèle peine déjà à apprendre un signal cohérent sur le cas simple (0-20 boîtes non ambiguës, cf. sessions de debug `heatmap_prior`/`HeatmapActivation`, Stories 7.2/7.6 addendums).
- **[Discussion][Decision] `max_boxes` maintenu à 20, pas baissé à 10.** Le déséquilibre de classe combattu tout au long de cette session (π≈2,68e-5, mesuré Story 7.2 addendum) est dominé par la masse d'images à 1-2 avions (72,2% des images de `detection/train` n'ont qu'1 seule box, `tools/box_count.csv`) — baisser le plafond ne changerait quasiment rien à cette statistique globale, seulement au sort d'une poignée d'images à 11-20 boîtes, sans lien avec le plateau train/val actuellement observé (cause probable : dropout/volume/patience, déjà traité séparément). `max_boxes=20` était déjà choisi par l'utilisateur comme marge de sécurité au-dessus de sa propre estimation du cas réel ("rare d'avoir plus de 10 avions dans une vidéo") — le baisser retirerait cette marge sans bénéfice identifié.
- **[Discussion][Defer] Réutilisation future recommandée : jeu de stress-test dédié, pas perdu.** Une fois le décodage réel de boîtes en place (Epic 8, top-K/NMS-free), ces 242 images à densité extrême seraient un excellent test de saturation gracieuse (le modèle dégrade-t-il proprement sur une scène à 70 avions, ou n'importe comment ?) — à garder identifié séparément pour cet usage, pas juste ignoré.
- **[Discussion][Defer] Piste technique plus sophistiquée si le sujet revient** : modifier `encode_detection_targets` (Story 7.1) pour marquer les boîtes écartées (au-delà de `max_boxes`) comme une **zone à ignorer** dans la loss (ni positif ni fond, exclue du calcul) plutôt que de les traiter comme du fond silencieux — permettrait de réintégrer ce type d'images sans le bruit d'étiquetage contradictoire décrit ci-dessus. Changement de code réel (Story 7.1 `encode_detection_targets`/Story 7.3 fonctions de perte), pas une bascule de config — non traité maintenant, à instruire si ce sujet est repris.

## Deferred from: discussion organisation du repo avec Amelia (bmad-agent-dev) (2026-07-18)

**Constat déclencheur** : 7 fichiers `test_*.py` accumulés à la racine du projet depuis le début de l'Epic 7 (`tests/test_detection_target_encoding.py`, `tests/test_aircraft_detector_centernet.py`, `tests/test_centernet_loss.py`, `tests/test_fighterjet_detection_dataset_tools_v2.py`, `tests/test_centernet_detection_dataset.py`, `tests/test_centernet_detection_strategy.py`, `tests/test_jax_detector_config.py`), suivant la convention "script autonome, racine plate" déjà documentée dans ce projet (pas de framework de test formel — voir Testing Standards de chaque story Epic 7). L'utilisateur note à juste titre que le sujet dépasse les seuls tests : les scripts de génération de chunks `.npz` (`fighterjet_detection_dataset_tools.py`, `fighterjet_detection_dataset_tools_v2.py`, `fighterjet_classification_dataset_tools.py`, `cifar10_classification_dataset_tools.py`) et l'ensemble du dossier `tools/` (déjà un premier écart à la convention plate, mais lui-même hétérogène) mériteraient une vraie refonte d'organisation, pas un rangement ponctuel des seuls tests.

- **[Discussion][Defer] Réorganisation du repo (tests/, scripts de génération de données, tools/) — reportée à la fin de l'Epic 7 en cours, explicitement.** Pas un simple `mv` : chaque `test_*.py` importe aujourd'hui les modules testés (`from model_library import ...` etc.) en s'appuyant sur le fait d'être **dans le même dossier** que ces modules (résolution `sys.path` implicite de Python pour un script exécuté directement) — un déplacement vers un sous-dossier `tests/` casserait ces imports sans `sys.path.insert(...)` explicite (ou `conftest.py`/`PYTHONPATH` si adoption de pytest). Par ailleurs, plusieurs stories (Dev Agent Record / Debug Log References, Stories 7.1 à 7.7) citent des commandes exactes (`python3 tests/test_centernet_loss.py`, etc.) qui deviendraient obsolètes sans mise à jour. Une vraie refonte devrait couvrir en une seule fois : dossier `tests/` (avec gestion des imports), regroupement cohérent des scripts de génération de dataset (aujourd'hui à la racine, mélangés avec le code applicatif), et une passe de clarification sur `tools/` (déjà hétérogène — voir aussi la note existante plus bas sur `tools/boxes_process_manual_tkinter.py`, classe unique ~1220 lignes mélangeant GUI/logique métier/inférence). Ne pas traiter par petits bouts pour éviter des demi-mesures incohérentes.

## Deferred from: exécution réelle Story 7.8, entraînement JAX_DETECTOR (2026-07-17/18)

**Contexte** : `fighterjet_detection_dataset_tools_v2.py` combine `detection/train` + `classification/train` comme sources (`TRAIN_DIRS`, hérité tel quel de l'outil actuel `fighterjet_detection_dataset_tools.py` — même choix pour l'ancien UNet et le nouveau CenterNet, jamais remis en question avant cette session).

- **[Discussion][Question ouverte] Inclure `classification/train` aide-t-il vraiment CenterNet, comme cela avait clairement aidé UNet ?** Chiffres mesurés (`tools/box_count.csv` = `detection/train`, `tools/comptage_classes_train_val.csv` = `classification/train`, générés par l'utilisateur via `tools/reporting_dataset_pandas.py`) : `detection/train` seul = 97 531 images, 1,716 boxes/image en moyenne, **72,2% à 1 seule box / 27,8% à 2-20 boxes** (vraie diversité multi-avions). `classification/train` = 116 507 images, essentiellement 1 avion/image par construction (32 classes équilibrées, sans rapport avec la position réelle des boîtes). Combiné : ~214 038 images (proche des 211 266 mesurées à la génération réelle des chunks JAX_DETECTOR, écart ~1,3% probablement dû à un état de dataset légèrement différent entre ces CSV et le scan complet), **~87% à 1 seule box** contre 72% sur `detection/train` seul.
  - **Pourquoi ça compte plus pour CenterNet que pour UNet** : UNet apprend une segmentation dense (tout le contour de l'avion, signal spatialement étendu, peu sensible à un biais de position). CenterNet apprend un unique pixel-pic par objet — bien plus sensible à un biais de position si l'écrasante majorité des exemples d'entraînement a son centre réel proche du centre géométrique de l'image (cas typique d'une photo de classification, sujet unique cadré/centré). `classification/train` ne contribue en outre **aucun** exemple multi-avions, alors que c'est précisément le cas motivant tout ce chantier (FR3, fusion de boîtes sur formations serrées).
  - **Observation déclenchante** : `archive/final_detection_centernet_vis.png` (rapport qualitatif `generate_reports`, Story 7.6) — le pic de heatmap vrai est net et localisé sur l'avion (qui remplit quasi tout le cadre, cas typique d'une image de `classification/`), sans qu'on puisse encore juger si le modèle apprend une vraie localisation par contenu ou un raccourci de position.
  - **Statut** : non tranché. L'utilisateur va tester l'entraînement (probablement `detection/train` seul vs combiné) et communiquera le résultat. Si `detection/train` seul s'avère suffisant/meilleur, envisager de retirer `classification/train`/`classification/val` de `TRAIN_DIRS`/`VAL_DIRS` dans `fighterjet_detection_dataset_tools_v2.py` (Story 7.4) — nécessiterait une régénération complète des chunks `JAX_DETECTOR`, hors périmètre de cette note.

## Deferred from: discussion qualité de code avec Amelia (bmad-agent-dev) (2026-07-17)

**Contexte** : pendant l'attente d'un entraînement Colab long (Story 7.8), Aymeric a ouvert une discussion (pas une revue de diff) sur la qualité de code du projet dans son ensemble — le constat de départ : « une accumulation au cours du temps de programmes, de méthodes, plus ou bien écrit, sans vraiment normer les fichiers ou l'architecture du projet ». Question posée : SonarQube serait-il une bonne solution pour un projet BMAD comme celui-ci ?

- **[Discussion][Defer][Décision] SonarQube écarté pour ce projet — pas par principe, par inadéquation constatée.** Trois raisons concrètes : (1) projet solo, pas d'équipe — l'essentiel de la valeur de Sonar (gate de qualité bloquant un merge d'un autre contributeur, dashboard partagé) ne s'applique pas quand chaque diff est déjà relu par le même mainteneur ; (2) **aucun pipeline CI/CD** dans ce projet (confirmé, `_bmad-output/implementation-artifacts/7-1-...md` § Testing Standards : validation par comparaison baseline/diff manuelle) — le mécanisme d'enforcement principal de Sonar (quality gate bloquant) a besoin d'un CI pour exister, en construire un seulement pour Sonar serait disproportionné pour un projet ML qui itère vite sur Colab ; (3) **inadéquation avec la classe de bugs réelle de ce projet** — l'historique de cette seule session (Epic 7) : signe inversé dans une formule de rayon gaussien (Story 7.1), double-négation dans une focal loss (Story 7.3), mismatch de rank sur `jax.lax.reduce_window` (anticipé Story 8.3), offset demi-pixel oublié dans un rescale (anticipé Story 8.4), désynchronisation taille/image sur un zoom clampé (Story 7.5), `chunk_size` faisant exploser la RAM (Story 7.8) — tous des bugs de correction numérique/géométrique que l'analyse statique de Sonar (complexité cyclomatique, duplication, code smells) ne détecte pas. Ce qui les a trouvés cette session : revue indépendante à froid (modèle différent, sans contexte d'implémentation) + vérification manuelle des calculs, pas un linter.
- **[Discussion][Defer] Alternatives proposées, par ordre de rapport effort/valeur — à reprendre après l'epic en cours (Epic 7, et plus largement l'initiative JAX Single-Pass) :**
  1. **`ruff`** (linter+formatter Python tout-en-un) — coût quasi nul, zéro serveur, exécutable en pre-commit hook ou manuellement. Aurait attrapé au moins un cas réel rencontré cette session (variable de boucle `i` non utilisée dans `fighterjet_detection_dataset_tools_v2.py`, hint IDE ignoré faute d'outil de lint systématique).
  2. **Consolider les scripts `test_*.py` existants (convention déjà en place, assert+print) en suite `pytest` découvrable** — rendrait l'investissement test déjà fait réutilisable (`pytest test_*.py` au lieu d'exécutions une par une), ouvrirait la porte à une vraie couverture si un jour souhaitée.
  3. **Écrire la norme avant de vouloir la mesurer** — le vrai problème nommé par Aymeric est l'absence de convention établie (fichiers, architecture), pas un déficit d'outil de mesure ; aucun outil ne peut mesurer une conformité à une règle qui n'existe pas. `bmad-generate-project-context` (déjà dans le catalogue BMAD de ce projet) scanne le code existant pour produire un `project-context.md` — point de départ naturel pour capturer les conventions réellement en usage, à durcir consciemment ensuite.
  4. **Un audit ponctuel du code accumulé (pas un dashboard continu)** via `bmad-code-review` — **précision importante trouvée en vérifiant le mécanisme du skill (`steps/step-01-gather-context.md`) avant de le proposer** : ce skill est conçu pour un **diff**, pas un balayage de repo entier ; au-delà de ~3000 lignes de diff il avertit et propose un découpage par groupes de fichiers. État du projet au moment de cette discussion : **12 542 lignes, 48 fichiers Python** (racine + `tools/`, hors `archive/`) — un audit complet nécessiterait ~4-5 passes (mode « liste de fichiers » du Step 1, contenu complet traité comme diff via `git diff --no-index /dev/null <fichier>`), chacune lançant Blind Hunter + Edge Case Hunter en parallèle. Estimation basée sur les temps réels observés cette session pour des revues indépendantes comparables (subagents Opus, ~1,5 à 4 min de calcul chacune) : **~30 à 50 minutes de travail actif au total**, réalisable en tâche de fond pendant un futur entraînement long, par groupes validés un par un.
  5. **`mypy`/annotations de type** — piste plus lourde (zéro annotation de type actuellement dans le projet), à considérer plus tard ; aurait pu attraper mécaniquement la classe de bug trouvée en Story 7.7 (`main.py:87-88`, `sample_train[1].shape` cassant sur un dict au lieu d'un tenseur) plutôt qu'à la lecture manuelle.

## Deferred from: code review of story 7-1 (2026-07-16)

- **[Review][Defer] Le plafond `max_boxes` trie par aire en pixels SOURCE, pas en pixels `target_size`** [`detection_target_encoding.py:144`] — `sorted(boxes, key=lambda b: b[2]*b[3], reverse=True)[:max_boxes]` classe les boîtes par `bw*bh` dans le repère image source. Le rescale réel (1920×1080 → 224×224) est anisotrope (échelle x ≠ échelle y), donc « garder les 20 plus grandes » peut conserver un jeu différent des 20 plus grandes dans le repère `target_size` (une boîte large-et-basse et une boîte étroite-et-haute peuvent s'inverser après rescale). Non bloquant : le plafond lui-même est une heuristique acceptée (« silent-cap-at-20-by-design »), et ce n'est qu'un cas limite (>20 boîtes sur une image). Corrigeable trivialement en triant sur l'aire re-scalée si le sujet est repris.
- **[Review][Defer] Centre de boîte hors cadre silencieusement rabattu sur le bord (taille non rabattue)** [`detection_target_encoding.py:160-161`] — `np.clip(round(cx), 0, W-1)` force un centre hors `[0,W-1]×[0,H-1]` sur le pixel de bord le plus proche, mais la taille `(w,h)` stockée reste la taille complète non rabattue ; le décodage reconstruit alors une boîte décalée de plus que la tolérance de 1px. Les boîtes dégénérées (`w<=0`) sont explicitement écartées, mais les centres hors cadre ne le sont pas — choix de traitement incohérent et non documenté. Cas limite (les annotations sont normalement dans le cadre) ; à trancher si des boîtes partiellement hors champ apparaissent dans le dataset.
- **[Review][Defer] `decode_detection_targets` est une double boucle Python O(H·W)** [`detection_target_encoding.py:201-208`] — ~50k itérations par image en 224², avec un `.max()` par pixel. Acceptable pour le scope hors-ligne annoncé (validation round-trip), mais le module invite à la réutilisation (« chargeur Story 7.5 si besoin ») ; à vectoriser (`scipy.ndimage.maximum_filter` ou équivalent) si jamais appelé à l'échelle du dataset.
- **[Review][Defer] Division par zéro si `orig_w` ou `orig_h` vaut 0** [`detection_target_encoding.py:148-151`] — `bx/orig_w` / `by/orig_h` non gardés ; une fiche d'annotation malformée (dimension source nulle) fait planter la préparation dataset. Échec bruyant (pas de corruption silencieuse), donc faible priorité ; un garde explicite avec message clair serait plus propre pour un module « source unique de vérité ».

## Deferred from: bmad-create-story exhaustive analysis for Story 7.1, JAX Single-Pass (2026-07-16)

- **[Story-prep][Defer][Correction] `compute_grid_loss`/`compute_grid_loss_multilevel` (`loss_functions.py`) are NOT dead code removed at Epic 3 — they are still present and actively imported by `task_strategies.py` (`DetectionStrategy.compute_loss`, branches `loss_method in {"grid", "grid_multilevel"}`).** Found while reading `loss_functions.py` for Story 7.1's context (verifying nothing exists to reuse for the new heatmap+size loss). This corrects a false claim that had propagated into `notes-jax-single-pass.md` and `epics.md` (Story 7.3 AC, since fixed) — Epic 3's actual scope (Story 3.3) only deleted `bounding_boxes_with_classification_from_benchmark.py` and its `decode_grid_and_detect`, a different symbol. Verified via `grep`: no entry in `dataset_configs.py` (`FIGHTERJET_CLASSIFICATION`, `FIGHTERJET_DETECTION`, `JAX_KEPLER`, `CIFAR10`) sets `loss_method` to `"grid"`, `"grid_multilevel"`, or `"v7"` — so `compute_grid_loss`, `compute_grid_loss_multilevel`, and likely `compute_v7_loss` are genuinely unreachable in practice today, same class of dead-code-in-practice as the `SophisticatedCNN64Plus`/`MiniUNet` removals earlier this project — but not actually removed. Not fixed here (out of scope for Story 7.1 and JAX Single-Pass generally, which don't touch `DetectionStrategy`, AD-20 non-regression) — a candidate for a future dead-code cleanup cycle if picked up.

## Deferred from: bmad-party-mode review (Code Review Crew) of Epic 8 stories, JAX Single-Pass (2026-07-15)

- **[Party][Defer] Chargement de checkpoints `.pkl` via `pickle` depuis un dossier Google Drive synchronisé — pas de garde-fou sur fichier corrompu/tronqué** — soulevé par Vex (persona sécurité) en revue de l'Epic 8. `load_jax_model`/`load_detection_model` (`inference_utils.py`, AD-1/AD-3 de la spine parente) désérialisent des `.pkl` sans validation d'intégrité ; ces fichiers vivent sur `MyDrive/jax_supervised_training/` (renommé Epic 6), synchronisés par un pipeline Colab/rclone. Un fichier partiellement synchronisé/corrompu produirait un comportement de désérialisation non défini plutôt qu'une erreur explicite garantie. Pas un risque d'attaque (projet solo, pas de surface d'entrée non fiable) mais un gap de robustesse pré-existant, hors périmètre d'Epic 7/8 (aucune story de JAX Single-Pass ne touche `load_jax_model`/`load_detection_model` eux-mêmes). Rattaché à AD-3 de la spine parente (`architecture-JAX_Detection-2026-07-12/ARCHITECTURE-SPINE.md`, robustesse de chargement de checkpoint) — à traiter comme un renforcement de cette AD si ce sujet est repris, pas comme une nouvelle story de ce chantier.

## Deferred from: code review of group_a.diff (2026-07-12)

- **[Review][Defer][Réévalué le 2026-07-19, JAX Single-Pass] Constantes de batch size dupliquées et non synchronisées — risque désormais scindé en deux, structurellement résolu côté nouveau pipeline.** `inference_utils.py` définit toujours `_CLF_BATCH_SIZE = 32` et `_DET_BATCH_SIZE = 32` en privé, utilisées uniquement par `predict_crops_batch`/`decode_segmentation_and_detect_batch` — les fonctions de **l'ancien pipeline** (`FIGHTERJET_DETECTION`, UNet+classification séparée), protégé par AD-20 (non-régression, sans modification). Vérifié par grep (2026-07-19) : le nouveau pipeline Single-Pass (`build_single_pass_predict_fn`, Story 8.6) ne les appelle plus du tout — il passe par `jax.vmap` directement sur la shape reçue, sans chunking interne. Le `BATCH_SIZE=8` de `bounding_boxes_with_classification_from_video_generation.py` est donc désormais une constante autonome, sans lien caché avec `_CLF_BATCH_SIZE`/`_DET_BATCH_SIZE` — le risque de recompilation silencieuse décrit initialement ne s'applique plus à ce script. **Reste ouvert uniquement côté ancien pipeline** (`tools/audit_dataset_detection.py` BATCH_SIZE=32, `tools/audit_dataset_classification.py` BATCH_SIZE=64) — sciemment non corrigé : AD-20 interdit de toucher ces fonctions, et il n'y a plus de bénéfice réel à le faire maintenant que le chemin de production (Single-Pass) n'y est plus exposé.

- **[Review][Defer] `_CROP_MARGIN_PERCENT` figée en constante privée, non exposée en paramètre** — même catégorie que ci-dessus. Valeur (0) inchangée par rapport au comportement pré-refactor (déjà une constante globale de script figée à 0, jamais appelée avec une autre valeur). Pas de régression fonctionnelle ; juste une perte de configurabilité déjà présente avant ce refactor.

- **[Review][Defer][Corrigé le 2026-07-15] `tools/boxes_process_manual_tkinter.py` conservait des wrappers JIT inline dupliquant `build_predict_fn`/`build_clf_predict_fn`** — `jax.jit(lambda x: det_model.apply(...))` / `jax.jit(lambda x: clf_model.apply(...))` (`load_jax_models`, lignes ~1082-1083) remplacés par les fonctions canoniques de `inference_utils.py`. Vérifié avant de changer le contrat de `clf_predict_fn` (raw logits → `(probs, pred_indices)`, `build_clf_predict_fn`) : `predict_crops_batch` (`inference_utils.py:219-226`) détecte déjà les deux contrats (`isinstance(prediction_output, tuple)`) — aucun changement de comportement, juste le softmax/argmax déplacé à l'intérieur du JIT au lieu d'être fait implicitement en aval. `import jax` devenu inutile dans `load_jax_models` retiré au passage. Reste de la duplication interne au fichier (une seule classe `PhotoViewer` de ~1220 lignes mélangeant GUI/logique métier/inférence ML) — non traité, voir note ci-dessous pour un futur cycle si le sujet est repris.

- **[Nouveau, 2026-07-15][Repris le 2026-07-24] `tools/boxes_process_manual_tkinter.py` — `PhotoViewer` est une classe unique de ~1220 lignes** (gestion tkinter, zoom/drag, CRUD des boîtes, persistance JSON, ET chargement/inférence des modèles JAX, tout mélangé). Repéré en corrigeant l'item ci-dessus. Pas un défaut fonctionnel — le fichier marche — mais une vraie opportunité de séparation des responsabilités. Session dédiée tenue le 2026-07-24 (Winston) : analyse complète (1284 lignes lues intégralement) + stratégie de refactorisation incrémentale en 5 étapes, voir `refactor-boxes-process-manual-tkinter.md` (document vivant, suivi étape par étape). Tracké aussi dans `sprint-status.yaml` (`epic: "refactor-boxes-tkinter-2026-07-24"`). Ne plus mettre à jour cette entrée — le document dédié est désormais la source de vérité pour ce chantier.

## Deferred from: code review of Epic 5 (CIFAR-10) (2026-07-14)

- **[Review][Defer] `_get_export_path` a perdu son garde-fou pour un `checkpoint_path` de type dossier** (`task_strategies.py`, `ClassificationStrategy`) — l'ancien code redirigeait vers un nom de fichier sûr si `checkpoint_path` contenait `"checkpoints"` sans extension `.pkl`. Vérifié : aucune des 4 configs actuelles (`FIGHTERJET_CLASSIFICATION`, `FIGHTERJET_DETECTION`, `JAX_KEPLER`, `CIFAR10`) ne déclenchait ce garde-fou — code mort en pratique. Le nouveau fallback dérivé de `dataset_name` couvre mieux le vrai risque (absence de `checkpoint_path`) que l'ancien garde-fou ne le faisait pour son cas plus étroit. Pas une régression ; à surveiller si une future config venait à passer un chemin de dossier par erreur.
- **[Review][Defer] `cifar10_classification_dataset_tools.py` n'a aucune vérification d'intégrité du téléchargement** (pas de checksum, pas de reprise/retry sur `urllib.request.urlretrieve`, pas de validation du schéma des batches désérialisés). Risque réel pour un script prévu pour tourner sans supervision de façon répétée, mais faible probabilité de corruption *silencieuse* (un tar.gz tronqué ou un pickle corrompu font généralement échouer bruyamment l'extraction/désérialisation plutôt que de produire des données fausses en silence). Le script est explicitement cadré comme un spike de validation rapide du pipeline (Epic 5), pas un outil de données durci — à revisiter si la génération CIFAR-10 devient une étape récurrente/CI.

## Deferred from: code review of CIFAR10 augmentation/patience/batch tuning (2026-07-14)

- **[Review][Defer][Corrigé le 2026-07-14] `label_smoothing` et `mixup_alpha` non-nuls simultanément sur `FIGHTERJET_CLASSIFICATION` (`dataset_configs.py:103-104`) — `label_smoothing` y était mort depuis l'introduction de mixup.** `task_strategies.py:110-114` (`ClassificationStrategy.preprocess_batch`) est un `if mixup_alpha>0 ... elif label_smoothing>0` strictement exclusif : `rng` est toujours non-`None` à l'appel (`trainer.py:174`), donc la branche mixup gagne systématiquement dès que `mixup_alpha>0`, et `label_smoothing` ne s'exécute jamais dans ce cas. `FIGHTERJET_CLASSIFICATION` avait les deux à des valeurs non-nulles (`0.15`/`0.05`) avec des commentaires suggérant que les deux avaient été validés indépendamment ("✅ Aide légèrement" / "✅ OPTIMAL") — la mention "aide légèrement" pour `label_smoothing` était donc suspecte : soit testé à une époque où `mixup_alpha` était à 0, soit jamais réellement vérifié isolément. Découvert en propageant le même pattern (par erreur) sur `CIFAR10` lors de ce tuning.
  - **Fix appliqué** : `label_smoothing` retiré de `FIGHTERJET_CLASSIFICATION` (`dataset_configs.py:103`, commit suivant celui-ci) — comportement runtime strictement inchangé (`config.get("label_smoothing", 0.0)` valait déjà 0.0 en effet réel), vérifié par `dataset_configs.DATASET_CONFIGS['FIGHTERJET_CLASSIFICATION'].get('label_smoothing', 0.0) == 0.0` avant/après. Aucun risque de régression, modèle en production non affecté.
  - **Suite [2026-07-14, même session]** : `task_strategies.py:110-118` corrigé pour combiner mixup et label smoothing au lieu du `if/elif` exclusif — quand `mixup_alpha>0`, le smoothing standard (`y*(1-f) + f/K`) s'applique désormais aux labels one-hot déjà mixés par `mixup_batch` (mathématiquement valide : la formule est linéaire, s'applique identiquement à des labels durs ou mixés, la somme reste 1 par ligne). Vérifié par test unitaire direct de `ClassificationStrategy.preprocess_batch` : sortie combinée diffère de mixup seul, les 3 chemins existants (smoothing seul, ni l'un ni l'autre, `is_training=False`) restent inchangés. `label_smoothing=0.15` testé sur `CIFAR10` d'abord (résultat négatif là-bas, non transférable — modèle/dataset trop différents) puis sur `FIGHTERJET_CLASSIFICATION` directement (modèle en production, checkpoint `best_model.pkl` sauvegardé par l'utilisateur avant le run).
  - **[Clôturé, 2026-07-14] Résultat FIGHTERJET_CLASSIFICATION : positif, combinaison conservée.** `archive/training_classification_log_128x4_mixup_smoothing.txt` — comparaison directe contre `archive/training_classification_log.txt` (même config exacte : batch=128×4, float16, seule différence = smoothing réellement actif au lieu de mort) : **0.9521 val accuracy (0.9522 confusion matrix) contre 0.9458 référence, soit +0.63 pt**. Écart train/val sain (~8-9 pts), early stopping propre à l'epoch 30 (patience=5 depuis le meilleur epoch 25), pas de dégradation par classe. `checkpoint_path: "best_model.pkl"` (le fichier consommé par `bounding_boxes_with_classification_from_video_generation.py` en inférence réelle) a été écrasé par ce run — c'est le nouveau modèle de production. Config finale : `label_smoothing=0.15` + `mixup_alpha=0.05` tous les deux actifs sur `FIGHTERJET_CLASSIFICATION`.

## Deferred from: revue adversariale + edge-case hunter, spec augmentation zoom-plein-cadre JAX_DETECTOR (2026-07-19)

- source_spec: `_bmad-output/implementation-artifacts/spec-jax-detector-fullframe-zoom-augmentation.md`
  summary: L'augmentation zoom-plein-cadre tire au sort uniformément (probabilité fixe par image), sans cibler spécifiquement les images où la boîte cible est déjà petite relative au cadre — sur une image où l'objet remplit déjà presque tout le cadre, la variante générée est quasi un doublon de l'originale, gaspillant le budget d'augmentation.
  evidence: Le diagnostic motivant cette story (`tests/diagnose_single_full_size_aircraft.py`) porte spécifiquement sur la sous-représentation des objets plein-cadre — une sélection pondérée par le ratio (aire boîte / aire image) cibleraerait l'effort d'augmentation là où il compte, au lieu de le diluer uniformément. Raffinement v2 légitime, hors scope de la story actuelle (opt-in simple avec probabilité fixe).
- source_spec: `_bmad-output/implementation-artifacts/spec-jax-detector-fullframe-zoom-augmentation.md`
  summary: Une annotation malformée avec largeur ET hauteur négatives simultanément (produisant une aire `w*h` positive par erreur de signe) peut faire sélectionner cette boîte dégénérée comme "cible" (plus grande aire) au lieu d'une boîte valide plus petite, faisant échouer `_generate_fullframe_zoom_variant` (retour `None`) pour toute l'image au lieu de se rabattre sur la meilleure boîte valide.
  evidence: Trouvé par revue adversariale du diff. Edge case réel mais peu probable (nécessite une corruption de données à 2 signes simultanément incohérents) vu que les annotations de ce dataset sont produites par un pipeline amont de confiance ; conséquence bornée (aucune exception, juste pas de variante générée pour cette image), pas de corruption de données silencieuse.

## Deferred from: audit qualité de code avec Amelia — Groupe 1/4 (inference_utils.py, model_library.py, loss_functions.py, task_strategies.py, detection_target_encoding.py, checkpoint_manager.py, utils.py) (2026-07-19)

**Contexte** : reprise de la discussion qualité de code du 2026-07-17 (alternatives à SonarQube), pendant un entraînement Colab long. Audit ponctuel via `bmad-code-review` (mode `no-spec`, tout le code accumulé traité comme un faux diff `git diff --no-index /dev/null <fichier>`), Blind Hunter + Edge Case Hunter en parallèle, chaque finding vérifié contre le code réel (call sites, reachability) avant triage — pas pris pour argent comptant.

- source_spec: none
  summary: `original_size=(1920,1080)` codé en dur dans `_rescale_boxes`/`build_single_pass_predict_fn` (`inference_utils.py`), jamais dérivé de la résolution réelle de l'image d'entrée — toute image non-1920×1080 produirait un rescale de boîtes silencieusement faux.
  evidence: Touche au contrat de sortie AD-15/AC3 déjà figé (Story 8.6) — changer ça (paramétrer la résolution) est une décision d'architecture, pas un patch mécanique. En pratique inoffensif tant que le pipeline vidéo reste 1920×1080 natif (canonicalisé en amont).
- source_spec: none
  summary: Aucun NMS (Non-Maximum Suppression) appliqué sur les 20 boîtes du pipeline single-pass — deux pics locaux voisins peuvent produire 2 boîtes fortement chevauchantes pour le même objet.
  evidence: Contrat de sortie AD-15 figé (20 slots fixes, Story 8.6) — ajouter un NMS changerait la sémantique de `valid_mask`/le nombre de détections réelles, décision d'architecture à instruire, pas un patch.
- source_spec: none
  summary: `decode_segmentation_and_detect`/`_batch` (`inference_utils.py`, ancien pipeline UNet) dupliquent presque intégralement la même logique (resize, morphologie, contours), avec une divergence non expliquée sur la taille du kernel de fermeture morphologique (9×9 vs 21×21).
  evidence: Appartient à l'ancien pipeline `FIGHTERJET_DETECTION`, protégé par AD-20 (non-régression, sans modification) — risqué à toucher sans bénéfice pour le pipeline de production actuel (Single-Pass).
- source_spec: none
  summary: `load_detection_model` (`inference_utils.py`) réinitialise silencieusement les `batch_stats` manquants à 0/1 sur un checkpoint mal sauvegardé — un simple print, aucun signal programmatique renvoyé à l'appelant pour détecter un modèle chargé en mode dégradé.
  evidence: Gap de robustesse réel (déjà noté par une revue party-mode antérieure, Epic 8) mais le bon comportement (lever une exception ? juste mieux logguer ?) est un choix de design, pas un fix mécanique.
- source_spec: none
  summary: 3 fonctions nommées "predict_fn" dans `inference_utils.py` ont 3 contrats de sortie différents (logits bruts / tuple probs+indices / dict à 5 clés) sans convention de nommage qui les distingue.
  evidence: Cosmétique/lisibilité, pas un bug — renommer toucherait de nombreux call sites pour un bénéfice marginal.
- source_spec: none
  summary: `AircraftDetectorUNet` et `AircraftDetectorCenterNet` (`model_library.py`) dupliquent ~90 lignes d'encodeur/décodeur quasi à l'identique, sans classe de base/mixin partagé.
  evidence: Refactor de fond touchant 2 modèles entraînés séparément (dont un protégé AD-20) — réel mais gros, pas un patch ponctuel.
- source_spec: none
  summary: `compute_grid_loss`/`compute_grid_loss_multilevel`/`compute_v7_loss` (`loss_functions.py`, ~350 lignes cumulées) contiennent des commentaires de raisonnement à voix haute laissés en prod, et un `.at[].set()` calculé puis immédiatement écrasé sans jamais être utilisé (calcul pur gaspillage).
  evidence: Déjà identifié comme dead-code-in-practice (aucune config active n'utilise `loss_method` grid/grid_multilevel/v7, note de revue Story 7.1 du 2026-07-16) — le gaspillage de calcul est dans du code déjà mort, sans impact réel. Candidat pour un futur nettoyage dead-code plutôt qu'un patch isolé.
- source_spec: none
  summary: `compute_size_regression_loss` (`loss_functions.py`) documente en prose un changement cassant (linéaire→log-scale, 2026-07-18) rendant tout checkpoint antérieur silencieusement incompatible, sans aucun garde-fou exécutable (pas de champ de version dans les checkpoints, `checkpoint_manager.py`).
  evidence: Réel gap de robustesse mais nécessite une décision de design (où/comment versionner les checkpoints) — pas un patch mécanique.
- source_spec: none
  summary: `TaskStrategy.export_model` (`task_strategies.py`) avale toute exception silencieusement (`except Exception as e: print(...)`) — un échec de sauvegarde de checkpoint (disque plein, permissions) devient invisible, l'entraînement continue comme si le meilleur modèle avait été sauvegardé.
  evidence: Réel, mais le comportement correct (relever ? juste logguer plus fort ?) est un choix de design à trancher, pas un fix mécanique évident.
- source_spec: none
  summary: Aucune validation de `loss_method` à la construction des strategies (`task_strategies.py`) — une faute de frappe dans la config n'est détectée que loin de sa source, à l'intérieur d'une fonction JIT-compilée.
  evidence: Toucherait la construction de toutes les strategies (plusieurs classes), changement plus large qu'un patch isolé.
- source_spec: none
  summary: `generate_reports` dupliqué entre `CenterNetDetectionStrategy` et `DetectionStrategy` (`task_strategies.py`) — logique de visualisation quasi identique (resize, colormap, hconcat, imwrite).
  evidence: Refactor de fond (extraire un helper partagé), réel mais pas urgent.
- source_spec: none
  summary: `KeplerStrategy.generate_reports` (`task_strategies.py`) a un contrat d'appel incohérent avec le reste du fichier — attend `(outputs, _)` du modèle alors que les autres strategies attendent une sortie unique, et accède à `batch['images']`/`batch['labels']` (dict) alors que `preprocess_batch` de la même classe reçoit `images, targets` en positionnel.
  evidence: `JAX_KEPLER` reste une config active dans `dataset_configs.py` (pas supprimée) — bug potentiel réel si ce chemin est réutilisé, mais nécessiterait un test avec de vraies données Kepler avant de patcher à l'aveugle (caution établie sur ce projet : ne pas modifier du code d'entraînement sans vérification empirique).
- source_spec: none
  summary: Images Kepler de forme inattendue (`task_strategies.py`, `KeplerStrategy`) non gardées — un `else` manquant pourrait transmettre une forme non aplatie au `Conv1D` en aval sans erreur explicite.
  evidence: Même remarque que ci-dessus — `JAX_KEPLER` actif mais peu utilisé récemment dans cette session ; à vérifier avec de vraies données avant de patcher.
- source_spec: none
  summary: `compute_loss` a une signature `**kwargs` incohérente entre strategies (`task_strategies.py`) — 4 sur 5 l'ont, `KeplerStrategy.compute_loss` non. Pas un bug actif aujourd'hui (vérifié : `trainer.py` n'appelle jamais que `use_onehot_labels=...`, nommé explicitement dans les 5 signatures), mais fragile si un futur appel générique ajoutait un autre kwarg.
  evidence: Vérifié par lecture de `trainer.py:192/220` — les seuls call sites réels passent uniquement `use_onehot_labels`, donc pas de `TypeError` atteignable aujourd'hui. Incohérence de style à corriger si `KeplerStrategy` est retouchée pour la raison ci-dessus.
- source_spec: none
  summary: `resume_training` (`checkpoint_manager.py`) recrée le schedule optax avec des hyperparamètres codés en dur (`warmup_steps=500, decay_steps=5000, end_value=1e-6`) qui doivent correspondre exactement à ceux du chemin d'entraînement "à froid", sans partage ni vérification croisée.
  evidence: Risque réel de désynchronisation silencieuse, mais le fix (partager la source de vérité du schedule) touche à `trainer.py` également, pas un patch isolé à `checkpoint_manager.py` seul.
- source_spec: none
  summary: `utils.py::create_eval_step` (utilisée par `trainer.py` — pas du code mort, contrairement à ce qu'affirmait initialement Blind Hunter avant vérification) injecte un RNG de dropout même en évaluation (`training=False`) et jette la sortie mutée (`logits, _ = ...`) — travail inutile, pas un bug de résultat (le dropout est déjà désactivé par `training=False`).
  evidence: Cosmétique/performance mineure, pas une erreur de correction — mesuré comme négligeable, pas prioritaire.
- source_spec: none
  summary: Plusieurs fonctions utilitaires (`smooth_labels`/`mixup_batch`/`tree_div` dans `utils.py`, `_gaussian_radius`/`encode_detection_targets` dans `detection_target_encoding.py`) n'ont pas de garde sur leurs paramètres numériques (factor, alpha, denom, min_overlap, orig_w/orig_h) — division par zéro ou domaine invalide possible en théorie.
  evidence: Ces fonctions ne sont appelées qu'avec des valeurs de configuration fixes (jamais d'input utilisateur/variable) — reachability réelle très faible aujourd'hui. Grouper en un futur passage de garde-fous défensifs plutôt que des patches isolés à faible valeur immédiate.

## Deferred from: audit qualité de code avec Amelia — Groupe 2/4 (data_management.py, dataset_configs.py, trainer.py, main.py, reporting.py) (2026-07-19)

**Contexte** : suite directe du Groupe 1/4 (même session, même méthode `bmad-code-review` mode `no-spec`). Blind Hunter + Edge Case Hunter en parallèle sur les 5 fichiers de la couche données/config/orchestration (~2947 lignes). Chaque finding vérifié contre le code réel (call sites, reachability, y compris confirmation que `confusion_matrix_from_state`, `show_predictions_from_dir` et toute la classe `DetectionReporter` n'ont aujourd'hui aucun point d'appel dans le reste du dépôt) avant triage.

- source_spec: none
  summary: `dataset_configs.py::get_dataset_config` mute et retourne par référence le dict partagé au niveau module `DATASET_CONFIGS[dataset_name]` (ajout de `dataset_name`/`task_type` en place) — tout appelant qui mute ensuite le config retourné corrompt le singleton partagé pour le reste du process.
  evidence: Le correctif évident (retourner une copie profonde) change la sémantique/perf de chaque appel — pas un patch mécanique sans trancher si un appelant a besoin de la référence partagée. Aucun appelant actuel ne mute réellement le config retourné (vérifié), donc inoffensif aujourd'hui.
- source_spec: none
  summary: `data_management.py` — `DetectionDataset`/`CenterNetDetectionDataset` batchent le split de validation avec `drop_remainder=True` (identique au train), contrairement à `ChunkManager` (classification) qui utilise `drop_remainder=False` pour le val — perte silencieuse des derniers échantillons de validation à chaque epoch, biais sur la métrique val rapportée.
  evidence: Possiblement un choix délibéré (forme de batch statique pour éviter un retracing JIT sur le dernier batch partiel de `evaluate()`) — nécessite de trancher entre exactitude de la métrique et coût de recompilation, pas un patch mécanique.
- source_spec: none
  summary: `data_management.py` — l'augmentation "zoom" de `CenterNetDetectionDataset` (crop central + resize `nearest` sur heatmap/size) peut, si le crop aléatoire exclut le pixel-pic, produire un échantillon augmenté sans aucune supervision positive (heatmap entièrement à zéro), silencieusement.
  evidence: Corriger nécessiterait de garantir que le crop contient toujours le centre de la boîte — changement réel de logique d'augmentation, pas un patch ponctuel.
- source_spec: none
  summary: `main.py` — l'initialisation matérielle (`jax.default_backend()`, `jax.config.update(...)`, sélection du dtype) s'exécute au moment de l'import du module, pas dans `main()`/sous `if __name__`. Un simple `import main` déclenche donc la sélection de backend JAX comme effet de bord. La branche `else` (non-TPU) suppose en plus systématiquement un GPU présent et force `jax_platform_name="gpu"` même si le backend réel est `"cpu"`.
  evidence: Restructurer proprement (passer `backend`/`dtype` en paramètres plutôt qu'en globals de module) est un choix d'architecture, pas un fix d'une ligne. Risque concret uniquement sur une machine sans GPU/TPU — jamais le cas en pratique sur ce projet (poste local GPU + Colab GPU/TPU).
- source_spec: none
  summary: `reporting.py` — `confusion_matrix_from_pkl` et `confusion_matrix_from_state` dupliquent ~80 lignes de logique quasi identique (collecte dataset → sous-échantillonnage → prédiction par batch → matrice de confusion → precision/recall/F1). `confusion_matrix_from_state`, `show_predictions_from_dir` et toute la classe `DetectionReporter` (visualisation batch de détection) n'ont aujourd'hui **aucun point d'appel** ailleurs dans le dépôt (vérifié par grep) — `show_predictions_from_dir` reste néanmoins un utilitaire d'appel manuel documenté ("Valeurs par défaut pour Spyder"), probablement utilisé interactivement, contrairement à `confusion_matrix_from_state`/`DetectionReporter` qui semblent réellement orphelins.
  evidence: Décision à trancher : supprimer le code mort (`confusion_matrix_from_state`, `DetectionReporter`) ou le garder et dédupliquer avec `confusion_matrix_from_pkl` — supprimer du code est un choix hors mandat d'un audit no-spec. Ces fonctions mortes contiennent aussi de vrais bugs latents non patchés ici faute d'être atteignables (ex. `DetectionReporter.visualize_batch` plante par division par zéro sur un batch vide ; `plt.subplots(1,1,...)` renvoie un seul `Axes` sans `.flatten()`).
- source_spec: none
  summary: `trainer.py` — `visualizer.plot_training_curves(...)` (figure matplotlib complète + `savefig`) s'exécute sans throttling à **chaque** epoch, alors qu'un concept `vis_freq` existe déjà dans `dataset_configs.py` pour les tâches de détection mais n'est pas réutilisé ici.
  evidence: Ajouter un throttling nécessite de choisir une politique (sauter N epochs ? toujours tracer la dernière ?) — pas un fix mécanique évident.

**Note méthodologique** : deux affirmations initiales des reviewers, corrigées après vérification du code réel — (1) le risque "loss NaN silencieusement sauvegardée comme meilleur modèle" (Blind Hunter) ne se produit pas : une comparaison Python avec NaN est toujours `False`, donc `is_better` reste `False` en mode `min` ; le vrai gap est l'absence d'avertissement diagnostique, patché séparément. (2) `checkpoint_manager.py::resume_training` retournant `state=None` (Edge Case Hunter) est inatteignable : la fonction ne retourne `None` que si `checkpoint is None`, mais `trainer.py` ne l'appelle que sous une garde `checkpoint is not None`.

## Deferred from: audit qualité de code avec Amelia — Groupe 3/4 (pipeline de génération de données et vidéo : bounding_boxes_with_classification_from_video_generation.py, dataset_builder/*, plusieurs tools/*) (2026-07-19)

**Contexte** : suite du Groupe 2/4. 13 fichiers, ~2879 lignes (script de génération vidéo à la racine, 4 fichiers `dataset_builder/`, 8 scripts `tools/` de génération/nettoyage de dataset). Blind Hunter + Edge Case Hunter en parallèle. Vérification ciblée (pas exhaustive fichier par fichier comme les Groupes 1-2, compte tenu du volume et de la nature "scripts utilitaires ponctuels" de la plupart de ces fichiers) avant triage. `dataset_builder/fighterjet_detection_dataset_tools.py` exclu de tout patch : protégé AD-20 (ancien pipeline UNet, non-régression), malgré plusieurs findings réels identifiés dedans (variable `USE_GRAYSCALE` morte, paramètre `max_boxes` jamais utilisé, glob de nettoyage de chunks trop large).

- source_spec: none
  summary: Aucun seed sur `random.shuffle`/`np.random.shuffle` dans les scripts de génération/split de dataset (`fighterjet_classification_dataset_tools.py`, l'augmentation zoom de `jax_detector_dataset_tools.py`, `move_excess_to_detection.py`) — chaque régénération produit un split train/val ou un sous-ensemble "excess" différent, non reproductible.
  evidence: Ajouter un seed partout changerait le split réel de données utilisées pour entraîner des checkpoints déjà existants — pas neutre, décision à prendre consciemment plutôt qu'un patch silencieux.
- source_spec: none
  summary: Chemins absolus codés en dur dans les 13 fichiers du groupe — aucun n'est exécutable sur une autre machine sans édition manuelle du source.
  evidence: Le projet a déjà un pattern d'override par variable d'environnement pour les configs d'entraînement (`DATA_ROOT`, `dataset_configs.py`) mais ces scripts `tools/`/`dataset_builder/` ne le suivent pas — généraliser le pattern est un chantier transverse, pas un patch fichier par fichier.
- source_spec: none
  summary: Opérations destructives irréversibles sans dry-run (`os.remove`, `shutil.move`/`os.rename` avec écrasement silencieux en cas de collision de nom) dans plusieurs scripts (`duplicate_image_detection_and_normalization.py::process_all_files`, `build_fixed_aircraft_dataset.py`, `find_and_deduplicate.py`) tournant directement sur ce qui semble être la seule copie d'un dataset annoté à la main.
  evidence: Un mode dry-run/staging est un vrai changement de fonctionnalité (pas un patch mécanique) — à instruire si l'utilisateur veut sécuriser ces scripts avant leur prochaine utilisation réelle.
- source_spec: none
  summary: Trois outils de déduplication incompatibles coexistent (`duplicate_image_detection_and_normalization.py` par hash perceptuel, `duplicate_files_find.py` et `find_and_deduplicate.py` par nom de base seul sans vérification de contenu) — enchaîner deux de ces outils sur le même dossier peut produire des résultats contradictoires.
  evidence: Unifier nécessite de choisir une seule définition de "doublon" — décision de design, pas un patch.
- source_spec: none
  summary: `bounding_boxes_with_classification_from_video_generation.py` — si `render_thread_func` lève une exception (image malformée, erreur GPU), le thread meurt silencieusement ; `writer_thread` puis le thread principal se bloquent indéfiniment sur `queue.get()`/`queue.put()`, sans aucun message d'erreur ni traceback.
  evidence: Remonter proprement une exception entre threads (sentinelle d'erreur dans la queue, ou `threading.excepthook`) est un changement de structure, pas un patch d'une ligne — à haute valeur pour un script destiné à tourner longtemps sans supervision (génération vidéo complète).
- source_spec: none
  summary: `dataset_builder/cifar10_classification_dataset_tools.py` — pas de vérification d'intégrité de l'archive téléchargée avant extraction, et `tarfile.extractall` sans le paramètre `filter=` (protection path-traversal, disponible Python 3.12+).
  evidence: Script de téléchargement one-shot depuis une source connue (Kaggle/CIFAR officiel) — risque réel mais faible en pratique ; à vérifier si l'environnement `jax_env` tourne en Python ≥3.12 avant d'ajouter `filter=` (sinon `TypeError` sur un argument inexistant).
- source_spec: none
  summary: `tools/build_fixed_aircraft_dataset.py` — l'appel réel à `build_fixed_dataset(...)` dans `__main__` est laissé à l'intérieur d'une chaîne triple-quote inerte : exécuter ce script ne fait donc que du reporting, jamais de build réel, malgré son nom.
  evidence: Pourrait être un mode "dry-run par défaut" volontaire plutôt qu'un oubli — à vérifier auprès de l'utilisateur avant de "réactiver" un appel qui déplacerait potentiellement de vraies données.
- source_spec: none
  summary: `dataset_builder/fighterjet_classification_dataset_tools.py` — l'appel à `has_overlapping_boxes(...)` dans `process_dataset_stretched` est commenté (désactivé), mais le `print` de `__main__` annonce "Maintenant avec détection de superposition" — le programme prétend faire quelque chose qu'il ne fait pas.
  evidence: Réactiver la fonctionnalité changerait le comportement réel du filtrage de dataset (des images seraient exclues) — décision utilisateur sur l'intention réelle, pas un patch mécanique.
- source_spec: none
  summary: `dataset_builder/fighterjet_detection_dataset_tools.py` (protégé AD-20) contient plusieurs findings réels non patchés par prudence : variable `USE_GRAYSCALE` déclarée et jamais utilisée (le vrai contrôle passe par `config.get("grayscale", True)`), paramètre `max_boxes` accepté mais jamais utilisé dans le corps de la fonction, glob de nettoyage des anciens chunks (`dataset_detection_*_chunk*.npz`) sans portée limitée au run/split courant.
  evidence: AD-20 interdit explicitement de modifier ce fichier (pipeline UNet legacy, non-régression) — findings réels mais volontairement laissés tels quels.
- source_spec: none
  summary: Volume restant de findings de robustesse mineurs sur les 13 fichiers (try/except manquants autour d'E/S fichier individuelles, absence de validation de schéma JSON avant accès aux clés, edge cases sur images de taille dégénérée, style : imports locaux dans une boucle, `tqdm` recréé par sous-dossier) — env. 60 findings bruts au total entre les deux reviewers, dont ceux listés ci-dessus et les 8 patches appliqués couvrent les plus significatifs.
  evidence: Chacun individuellement à très faible valeur/risque sur des scripts utilitaires ponctuels (pas le chemin d'entraînement production) — non détaillés un par un ici pour rester lisible ; revue disponible en entier dans la session si besoin d'y revenir.

## Deferred from: audit qualité de code avec Amelia — Groupe 4/4 (dernier) (reste de tools/ : audit/conversion/inspection, hors boxes_process_manual_tkinter.py) (2026-07-19)

**Contexte** : dernier groupe de l'audit ponctuel initié le 2026-07-17. 12 fichiers, ~1962 lignes. `tools/boxes_process_manual_tkinter.py` (1284 lignes, classe unique GUI/logique métier/inférence) explicitement exclu par l'utilisateur — une session dédiée séparée lui sera consacrée. `tools/audit_dataset_detection.py` protégé AD-20 (consommateur du pipeline UNet legacy) — tout finding dessus déféré, jamais patché.

- source_spec: none
  summary: `tools/audit_dataset_detection.py` (protégé AD-20) — plusieurs findings réels non patchés par prudence : paramètre `box_aera_min`/constante `BOX_AERA_MIN` (faute de frappe "AERA" pour "AREA" figée dans une signature de fonction), `calculate_mean_iou` est un matching glouton sans assignation un-à-un (plusieurs GT peuvent matcher la même prédiction, les faux positifs ne pénalisent jamais le score), `convert_bbox_format` réimplique une géométrie de rescale indépendante (stretch x/y) sans jamais la comparer au vrai pipeline de prétraitement du modèle — si celui-ci letterbox/pad plutôt que d'étirer, tous les chiffres d'IoU de cet outil d'audit sont silencieusement faux.
  evidence: AD-20 interdit explicitement de modifier ce fichier. Le point sur `convert_bbox_format` est potentiellement le plus sérieux du lot (fausserait un outil de mesure de qualité, pas juste un bug applicatif) — à instruire séparément si l'utilisateur veut fiabiliser cet audit un jour, en dehors du cadre AD-20 (ex: nouvel outil plutôt que modifier l'existant).
- source_spec: none
  summary: Taxonomies de classes divergentes et non validées entre convertisseurs qui alimentent le même dataset final — ex. `convert_aeroscan_aircraft_to_json.py` utilise `"b1"` pour un avion que `convert_military_aircraft_to_json.py` mappe en `"b1b"` (`CLASS_MAPPING`, dictionnaires de 40-90 entrées tapés à la main, zéro validation croisée entre eux ni avec la liste canonique de `dataset_configs.py`).
  evidence: Risque réel de pollution du dataset par quasi-doublons de catégories, mais unifier les taxonomies est une décision de nommage à trancher par l'utilisateur (quelle graphie est la bonne ?), pas un patch mécanique.
- source_spec: none
  summary: `convert_yolo8_to_json.py::CLASS_MAPPING[70] = "unknown"` — un vrai label identifié en commentaire ("Mi-26 Halo") est sciemment renvoyé dans le fourre-tout "unknown" sans avertissement au moment de la conversion, indistinguable d'un ID réellement inconnu.
  evidence: Comportement peut-être intentionnel (classe jugée non pertinente) — décision utilisateur à confirmer avant de changer le mapping ou d'ajouter un avertissement.
- source_spec: none
  summary: Validation de coordonnées YOLO manquante dans les 4 convertisseurs YOLO→JSON (`convert_aeroscan_aircraft_to_json.py`, `convert_HRPlanesv2_aircraft_to_json.py`, `convert_military_aircraft_to_json.py`, `convert_yolo8_to_json.py`) — une ligne de label malformée (coordonnées hors [0,1] ou largeur/hauteur ≤0) produit silencieusement une bbox dégénérée dans le JSON de sortie plutôt que d'être ignorée.
  evidence: Motif identique répété dans 4 fichiers différents — un vrai correctif mécanique (ajouter le même garde-fou partout), mais volontairement pas fait dans ce passage pour rester dans le temps imparti à ce groupe ; bon candidat pour un futur passage ciblé "un seul motif, plusieurs fichiers".
- source_spec: none
  summary: Collision de nom de fichier JSON possible entre deux images de même `base_name` mais d'extensions différentes (ou provenant de sous-dossiers walkés différents), dans plusieurs convertisseurs (`convert_aeroscan_aircraft_to_json.py`, `convert_HRPlanesv2_aircraft_to_json.py`, `convert_military_aircraft_to_json.py`, `convert_yolo8_to_json.py`) — écrase silencieusement les annotations de la première image.
  evidence: Le correctif (inclure l'extension ou un hash dans le nom de fichier JSON) changerait la convention de nommage déjà en place sur des données existantes — pas neutre, décision à prendre plutôt qu'un patch silencieux.
- source_spec: none
  summary: Absence de confirmation/dry-run avant `shutil.move`/`os.remove` dans certains scripts et incohérence du garde-fou `input()` y/N entre convertisseurs — déjà en partie traité ce groupe (ajout de la garde `__main__` manquante sur `convert_Military_Aircraft_Detection_Dataset.py` et `convert_Military_Aircraft_Detection_Dataset_Yolo.py`), mais l'ajout d'un vrai prompt de confirmation cohérent avec les autres convertisseurs n'a pas été fait (changerait l'UX/bloquerait un usage non-interactif).
  evidence: Décision de design (faut-il un prompt partout, ou un flag `--yes`/`--dry-run` générique ?) plutôt qu'un patch mécanique.
- source_spec: none
  summary: `tools/inspect_pickle.py` — logique d'inspection dupliquée 3 fois quasi à l'identique (`params`, `batch_stats`, `model_state['batch_stats']`) au lieu d'un inspecteur générique/récursif, chemin de checkpoint codé en dur, aucun argument CLI.
  evidence: Script de debug ponctuel, faible valeur à refactorer maintenant ; aucun bug actif trouvé par les deux reviewers (le seul fichier du groupe sans finding).
- source_spec: none
  summary: Volume restant de findings de robustesse mineurs sur les 12 fichiers (validation de colonnes CSV/DataFrame manquante avant accès, gestion `EOFError` absente sur les prompts `input()` non-interactifs, `except Exception` généralisés sans compteur d'erreurs agrégé dans le résumé final, `split('_')[0]`/`.replace('.jpg', ...)` fragiles pour dériver un nom de base) — une quarantaine de findings bruts au total entre les deux reviewers, dont ceux listés ci-dessus et les 7 patches appliqués couvrent les plus significatifs.
  evidence: Chacun individuellement à très faible valeur/risque sur des scripts utilitaires ponctuels à usage manuel occasionnel (pas le chemin d'entraînement production) — non détaillés un par un ici ; revue disponible en entier dans la session si besoin d'y revenir.

## Deferred from: discussion performance JAX Single-Pass avec Winston (bmad-architecture) (2026-07-19)

**Contexte** : question de l'utilisateur — le training de `JAX_DETECTOR` (CenterNet) est nettement plus long que l'ancien `FIGHTERJET_DETECTION` (UNet), et il envisageait de réduire `max_boxes` de 20 à 10 (peu de cas réels à plus de 10 avions) pour accélérer le training, avec une intuition d'un gain ~2x côté inférence par analogie avec `bounding_boxes_with_classification_from_video_generation.py`. Analyse faite en vérifiant le code réel (pas de spéculation) : deux paramètres `20` distincts et indépendants existent dans le projet, source de la confusion initiale.

- **[Discussion][Conclusion vérifiée] `max_boxes` (20→10, `dataset_configs.py`/`detection_target_encoding.py::encode_detection_targets`) n'a AUCUN effet sur la vitesse de training.** `encode_detection_targets` produit des tenseurs cibles denses de forme fixe `(H,W,1)` heatmap + `(H,W,2)` size, indépendante du nombre de boîtes — aucune boucle par-boîte dans le forward/backward (`AircraftDetectorCenterNet`, `compute_centernet_loss`). `max_boxes` ne fait que plafonner combien de boîtes réelles sont gardées comme vérité terrain sur les (rares) images à plus de `max_boxes` annotations (même compromis que la discussion `too_much_boxes` du 2026-07-18) — une décision d'étiquetage, pas un levier de performance training.
  - **Causes réelles vérifiées du training plus long** (`dataset_configs.py`) : `epochs=15` (JAX_DETECTOR) vs `epochs=8` (FIGHTERJET_DETECTION, ~2x à lui seul) + `zoom_augment_probability=0.3` (activé récemment par l'utilisateur, ajoute **~30%** d'échantillons/epoch via la variante zoomée sur ~30% des images — chiffre corrigé le 2026-07-19 après mesure réelle sur `archive/training_jax_detector_rv7.txt` : 992 steps/epoch mesurés vs 761 avant l'augmentation, 992/761=1.304 ; l'estimation initiale "~15%" de cette même note était fausse, voir l'entrée dédiée plus bas sur le bug `decay_steps` que cet écart a révélé). Architecture vérifiée identique entre les deux modèles (`model_library.py` : `AircraftDetectorCenterNet` a un encodeur/décodeur strictement identique à `AircraftDetectorUNet`, seules les têtes de sortie diffèrent) — écarte l'hypothèse "nouveau modèle plus lourd".
- **[Discussion][Implémenté + mesuré 2026-07-19] Le vrai levier pour l'inférence était `k=20` dans `inference_utils.py::_top_k_boxes`/`build_single_pass_predict_fn`, PAS `max_boxes`.** Le pipeline JIT (`build_single_pass_predict_fn`) traite systématiquement les `k` slots (crop + classification) même s'il n'y a qu'un seul avion réel détecté. Rendu paramétrable le jour même : `build_single_pass_predict_fn(k=...)` (défaut `20`, inchangé pour tous les appelants existants, aucun réentraînement requis — `k` est un choix d'inférence pur, le modèle produit toujours un heatmap dense indépendant de `k`) ; `bounding_boxes_with_classification_from_video_generation.py` expose `MAX_BOXES` (à côté de `BATCH_SIZE`/`FRAME_STRIDE`) branché sur ce paramètre.
  - **Mesuré en conditions réelles** (vidéo réelle, 7 avions, utilisateur, 2026-07-19) : `MAX_BOXES=20` → 23.84s (25.13 it/s) ; `=10` → 20.57s (29.12 it/s, ~14% plus rapide) ; `=7` → 20.39s (29.37 it/s) ; `=2` → 19.89s (30.11 it/s). **Rendement fortement décroissant après k=10** (~3% de mieux entre 10 et 2, pour 5x moins de slots) — confirme que le forward du détecteur (coût fixe, indépendant de `k`) et le pré-traitement/rendu CPU (également fixes, ~6-7.5s de pré-traitement CPU stable sur les 4 mesures) dominent le temps mur une fois `k` déjà réduit à ~10 ; la sous-étape crop+classification cesse d'être le facteur limitant en dessous de ce seuil.
  - **Recommandation** : `k=10` est le point d'équilibre identifié — capture l'essentiel du gain mesuré sans réduire la marge de sécurité sur les scènes chargées (au-delà de `k` avions réels dans une frame, les excédents sont silencieusement perdus, seuls les pics de heatmap les moins confiants). Descendre en dessous n'apporte quasiment rien de plus.
  - **Attention si `k` change par défaut ou dans un autre appelant** : touche le contrat de sortie figé **AD-15/AC3** de la spine `architecture-jax_supervised_training-2026-07-15` (initialement 20 slots fixes `{boxes, classes, class_scores, detection_scores, valid_mask}`) — plusieurs consommateurs en dépendent (tests, éventuellement d'autres scripts). Le défaut `k=20` de `build_single_pass_predict_fn` a été conservé pour ne rien casser ; seul `bounding_boxes_with_classification_from_video_generation.py` utilise `MAX_BOXES` pour l'instant. Si `k=10` (ou une autre valeur) devient un nouveau défaut de facto pour d'autres consommateurs, formaliser la mise à jour de la spine (nouveau AD ou amendement d'AD-15/AC3) plutôt que de le laisser implicite.

## Deferred from: analyse training rv7 (zoom_augment_probability) et bug decay_steps (2026-07-19)

**Contexte** : bilan demandé par l'utilisateur sur `archive/training_jax_detector_rv7.txt` (premier training réel avec `zoom_augment_probability=0.3`). Comparaison chiffre à chiffre avec `archive/training_jax_detector_regression_loss_v6.txt` (run juste avant, sans l'augmentation) : rv7 est moins bon (meilleur HeatmapActivation val 0.1438 vs 0.1652, plateau quasi plat dès l'epoch 10/15) alors que v6 progressait encore nettement à l'epoch 15.

- **[Bug identifié et corrigé] `decay_steps` (TPU, `dataset_configs.py::JAX_DETECTOR`) était resté calé sur l'ancien volume de données après l'activation de `zoom_augment_probability=0.3`.** L'ancienne valeur (`11415 = 761 steps/epoch × 15`) datait du run v4, AVANT l'augmentation. Le run rv7 tourne réellement à 992 steps/epoch (+30%, confirmé exactement par le calcul 992/761=1.304) mais le schedule cosine (`warmup_steps=400` + l'ancien `decay_steps=11415`) atteignait donc son plancher (LR≈1e-6) au step 11815 — en plein milieu de l'epoch 12/15 avec le nouveau volume — gelant l'apprentissage pour les ~3 dernières epochs. Corrigé à `decay_steps=14880` (992×15, mesuré réellement sur rv7) le 2026-07-19. **Conséquence importante : le mauvais résultat de rv7 n'est probablement PAS un verdict sur la stratégie zoom_augment elle-même — c'est un training bridé prématurément par ce bug. Il faudra relancer avec le decay_steps corrigé avant de juger l'apport réel de l'augmentation.**
  - Même classe de bug que celui déjà documenté sur CIFAR10 (Epic 5, Addendum 3) — un `decay_steps` codé en dur et dérivé d'un `steps/epoch` mesuré manuellement à un instant donné, jamais recalculé automatiquement quand le volume de données change.
- **[Discussion][✅ IMPLÉMENTÉ 2026-07-21] `decay_steps` calculé automatiquement depuis le vrai volume de données, plutôt que codé en dur par backend dans `dataset_configs.py`.** Déclenché par un **3ᵉ incident réel** du même bug (CIFAR10 Epic 5, `JAX_DETECTOR rv7` après activation de `zoom_augment_probability`, et `JAX_DETECTOR v8` après un changement de `micro_batch_size` 128→256 en cours de session — le decay_steps recalculé pour 992 steps/epoch ne correspondait plus aux 496 steps/epoch réels à `micro_batch_size=256`).
  - **Implémentation** : `trainer.py::_count_real_train_samples(output_prefix)` — compte les échantillons réels en ouvrant chaque chunk `{output_prefix}_train_chunk*.npz` (glob) et en lisant la forme de la première clé du npz (peu importe son nom — le premier axe est toujours N, quel que soit le schéma : classification, détection, detection_centernet, kepler). `Trainer.create_train_state` utilise ce compte pour calculer `decay_steps = (real_train_samples // micro_batch_size) × epochs` automatiquement, avec repli propre sur `backend_config.get("decay_steps", 6000)` si aucun chunk n'est trouvé sur disque (ex. contexte de test sans vraies données).
  - **Vérifié** sur les vrais chunks locaux de `JAX_DETECTOR` (10 chunks) : 127 096 échantillons réels → 992 steps/epoch × 15 epochs = **14880**, résultat identique au calcul manuel fait pour `rv7` à partir du log réel — confirme la justesse du calcul automatique. Repli propre vérifié aussi (`JAX_KEPLER`, 0 chunk local → `None`, pas de crash).
  - **Coût** : ~2 minutes pour décompresser les 10 chunks au démarrage du Trainer (une seule fois, négligeable face à un training de plusieurs heures).
  - **Conséquence** : ce calcul s'applique maintenant à **tous** les datasets (`FIGHTERJET_CLASSIFICATION`, `FIGHTERJET_DETECTION`, `JAX_KEPLER`, `CIFAR10`, `JAX_DETECTOR`), pas seulement `JAX_DETECTOR` — les valeurs `decay_steps` codées en dur dans `dataset_configs.py` restent en place comme repli (contexte sans données réelles) mais ne pilotent plus le training tant que les chunks existent sur disque.
- **[Correction]** Le `decay_steps` du bloc `gpu` de `JAX_DETECTOR` (`22845`, commentaire "97531 // 64 = 1523 steps/epoch") semble lui aussi calculé à partir d'un mauvais compte d'images : 97531 correspond au nombre d'images de `FIGHTERJET_DETECTION.detection/train` seul (voir note "exécution réelle Story 7.8" plus haut dans ce fichier), pas au vrai jeu d'entraînement `JAX_DETECTOR` (qui combine plusieurs sources + l'augmentation zoom). Non corrigé ici faute de mesure réelle sur GPU avec le dataset actuel — à vérifier/mesurer avant tout futur run GPU de JAX_DETECTOR.

## Deferred from: doute sur zoom_augment_probability + hypothèse "champ réceptif CenterNet" (2026-07-19) — 🟢 VALIDÉ (dilatation), 🔴 2ᵉ itération en cours (contexte global)

### Mise à jour 2026-07-19 (fin de journée) : décision prise, implémenté

Pistes 1 (distribution mesurée) et 3 (RF théorique calculé) convergeant vers l'hypothèse champ réceptif, l'utilisateur a tranché : partir directement sur le bottleneck dilaté plutôt que de d'abord relancer un training "baseline propre" juste pour confirmer le bug `decay_steps` (déjà corrigé de toute façon, inutile de payer un run TPU rien que pour ça).

**Implémenté** : `AircraftDetectorCenterNet` (`model_library.py`, bottleneck) — les 2 `Conv 3×3` non-dilatées remplacées par `kernel_dilation=(2,2)` puis `kernel_dilation=(4,4)`. `AircraftDetectorUNet` non touché (AD-20 respecté, classe séparée). `jax-single-pass.mmd` mis à jour avec un nouveau sous-graphe `ARCH` détaillant chaque couche et sa résolution (224→112→56→28), bottleneck marqué comme la proposition.

**Vérifié** (pas de training réel lancé, juste la mécanique) :
- Tous les tests existants passent inchangés (`test_aircraft_detector_centernet.py`, `test_centernet_loss.py`, `test_centernet_detection_strategy.py`, `test_peak_extraction_topk.py`, `test_detector_inference_composition.py`).
- Nombre de paramètres strictement identique (1 927 907 — la dilatation ne change pas la forme des noyaux, seulement leur espacement).
- Forward+backward complet sur données synthétiques : loss finie, gradients finis.

**⚠️ Important** : `best_model_jax_detector.pkl` existant (entraîné SANS dilatation) se charge toujours sans erreur dans la nouvelle architecture (les formes de paramètres sont identiques) — mais ces poids n'ont pas été entraînés pour ce nouveau contexte de dilatation, les utiliser tels quels ne serait pas significatif. **Un vrai réentraînement from scratch est nécessaire avant toute utilisation réelle** — c'est prévu, pas encore fait (training réel à lancer par l'utilisateur sur son infra TPU/Colab).

**Contexte** : suite directe de l'analyse du run `rv7` ci-dessus. Malgré l'explication `decay_steps`, l'utilisateur reste sceptique sur le fond de la stratégie `zoom_augment_probability` (ressentie comme de la "bidouille") et avance un argument chiffré : `tools/box_count.csv` (dataset `detection/train`, 97 637 images) montre que les images à **1 seule boîte** sont déjà 72,2% du dataset (70 470/97 637) — largement majoritaires, pas rares. Son raisonnement : si le cas "un seul avion" est déjà archi-représenté, en rajouter via l'augmentation zoom n'a pas de sens et ne peut qu'ajouter de la confusion. Conclusion de l'utilisateur : le vrai problème de CenterNet sur les avions plein-cadre est probablement ailleurs (architecture), pas un manque de données — UNet n'a jamais eu besoin de cette béquille. **Décision immédiate** : `zoom_augment_probability` remis à `0.0` (défaut) dans `dataset_configs.py::JAX_DETECTOR` par l'utilisateur — le code de l'augmentation (`_generate_fullframe_zoom_variant`, `dataset_builder/jax_detector_dataset_tools.py`) est conservé (testé, sans effet quand désactivé) mais N'EST PLUS ACTIF. Pas de régénération de `.npz` nécessaire pour revenir en arrière tant que la probabilité reste à 0.

- **[Nuance apportée, à vérifier avant de conclure]** `box_count.csv` mesure le **nombre** de boîtes par image, pas leur **taille**. Le diagnostic original (`tests/diagnose_single_full_size_aircraft.py`, Epic 7/8) portait spécifiquement sur des images où un avion **remplit quasiment tout le cadre** (style photo de classification zoomée) — pas simplement "1 seule boîte, taille quelconque" (un avion photographié au loin dans un ciel large compte aussi comme "1 boîte" dans ce comptage). Les 72,2% d'images à 1 boîte du dataset `detection/train` ne sont donc pas forcément représentatives du cas "plein-cadre" qui posait problème — il faudrait croiser `box_count.csv` avec la taille réelle des boîtes (`box_l × box_h` rapporté à `image_width × image_height`) pour trancher si les avions PLEIN-CADRE sont eux-mêmes rares ou non. **Pas encore mesuré** — nécessiterait un nouveau passage sur les JSON d'annotation réels (`tools/reporting_dataset_pandas.py` a déjà les colonnes nécessaires : `box_l`, `box_h`, `image_width`, `image_height`, mais pas encore de calcul de ratio d'aire). Premier jalon concret à poser avant de rouvrir ce chantier.
- **[Hypothèse principale à instruire, proposée par Amelia/Claude] Le vrai problème pourrait être une limite architecturale de CenterNet sur les objets plein-cadre, pas un manque de données.** Les détecteurs par point central (heatmap + régression de taille dense) ont une faiblesse documentée dans la littérature sur les objets très grands relativement au cadre : le réseau doit intégrer de l'information sur (quasiment) toute l'image pour placer un seul pic de confiance correctement centré et régresser la bonne taille. `AircraftDetectorCenterNet` (`model_library.py`) n'a que 3 étages de sous-échantillonnage (bottleneck à 28×28 pour une entrée 224×224) — un champ réceptif potentiellement insuffisant pour "voir" un objet qui remplit tout le cadre. Si c'est la vraie cause, plus de données augmentées (zoom_augment ou autre) ne réglerait rien — c'est une question de capacité architecturale, pas de volume d'exemples. Cohérent avec l'observation de l'utilisateur : UNet (segmentation dense, pas de pic unique à localiser) n'a structurellement pas ce problème.
  - **Pistes de vérification concrètes, avant tout changement de code** :
    1. Mesurer la vraie distribution de taille de boîte (ratio d'aire boîte/image) sur `detection/train`, croisée avec `box_count` — confirme ou infirme si les cas plein-cadre sont réellement rares dans le dataset actuel (voir nuance ci-dessus). **✅ FAIT (2026-07-19), voir résultat ci-dessous.**
    2. Si des prédictions/erreurs de validation existantes sont disponibles, vérifier si l'erreur de régression de taille (`compute_size_regression_loss`) est disproportionnellement pire pour les grandes boîtes spécifiquement (pas juste en moyenne) — signature typique d'une limite de champ réceptif plutôt que d'un manque de données. **Pas encore fait.**
    3. Calculer/estimer le champ réceptif effectif de `AircraftDetectorCenterNet` (3 étages de pooling, noyaux 3×3) et le comparer à la taille d'un objet plein-cadre en pixels — un calcul de champ réceptif théorique, pas empirique, mais rapide et éclairant. **✅ FAIT (2026-07-19), voir résultat ci-dessous.**
  - **Statut** : hypothèse non testée, priorité très haute pour l'utilisateur — candidat sérieux pour être la prochaine epic (après clôture du sujet courant). Ne pas relancer `zoom_augment_probability` avant d'avoir au moins vérifié la piste 1 (sinon on répète la même confusion diagnostic).

### Résultat piste 1 : distribution réelle mesurée (2026-07-19)

Nouvelles fonctions `reporting_global_boxes_size(df)` et `reporting_boxes_between_size_ratio(df, min_size, max_size)` ajoutées à `tools/reporting_dataset_pandas.py` (ratio `box_l×box_h / image_width×image_height`, jamais en pixels bruts). Mesure sur `detection/train`+`detection/val` (145 812 boîtes valides) :

| Bucket (% surface image) | Total | Ratio_% |
|---|---|---|
| 0-1% | 2 278 | 1,56% |
| 1-5% | 4 839 | 3,32% |
| 5-15% | 7 898 | 5,42% |
| 15-30% | 12 073 | 8,28% |
| 30-50% | 50 422 | 34,58% |
| 50-70% | 45 738 | 31,37% |
| 70-100% | 22 564 | 15,47% |

Moyenne 48,17%, médiane 48,66%. **Vérification importante avant d'interpréter** : `dataset_builder/jax_detector_dataset_tools.py:354-357` confirme que `classification/train` est actuellement COMMENTÉ dans `TRAIN_DIRS` — cette mesure sur `detection/train` seul est donc exactement la distribution réelle vue par `JAX_DETECTOR` à l'entraînement (le run `rv7` analysé plus haut), sans dilution par le dataset de classification (dont la distribution, mesurée séparément par l'utilisateur, est très différente : médiane ~10%, très concentrée sur les petites boîtes).

**Conclusion : ~47% des boîtes couvrent déjà ≥50% de la surface de l'image — les objets plein-cadre ne sont PAS rares dans les données d'entraînement, ils sont majoritaires.** Ferme définitivement l'hypothèse "pas assez d'exemples plein-cadre" (déjà suspectée via `box_count.csv`, maintenant confirmée par la vraie mesure de taille). Et surtout : **ce résultat renforce l'hypothèse champ réceptif plutôt que de l'affaiblir** — un modèle abondamment exposé à des objets larges pendant l'entraînement qui échoue quand même spécifiquement sur ce cas à l'inférence pointe vers une limite de capacité architecturale, pas un manque de données.

### Résultat piste 3 : champ réceptif théorique calculé (2026-07-19)

`AircraftDetectorCenterNet` (`model_library.py`) : 3 étages `Conv 3×3 ×2 → MaxPool 2×2` puis bottleneck `Conv 3×3 ×2` à résolution 28×28 (stride cumulé ×8, entrée 224×224). Calcul du champ réceptif standard, couche par couche (formule `RF_new = RF_old + (kernel-1) × jump`, `jump` = produit des strides déjà appliqués) :

- Bloc 1 (avant pool) : RF=5, jump=1 → après pool : RF=6, jump=2
- Bloc 2 (avant pool) : RF=14, jump=2 → après pool : RF=16, jump=4
- Bloc 3 (avant pool) : RF=32, jump=4 → après pool : RF=36, jump=8
- Bottleneck (2 conv 3×3) : **RF≈68 pixels sur une entrée 224 pixels, soit ~30% de chaque dimension**

Le décodeur (upsampling bilinéaire + skip connections) n'élargit pas ce maximum — il réinjecte des features de niveaux plus superficiels ayant un RF encore plus petit (16px au niveau x2, 6px au niveau x1). Le "champ réceptif effectif" (ce qui pèse réellement dans le calcul, documenté dans la littérature comme nettement plus petit que le RF théorique) est donc probablement encore sous ces ~30%.

**Conclusion : un objet couvrant 50-100% de la surface (46,84% du dataset mesuré ci-dessus) a une étendue spatiale plus grande que ce qu'un seul neurone du bottleneck peut directement "voir".** Le réseau doit agréger l'information depuis plusieurs neurones du bottleneck ayant chacun une vue partielle et non-recouvrante de l'objet, sans mécanisme de pooling global ni d'attention pour le faire. C'est exactement la limite structurelle proposée par l'hypothèse, maintenant chiffrée par le calcul plutôt qu'affirmée depuis la littérature générale.

**Triangulation** : pistes 1 et 3 convergent vers la même conclusion (limite architecturale, pas un problème de données). Piste 2 (erreur de régression de taille empiriquement pire sur les grandes boîtes) resterait utile pour une triangulation complète mais n'est pas bloquante — la conviction est déjà jugée suffisante par l'utilisateur pour passer à la discussion de solution (voir prochaine entrée ou discussion de session).

### Mise à jour 2026-07-21/22 : dilatation validée par un training réel propre, mais insuffisante seule

**Run propre obtenu** (`decay_steps` correct, `.npz` régénérés sans `zoom_augment_probability`, batch=128, comparable directement à `v6`) :

| | v6 (non-dilaté) | run dilaté (2026-07-21) |
|---|---|---|
| HeatmapActivation val | 0.1652 | **0.1954** (+18% relatif) |
| Val Loss | 2.1596 | **1.9060** |
| Steps/epoch | 761 | 764 (quasi identique — confirme la régénération propre) |

Progrès réel et mesuré, attribuable à la dilatation (comparaison enfin propre, sans confusion). **Mais le problème ciblé persiste** : sur un répertoire de test réel, `JAX_DETECTOR` ne détecte des avions que sur 121/278 images, contre la quasi-totalité pour `FIGHTERJET_DETECTION` — cohérent avec le calcul : RF dilaté ≈59% reste sous ce qu'il faudrait pour couvrir un objet à 70-100% de la surface de l'image.

**[✅ IMPLÉMENTÉ 2026-07-22, en attente de training] Branche de contexte global ajoutée en complément de la dilatation** (pas en remplacement — la dilatation reste en place, validée). Recommandation d'Amelia : plutôt que pousser encore la dilatation (rendements décroissants, risque de "gridding" sur une grille 28×28) ou ajouter un 4ᵉ étage de sous-échantillonnage (RF toujours fini, jamais garanti), une branche de contexte global (style PSPNet) **garantit par construction une couverture à 100% de l'image**, indépendamment de tout empilement de couches.

- **Implémentation** (`model_library.py::AircraftDetectorCenterNet`, juste après le bottleneck dilaté) : `GlobalAvgPool` spatial du bottleneck → `(B,1,1,256)`, projeté (`Conv 1×1` + silu), rediffusé (`broadcast`) à chaque position `(B,28,28,256)`, concaténé avec les features locales `(B,28,28,512)`, puis reprojeté à 256ch (`Conv 1×1` + BatchNorm + silu) avant le décodeur. `AircraftDetectorUNet` non touché (AD-20).
- **Vérifié** (pas de training réel) : tous les tests existants passent inchangés ; paramètres 1 927 907 → **2 125 539** (+197 632, cohérent avec l'estimation ~197k pour 2 convs 1×1 ajoutées) ; forward+backward sur données synthétiques → loss finie, gradients finis, formes de sortie inchangées (`heatmap` 224×224×1, `size` 224×224×2).
- **`jax-single-pass.mmd`** mis à jour : la dilatation passe de 🔴 (proposition) à ✅ (validée), la branche de contexte global ajoutée comme 🔴 (proposition actuelle).
- **Statut** : implémenté, en attente du prochain training réel pour validation (comme la dilatation avant elle).

## Deferred from: analyse reprise de checkpoint + LR schedule (2026-07-21)

**Contexte** : question de l'utilisateur sur ce qui se passe au LR schedule si on reprend un training complet avec `epochs` augmenté (pour continuer un training qui n'aurait pas atteint de plateau). Vérifié empiriquement (mécanisme optax identique à `checkpoint_manager.py::resume_training`) : le LR **réellement appliqué** aux gradients reprend correctement à une valeur intermédiaire de la nouvelle courbe (ni le pic, ni gelé au plancher de l'ancien schedule) — parce que `resume_training` restaure `opt_state` tel quel (ligne 135), et c'est DANS `opt_state` qu'optax garde son propre compteur de step interne pour évaluer le schedule, indépendamment du `step` de Flax.

- **[Bug identifié, non corrigé — accepté tel quel par l'utilisateur]** `trainer.py:494-495` calcule le LR **affiché**/loggé (`current_lr`, stocké dans `self.history['learning_rate']`) à partir de `self.state.step` — qui repart bien à 0 à chaque reprise (`checkpoint_manager.py::resume_training` ne restaure jamais `step`, seulement `opt_state`). Donc pendant un run repris : le LR réellement utilisé pour l'entraînement est correct (vérifié), mais le LR **affiché dans les logs et l'historique/courbes** est faux — il sous-estime la progression réelle sur le schedule (donne l'impression d'un LR proche du pic alors que le vrai LR appliqué est plus bas). N'affecte que l'affichage/les courbes de suivi, pas la qualité de l'entraînement lui-même.
  - **Statut** : non bloquant, laissé tel quel à la demande de l'utilisateur (2026-07-21) pendant qu'un training réel est en cours. Correctif simple si repris un jour : restaurer aussi `step` depuis le checkpoint dans `checkpoint_manager.py::resume_training` (actuellement seul `opt_state` l'est).

## Deferred from: audit IoU JAX_DETECTOR v10 + diagnostic scores bruts (2026-07-22) — 🟢 RÉSOLU (seuil + NMS JAX-natif)

**Contexte** : `tools/audit_dataset_detection_jax.py` lancé sur le checkpoint `v10` (dilatation + contexte global) contre les 120 102 images annotées de `detection/` — premier vrai chiffre comparable JAX_DETECTOR vs FIGHTERJET_DETECTION. Résultat global : **IoU moyen 0.5273, 60.2% GOOD (IoU≥0.5)** — contre >70% pour FIGHTERJET_DETECTION (chiffre utilisateur, `tools/audit_dataset_detection.py`, AD-20).

- **Training v10 validé** (comparaison propre avec `v9`, même dataset, même discipline `decay_steps` automatique — 764 steps/epoch confirmés) : HeatmapActivation val 0.1954 (v9, dilatation seule) → **0.2022** (v10, + contexte global). Encore un vrai progrès, encore en légère progression à l'epoch 15.
- **Croisement IoU × taille de boîte** (échantillon stratifié GOOD/POOR, ~6000 images, taille = plus grande boîte de l'image en % de la surface) : la chute n'est pas progressive avec la taille — c'est une falaise, exclusivement sur **70-100%** (mean_iou≈0.07-0.075, ~7-8% GOOD, contre 43-75% GOOD sur tous les autres buckets 0-70%).
- **Diagnostic décisif** : `audit_dataset_detection_jax.py` étendu avec 2 colonnes (`max_raw_detection_score` = meilleur score parmi les 20 slots AVANT filtrage ; `best_raw_iou` = IoU du candidat le plus confiant contre la vérité terrain, même sous le seuil). Sur le bucket 70-100% : **81% de géométrie correcte** (`best_raw_iou≥0.5`) mais seulement **9.6%** dépassent le seuil de confiance actuel (0.2, médiane des scores bruts ≈0.108). **Ce n'est donc pas une limite de capacité géométrique (l'hypothèse champ réceptif reste valide comme diagnostic initial, mais dilatation+contexte global l'ont déjà largement résolue côté géométrie) — c'est un problème de calibration de confiance** : le réseau localise/dimensionne correctement les objets plein-cadre mais n'est jamais assez confiant pour les déclarer.
- **✅ Fix appliqué (2026-07-22, sans réentraînement)** : `dataset_configs.py::JAX_DETECTOR.detection_score_threshold` abaissé de `0.2` à `0.1`. Cette valeur était déjà documentée comme vérifiée sans faux positif (diagnostic `threshold_sensitivity`, `archive/`, antérieur) — 0.2 n'était qu'une marge de prudence non nécessaire. Simulation sur les 120 102 images de l'audit (estimation basse, ne capture que le meilleur candidat par score, pas les 20 slots) : **60.2% → ~76.9% GOOD estimé** — potentiellement suffisant pour combler l'essentiel de l'écart avec FIGHTERJET_DETECTION, sans toucher à l'architecture.
- **✅ Confirmé par ré-audit réel (2026-07-22)** : `detection_score_threshold=0.1`, même checkpoint `v10`, 120 102 images. **IoU moyen 0.5273→0.7058, GOOD 60.2%→82.9%** — au-dessus de l'estimation basse (76.9%), et au-dessus du repère FIGHTERJET_DETECTION (>70%, chiffre utilisateur).

### Mise à jour 2026-07-22 (suite) : faux positifs après abaissement du seuil — NMS JAX-natif ajouté

Question légitime de l'utilisateur : abaisser le seuil à 0.1 a-t-il fait exploser les faux positifs ? `audit_dataset_detection_jax.py` étendu avec `count_unmatched_predictions` (proxy de précision : combien de prédictions valides ne correspondent à AUCUNE vraie boîte). Résultat réel (120 102 images) : **23.4% de faux positifs (66 437/283 452 prédictions)**.

- **Diagnostic croisé par taille de boîte** : contrairement à l'intuition initiale de l'utilisateur (doublons sur les grands avions), le taux de faux positifs le plus élevé est sur le bucket **0-15%** (34.5%, scènes multi-avions denses, 3.18 vraies boîtes/image en moyenne) — pas sur 70-100% (9.3%, plutôt bas). **Mais** en isolant précisément les images à 1 seul avion réel très grand (≥70%, 743 images) : 28.3% ont 2 détections ou plus (jusqu'à 15) sur ce seul avion — le phénomène observé à l'œil par l'utilisateur est réel, mais localisé, pas le principal contributeur agrégé.
- **Cause mécanique identifiée** : `_gaussian_radius` (`detection_target_encoding.py`, Story 7.1) — le rayon de la cible d'entraînement grandit avec la taille de la boîte. Un grand avion produit donc un plateau large de score élevé sur le heatmap (pas un pic pointu) ; à seuil abaissé, plusieurs pics voisins de ce plateau dépassent maintenant le seuil simultanément pour le même objet. La prémisse d'AD-9 ("la tête par point central n'a pas besoin de NMS") ne tient que pour un objet de taille normale au pic étroit.
- **✅ Implémenté** : `inference_utils.py::_non_max_suppression_jax` — NMS **JAX-natif** (`jax.lax.fori_loop` sur k=`boxes.shape[0]` itérations fixes, aucun branchement dépendant des données), pour rester dans le principe "un seul JIT de bout en bout" du Single-Pass (question posée explicitement par l'utilisateur — la fonction `non_max_suppression` existante est en Python pur, AD-20, utilisée seulement par l'ancien pipeline, jamais réutilisable ici sans casser le JIT). Fonction distincte et non couplée à l'ancienne. Câblée dans `build_single_pass_predict_fn` juste après le filtre de boîtes dégénérées, combinée par ET logique à `valid_mask`. Nouveau paramètre `nms_iou_threshold=0.5`.
- **Vérifié** : tests existants inchangés passent (`test_single_pass_predict_fn.py`, `test_rescale_boxes.py`, `test_detector_inference_composition.py`, `test_peak_extraction_topk.py`) ; nouveau test unitaire de `_non_max_suppression_jax` (suppression correcte, JIT-compatible via `jax.jit`, cas limites) ; test de bout en bout sur images réelles de `test_media/single_full_size_aircraft/` — un vrai cas de doublon nettoyé (2→1 détections), jamais d'augmentation du nombre de détections.
- **Effet de bord à surveiller** : avec le NMS, `MAX_BOXES`/`k` (`build_single_pass_predict_fn`) doit être fixé avec une marge au-dessus du nombre d'objets réels attendus dans une scène — les doublons consomment des slots du Top-K **avant** le nettoyage NMS, donc un `k` trop serré peut encore faire disparaître un vrai objet distinct au profit des doublons d'un autre. Documenté dans le docstring de `build_single_pass_predict_fn`.
- **`jax-single-pass.mmd`** mis à jour : contexte global marqué validé (✅, v10), nouveau nœud NMS inséré dans le sous-graphe `DETECT` entre `RESCALE` et `CROP`.
### Mise à jour 2026-07-22 (suite 2) : audit complet post-NMS — doublons résolus, FP denses intacts (attendu)

Ré-audit complet réel (120 102 images, même checkpoint `v10`, NMS actif `nms_iou_threshold=0.5`) :

| | Sans NMS | Avec NMS |
|---|---|---|
| Prédictions totales | 283 452 | 234 433 (-17,3%) |
| Faux positifs (nb) | 66 437 | 59 995 (-9,7%) |
| Taux de FP | 23,4% | 25,6% |
| IoU moyen | 0,7058 | 0,7003 |
| GOOD (IoU≥0,5) | 82,9% | 82,5% |

- **Le taux de FP monte alors que le nombre absolu de FP baisse — cohérent, pas une régression.** Sur les 49 019 prédictions supprimées par le NMS, seules 6 442 étaient des faux positifs ; les ~42 577 autres correspondaient bien à une vraie boîte (doublons corrects sur le même objet — exactement le phénomène observé à l'œil par l'utilisateur sur les gros avions). Confirmé via le ratio global prédictions/vraies-boîtes (vérité terrain identique entre les deux runs, 209 444 boîtes) : **1,353 → 1,119** — le modèle produit désormais quasiment 1 prédiction par objet réel.
- **Croisement par densité de scène** (`num_true_boxes` par image, run avec NMS) : le taux de FP reste dominé par les scènes multi-avions (2-3 boîtes : 30,1% ; 4-6 : 26,6% ; 7+ : 21,5%) plutôt que par l'image à 1 seul avion (24,2%, en baisse grâce au nettoyage des doublons). Confirme que le NMS n'avait mécaniquement pas de prise sur ce problème : un faux positif isolé dans une zone vide ne chevauche généralement aucune autre boîte, donc rien à supprimer.
- **Légère baisse d'IoU moyen/GOOD (0,7058→0,7003 / 82,9%→82,5%)** : le NMS conserve la boîte au score de confiance le plus élevé, pas nécessairement celle au meilleur IoU — dans de rares doublons, la boîte supprimée était la mieux localisée des deux.
- **Conclusion** : NMS validé sur son objectif (doublons sur gros objets, cause mécanique `_gaussian_radius`/plateau large) sans effet de bord notable sur la qualité globale. Le problème des faux positifs en scènes denses/petits objets (bucket 0-15%, déjà identifié avant le NMS comme dominant) reste ouvert et inchangé — attendu, car de nature différente (confusion de détection, pas duplication).
- **Statut** : ✅ clos pour le volet NMS. Le volet FP-scènes-denses reste un chantier séparé, non entamé (voir aussi le diagnostic de calibration de confiance ci-dessus, qui pourrait partager une cause racine — non vérifié).

### Mise à jour 2026-07-22 (suite 3) : nms_iou_threshold exposé en config (AD-15)

`nms_iou_threshold` (jusque-là un simple paramètre Python de `build_single_pass_predict_fn`, défaut `0.5` en dur) déplacé vers `dataset_configs.py::JAX_DETECTOR`, même discipline que `detection_score_threshold` (AD-15 : seuil en config, jamais une constante privée dupliquée) — pour permettre à l'utilisateur de le retoucher sans modifier le code pendant ses tests vidéo.

- `inference_utils.py::build_single_pass_predict_fn` : `nms_iou_threshold=None` par défaut, résolu depuis `config_det["nms_iou_threshold"]` si non fourni — la possibilité de surcharge explicite est conservée (utile pour les diagnostics A/B, ex. `nms_iou_threshold=1.01` pour désactiver le NMS sans y toucher).
- Câblé explicitement (constante module + passage à `build_single_pass_predict_fn`) dans les 3 consommateurs qui construisent un `predict_fn` JAX_DETECTOR : `tools/audit_dataset_detection_jax.py`, `bounding_boxes_with_classification_from_video_generation.py`, `tools/bounding_boxes_with_classification_from_images_generation.py` (commentaire AD-9 obsolète corrigé au passage — JAX_DETECTOR a désormais un NMS explicite pour les objets plein-cadre).
- **Le training n'est pas concerné** : l'entraînement détection ne construit jamais de `predict_fn`/NMS, il optimise directement heatmap+taille (`CenterNetDetectionStrategy`) — le NMS est un mécanisme d'inférence pure, sans lien avec la fonction de perte.
- Vérifié : suite de tests existante inchangée (`test_single_pass_predict_fn.py`, `test_rescale_boxes.py`, `test_detector_inference_composition.py`, `test_peak_extraction_topk.py`, `test_jax_detector_config.py`) toujours au vert.

## Deferred from: audit qualité noyau d'entraînement avec Winston (bmad-agent-architect) (2026-07-22) — 🟡 EN BACKLOG, rien d'urgent

**Contexte** : rétro `quickdev-2026-07-22`, item d'action #2 ("repérer les méthodes dupliquées avant toute consolidation"). Demande d'Aymeric : évaluer la généricité réelle du noyau d'entraînement (racine du projet, pas seulement détection/classification aviation — CIFAR10 et Kepler existent pour prouver et exercer cette généricité) + inventaire qualité (code mort, duplication, nommage, lisibilité). Détail complet dans `quickdev-2026-07-22-training-core-quality-audit.md`.

- **Verdict rassurant sur le nommage** : aucune dérive camelCase/snake_case trouvée (vérifié par grep sur les 12 fichiers du noyau) — l'inquiétude d'Aymeric n'était pas fondée.
- **Verdict d'architecte** : le pattern `TaskStrategy` (Strategy) généralise réellement — prouvé par Kepler (tabulaire/1D) et CIFAR10 (vision simple) à côté de la détection/classification, pas un hasard heureux.
- **Deux défauts structurels identifiés, pas corrigés** :
  1. `_get_export_path`/`get_training_state_path` déclarées `@abstractmethod` dans `TaskStrategy` (`task_strategies.py`) alors que les 4 sous-classes les implémentent à l'identique — la frontière d'abstraction est mal placée, devrait être un défaut concret dans la classe de base.
  2. L'augmentation de données (`data_management.py`, flip/translation/zoom, ~90 lignes) est dupliquée intégralement entre `DetectionDataset` et `CenterNetDetectionDataset` au lieu d'être factorisée — l'augmentation géométrique est indépendante du type de payload (masque vs heatmap+taille), la dupliquer va contre l'objectif de généricité.
  3. **Trouvé en creusant** (pas dans la demande initiale) : `KeplerStrategy.compute_loss` n'accepte pas `**kwargs`, contrairement aux 3 autres stratégies et à la méthode abstraite — latent, ne casse rien aujourd'hui (`trainer.py` n'appelle qu'avec `use_onehot_labels=`), mais rompt la promesse de généricité de l'interface le jour où un nouveau kwarg générique est introduit.
  4. **Ajouté le 2026-07-29 (Epic 9, domaine échecs), corrigé le 2026-07-30** : `Trainer.create_train_state()` (`trainer.py:139`) lisait `config["image_size"]` sans garde, alors que ce concept n'a pas de sens pour tous les domaines (échecs : plateau 8×8 de plans, pas une image redimensionnable ; Kepler détournait déjà `image_size` en `(longueur, 1)` pour une séquence 1D). **Résolu** : nouvelle clé `input_shape` (tuple complet hors batch, ex. `(224,224,1)`, `(3197,1)`, `(8,8,29)`) devenue l'unique source lue par `Trainer` pour construire son entrée factice — plus aucune branche `if task_type == "kepler"`, generique par construction pour n'importe quelle forme future. `image_size`/`grayscale` restent utilisés ailleurs (retraitement d'image réel dans `data_management.py`/`dataset_builder/`) — rôles désormais nettement séparés au lieu d'être mélangés. `image_size` retiré des 3 configs échecs (n'avait plus aucun usage réel). `input_shape` ajouté aux 8 configs (`FIGHTERJET_CLASSIFICATION/DETECTION`, `JAX_DETECTOR`, `CIFAR10`, `JAX_KEPLER`, `CHESS`, `CHESS_NO_HISTORY`, `CHESS_NAKAMURA_NO_HISTORY`), rendu requis dans `validate_config` avec une vérification de type. Validé par exécution réelle de `Trainer.create_train_state()` sur 4 familles de domaines (image classique, séquence 1D, échecs, détection) — tous réussissent avec la même logique, zéro branchement spécifique. Suite de régression complète (tests échecs + JAX_DETECTOR/CenterNet) au vert ; `tests/test_chess_task_strategy.py::test_trainer_change_is_the_single_authorized_deviation` retiré (sa prémisse, "trainer.py ne doit porter qu'un seul écart" de l'Epic 9, est explicitement dépassée par ce refactor voulu).
- **Duplication confirmée ailleurs** (moindre priorité architecturale, plus proche du nettoyage) : `generate_reports` (Detection vs CenterNetDetection, `task_strategies.py`), post-traitement single-image vs batch (`inference_utils.py`), `confusion_matrix_from_pkl` vs `confusion_matrix_from_state` (`reporting.py`, déjà noté dans un audit antérieur).
- **Code mort confirmé** (usages vérifiés par grep) : ~400 lignes de pertes grid/YOLO jamais atteintes (`loss_functions.py`, cohérent avec AD-19 — grid/YOLO reporté, jamais réactivé), classe `DetectionReporter` entière + `Reporter.confusion_matrix_from_state` (`reporting.py`, déjà noté), `create_eval_step`/`create_train_step` (`utils.py`, version pré-refactor), `list_available_models`/`get_model_info` (`model_library.py`).
- **Statut** : "on peut le laisser en backlog" (Aymeric, 2026-07-22, reconfirmé 2026-07-29 pour le point 4) — rien d'urgent, le noyau fonctionne et généralise déjà correctement. Repris quand une consolidation devient utile, probablement via Amelia pour l'implémentation. Inventaire complet dans `quickdev-2026-07-22-training-core-quality-audit.md`, pas à refaire depuis zéro à la reprise.

## Bug corrigé : artefacts JPEG en carrés sur `boxes_process_manual_tkinter.py::crop_bottom_zone` (2026-07-24) — 🟢 RÉSOLU

**Contexte** : Aymeric signale des carrés de couleur plus claire apparus en centaines dans `archive/e8ca95356acba932.jpg` après un appui prolongé sur `x` (crop répété, usage volontaire — `crop_height=4px`, plusieurs appuis nécessaires selon l'image). Repéré pendant le nettoyage de l'inventaire de fichiers, sans lien avec le refactor à 4 étapes du même fichier (`crop_bottom_zone` n'avait pas été touché par ces étapes).

- **Cause identifiée** : `crop_bottom_zone` sauvegardait l'image croppée sur disque (`cropped_image.save(image_path)`) puis appelait `self.load_image()`, qui **rechargeait ce même fichier depuis le disque** (`Image.open(image_path)`). À l'appui suivant sur `x` (la touche `<KeyRelease>` répète en continu si maintenue enfoncée, sous X11/Tkinter — aucune protection anti-répétition), le crop repartait donc de la version déjà recompressée, pas de la version propre en mémoire. Chaque appui composait sa perte JPEG sur celle du précédent.
- **Vérifié par simulation** (100 cycles crop+save, avant correctif) : écart mesurable (~1.25, énergie de bord de bloc 8×8 supérieure) par rapport à une compression unique — confirme le cumul.
- **Corrigé** : `crop_bottom_zone` ne rappelle plus `self.load_image()` — il réutilise directement `cropped_image` (jamais ré-encodée) pour rafraîchir `self.original_image`/`self.image`/le canvas, exactement le même code que `load_image()` sauf la lecture disque. Sauvegarde disque conservée à chaque appui (comportement inchangé, crop toujours immédiatement permanent) mais avec `quality=95, subsampling=0` en bonus (réduit la perte de la passe de compression elle-même, indépendamment du cumul).
- **Vérifié après correctif**, sur la vraie méthode (pas une simulation à côté) : 100 appels réels à `crop_bottom_zone()` sur une image synthétique en mémoire → écart de **0.0** par rapport à une compression unique de référence, dimensions finales correctes.
- **Non traité** : le fichier `archive/e8ca95356acba932.jpg` déjà dégradé n'est pas récupérable (perte déjà actée) ; seules les futures sessions de crop sont concernées par le correctif.

## Deferred from: code review of story 9-1-module-dechange-partage-builder-dataset-pgn (2026-07-27)

- `board.is_repetition(2)` (dans `encode_position`, `chess_target_encoding.py`) est appelé à chaque demi-coup de chaque partie traitée par `dataset_builder/chess_pgn_dataset_tools.py`. La documentation de `python-chess` avertit que cette fonction rejoue la partie entière à chaque appel (pas de table de transposition incrémentale) — coût potentiellement significatif à l'échelle d'une archive pgnmentor réelle (dizaines de milliers de parties). Spot-check du reviewer à ~130 demi-coups sans problème observé, mais non benchmarké à l'échelle réelle dans cette story — nécessite un test sur une vraie archive pgnmentor avant d'être tranché, hors périmètre de la Story 9.1 (exécution locale lourde à éviter sans confirmation explicite d'Aymeric). **Devient plus pressant si le dataset multi-joueurs ci-dessous (2026-07-27, section suivante) est lancé** — bien plus de parties que l'archive Carlsen seule.

## Deferred from: 1er entraînement réel CHESS + exploration `chess_game.py` (2026-07-27) — 🟢 Epic 9 validée, 🟡 backlog modèle v2 ouvert

**Contexte** : avant de clore l'Epic 9 (Story 9.4), premier entraînement réel local sur les 691 779 positions extraites de l'archive Carlsen (139 chunks, Story 9.1) — décidé par Aymeric pour prouver AC3 par exécution, pas seulement par `create_train_state()` (Story 9.3). En parallèle, exploration (hors périmètre PRD, Non-Goals) d'une intégration du modèle dans `chess/chess_game.py`.

- **✅ Entraînement réel réussi — preuve d'exécution pour Story 9.4/AC3** : `python3 main.py CHESS`, 15 epochs complets, aucun crash, aucune divergence. Progression quasi monotone : PolicyAccuracy validation 8.2% (epoch 1) → **24.43% (epoch 15, meilleur score)**, loss validation 6.38 → 3.98. Checkpoint final `best_model_chess.pkl` (382 017 paramètres, cohérent avec Story 9.2/9.3).
- **Hyperparamètres GPU ajustés en cours de route** (voir commentaire daté dans `dataset_configs.py::DATASET_CONFIGS["CHESS"]["gpu"]`) : `micro_batch_size` 64→256, `learning_rate` 2e-4→8e-4 (scaling linéaire), `decay_steps` 10000→36700 (recalculé pour couvrir les 15 epochs réelles). Diagnostic : à batch=64, écart train/val (~85ms vs ~15ms/step) pointait vers un coût dominé par le nombre de lancements de kernels (attention croisée+auto-attention, petits matmuls) plutôt que le calcul brut — confirmé partiellement à batch=256 (épochs ~835s→~335s, gain ~2.4x, pas le x4 pur attendu). **`accum_steps` volontairement laissé à 1** : n'aide pas ce type de goulot (traite les micro-batches séquentiellement, ne réduit pas le nombre de kernel-launches par step).
- **GPU sous-utilisé même à batch=256** (mesuré via `nvidia-smi` pendant le run : 6-7% d'utilisation GPU, VRAM à 4.4/6 Go — ce dernier chiffre est la préallocation par défaut de JAX, pas un signal de pression mémoire réelle). Confirme qu'il reste de la marge pour un batch encore plus gros, non exploitée cette session (rendements décroissants + risque `learning_rate` déjà identifiés, run laissé tel quel pour ne pas perdre la progression en cours).
- **Colab TPU jugé non pertinent pour ce run** (discussion avant lancement) : modèle minuscule (382K paramètres), les TPU ont besoin d'un gros batch pour amortir leur latence de dispatch host→device (pire que GPU sur ce point) — sans changer le batch, TPU risquait d'être pire, pas mieux. À reconsidérer si le dataset multi-joueurs ci-dessous est lancé avec un batch nettement plus gros (voir points suivants).
- **[Discussion][Decision] Le modèle actuel "se laisse trop facilement battre" (retour d'Aymeric après une partie complète jouée via `chess_game.py --vs-model`)** — attendu à ce stade (24% de PolicyAccuracy sur 4672 coups possibles, 1er entraînement de validation, pas d'objectif de qualité de jeu pour l'Epic 9, PRD Non-Goals). Mais si un modèle qui joue réellement bien devient un nouvel objectif (distinct d'Epic 9, à traiter comme une initiative à part) :
  1. **Dataset multi-joueurs curé, pas la totalité des 3.2 Go de PGN disponibles.** Calcul fait avec Aymeric : au rythme mesuré (~1870 positions/s à batch=256), le corpus complet (~403M positions estimées, 582x l'archive Carlsen) prendrait ~60h par epoch — non praticable tel quel. Recommandation : sous-ensemble de plusieurs joueurs (diversité de style, pas juste Carlsen), taille cible à définir (ordre de grandeur 200-500 Mo de PGN, 40-100x Carlsen) plutôt que le corpus entier.
  2. **Reformuler l'entraînement en nombre de steps fixé plutôt qu'en epochs sur un petit corpus** — avec un corpus large, un seul passage (voire une fraction) apporte déjà plus de diversité de gradient que plusieurs epochs sur un petit dataset ; le concept "epoch complet" tel qu'utilisé cette session ne tient plus à cette échelle.
  3. **Revoir l'architecture du modèle pour plus de capacité** — le point de départ d'Aymeric, non encore instruit techniquement (quel axe : profondeur, largeur, K du bottleneck Perceiver, têtes d'attention — à explorer avant tout changement de code).
  4. **Tester un batch encore plus gros AVANT de relancer un entraînement complet** — proposé par Aymeric comme 1er test rapide, non risqué (le sous-utilisation GPU mesurée ci-dessus le permet), à faire même avant de changer de dataset/modèle pour mesurer l'impact isolément.
  5. Prétraitement PGN→chunks à re-estimer avant de lancer à cette échelle (CPU-bound, `python-chess`) — non chiffré cette session pour un corpus multi-joueurs, seulement pour Carlsen seul (139 chunks, quelques minutes).
- **`chess/chess_game.py` — intégration modèle (Humain Blancs vs Modèle Noirs), exploration hors scope PRD Epic 9** : mode `--vs-model` ajouté (`chess/chess_model_inference.py`, nouveau module, réutilise `chess_target_encoding.py::index_to_move` pour la légalité — jamais de réimplémentation), testé avec succès par Aymeric (partie complète jouée). `chess/model_snapshot_chess.pkl` : copie figée du checkpoint (jamais le fichier `best_model_chess.pkl` racine, écrit en continu pendant un entraînement) — mise à jour avec le checkpoint final de cette session (24.43% val accuracy). Repris si le "modèle v2" ci-dessus voit le jour : re-snapshot après le prochain entraînement.
- **Incident mineur résolu, pattern déjà connu de ce projet** : 1er lancement d'Aymeric via le bouton "Run script" de VSCode, qui n'a jamais passé `--vs-model` (pas d'arguments passés par ce bouton) — pas un bug, juste le mode Humain vs Humain d'origine silencieusement actif. Même famille d'incident que le défaut VSCode déjà documenté pour `chess_pgn_dataset_tools.py` (Story 9.1/9.2) et `project_dev_environment_setup` (mémoire) — aucun correctif de code nécessaire, clarifié avec Aymeric.
- **Stockfish** : binaire non copié dans le repo (112 Mo, pollution git) — `chess_game.py` résout désormais un 3e niveau de repli vers l'emplacement d'origine sur cette machine (`/home/aobled/Desktop/Development/Chess/stockfish`) avant `shutil.which`. Sert uniquement la barre d'avantage (évaluation), aucun lien avec la légalité des coups (gérée nativement par `python-chess`, indépendamment de Stockfish) — point de confusion initial d'Aymeric, clarifié.
- **[Discussion][Defer] Encodage de l'historique (plans 19-28, `chess_target_encoding.py`) jugé grossier — piste "conv 3D" à instruire, pas pour ce soir.** Discussion sur les leviers d'amélioration du modèle (voir point 3 ci-dessus) : l'historique actuel n'encode que case source/case destination par demi-coup passé (2 plans/coup), jamais la pièce déplacée ni le plateau complet à cet instant — signal potentiellement faible comparé à un vrai stack façon AlphaZero (plateau complet par ply passé). Piste proposée par Aymeric : traiter l'historique comme une dimension temporelle explicite (conv 3D sur une pile de plateaux complets par ply, plutôt que des plans 2D aplatis dans le canal courant) — changerait la forme d'entrée du modèle (au-delà d'un simple hyperparamètre), donc plus structurant que le test K du bottleneck. Non instruit techniquement cette session (faisabilité, coût, impact sur `chess_target_encoding.py`/AD-18 à évaluer). Rattaché au chantier "modèle v2" ci-dessus, pas un sujet Epic 9.

### Mise à jour 2026-07-28 : test `num_bottleneck_tokens` K=8→32 — résultat négatif, coût confirmé négligeable

**Contexte** : 1er test du levier "capacité du bottleneck" proposé ci-dessus (point 3), suite à l'analyse des paramètres (voir Dev Notes de session : le Dense policy final concentre 78% des paramètres du modèle et ne dépend que de `token_dim`/D, pas de `num_bottleneck_tokens`/K — augmenter K est donc quasi gratuit en paramètres).

- **Câblage** : `num_bottleneck_tokens` ajouté à `DATASET_CONFIGS["CHESS"]` (`dataset_configs.py`), forwarding conditionnel dans `main.py` (`model_kwargs`, même discipline que `heatmap_prior`) et dans `chess/chess_model_inference.py::load_chess_model` (forwarding conditionnel de `num_bottleneck_tokens`/`token_dim`/`num_heads` depuis le `config` embarqué dans le checkpoint — nécessaire pour rester rétro-compatible avec les checkpoints déjà générés à K=8, dont le config ne porte pas ces clés).
- **Résultat mesuré (K=32, mêmes hyperparamètres batch=256/lr=8e-4 sinon, 15 epochs réelles)** : meilleur PolicyAccuracy validation **23.81%**, contre **24.43%** pour K=8 (même dataset Carlsen) — légèrement en dessous, dans la marge de bruit d'un seul run (les deux courbes sont restées proches tout du long, tantôt l'une devant tantôt l'autre). **Pas d'amélioration.**
- **Coût confirmé négligeable, comme prévu** : +1536 paramètres (383 553 vs 382 017), temps total quasi identique (~86 min vs ~84 min pour les 15 epochs).
- **Correction méthodologique en cours de session, à garder en tête pour la suite** : une comparaison initiale basée sur le débit `it/s` instantané de tqdm (K=8 ~11.4it/s vs K=32 ~7.5it/s, lu directement dans le flux du log) avait suggéré à tort un ralentissement de ~34% et une conclusion (TPU non pertinent) tirée de ce chiffre. Le vrai chiffre fiable est le champ `Time=` (mesuré par le code, pas par tqdm) : epoch 1 à 348.6s (K=8) vs 351.9s (K=32), soit +1% seulement. **`it/s` tqdm lu en cours de flux n'est pas une mesure fiable de débit réel sur ce projet — toujours comparer via `Time=` (ou une moyenne calculée sur l'epoch entière), jamais un extrait instantané.**
- **Conclusion/next step** : le levier "K du bottleneck" seul, isolé, ne débloque pas la qualité de jeu à ce stade. Renforce l'hypothèse que la contrainte actuelle est le volume/diversité de données (691K positions, un seul joueur) plutôt que la capacité du modèle — prochaine étape recommandée : le chantier dataset multi-joueurs (point 1 ci-dessus), pas d'autres retouches d'architecture isolées.
- **Statut checkpoints** : `chess/model_snapshot_chess.pkl` (utilisé par `chess_game.py --vs-model`) n'a pas été touché par ce run K=32 — reste sur le meilleur résultat (K=8, 24.43%), aucune action de restauration nécessaire.

## Bug corrigé : fenêtre de promotion vide/bloquante sur `chess/chess_game.py` (2026-07-28) — 🟢 RÉSOLU

**Contexte** : Aymeric signale qu'en déplaçant un pion sur la dernière rangée, la fenêtre de choix de promotion s'ouvre mais apparaît vide (aucun bouton visible), et le jeu reste bloqué sans pouvoir continuer.

- **Cause identifiée** (`ask_promotion_choice`, reproduit isolément hors de l'app complète pour diagnostiquer sans deviner) : les boutons étaient bien créés et correctement positionnés en interne (vérifié widget par widget), mais la fenêtre `Toplevel` n'avait ni position explicite ni mise au premier plan (`lift()`/`focus_force()`) — le positionnement par défaut de Tkinter calculait une coordonnée X légèrement négative (`-14`), et sur certains gestionnaires de fenêtres la fenêtre modale peut rester masquée derrière `root` faute d'être explicitement relevée. `grab_set()` intercepte alors tous les clics vers une fenêtre non visible : le jeu semble figé sans qu'aucun choix ne soit accessible.
- **Corrigé** : positionnement explicite centré sur la fenêtre principale (`dialog.update_idletasks()` + calcul de coordonnées + `dialog.geometry(...)`), suivi de `dialog.lift()` + `dialog.focus_force()` avant `grab_set()` (déplacé après la création des widgets, pas avant).
- **Vérifié** : reproduction isolée du dialogue avec clic simulé sur un bouton (`.invoke()`) — géométrie après correctif `460x88+70+308` (position positive, plus de coordonnée hors-écran), fermeture propre après clic, bon choix de pièce retourné.
- **Non testé en conditions réelles par Aymeric dans cette session** (correctif appliqué en fin de session) — à confirmer à la prochaine partie jouée.

## Comparaison K=8 vs K=32 par perte de centipawns Stockfish (2026-07-29) — impression subjective non confirmée, cause du "facile à battre" identifiée

**Contexte** : après avoir joué contre K=32 (`chess/model_snapshot_chess.pkl` remplacé manuellement par Aymeric pour tester, hors session Claude), impression subjective qu'il "s'en sortait mieux" que K=8 malgré une PolicyAccuracy légèrement inférieure (23.81% vs 24.43%, voir entrée K=32 ci-dessus). PolicyAccuracy (top-1 exact match contre le coup historique, `task_strategies.py`) ne mesure pas si un coup est bon, seulement s'il coïncide avec celui de la partie source — insuffisant pour trancher une question de qualité de jeu perçue.

- **Nouvel outil** : `chess/evaluate_model_vs_stockfish.py` (nouveau, exploration hors PRD Epic 9) — méthode standard de perte de centipawns (proche de l'"accuracy" Lichess/chess.com) : pour un échantillon de positions réelles (rejouées depuis `Carlsen.pgn` via `python-chess`, même archive que le dataset d'entraînement), compare l'éval Stockfish avant le coup (meilleur cas atteignable) à l'éval Stockfish après le coup du modèle, perte = différence, blunder = perte ≥100cp. Repli Stockfish à 3 niveaux dupliqué de `chess_game.py` (2e occurrence seulement — pas encore la 3e qui justifierait une centralisation, cf. mémoire `feedback_no_generic_library_folder`).
- **Test à 5 positions (fumée, profondeur 8)** : K=32 semblait meilleur (21.2cp vs 31.4cp) — **artefact de bruit sur un échantillon minuscule**, contredit par le test complet ci-dessous.
- **Test complet, 50 positions, profondeur 12** (résultat qui fait foi) :

  | | Perte moyenne | Blunders (≥100cp) |
  |---|---|---|
  | K=8 | **107.4cp** | 13/50 (26.0%) |
  | K=32 | 121.2cp | 12/50 (24.0%) |

  **L'impression subjective n'est pas confirmée** — si quoi que ce soit, K=8 est très légèrement meilleur (perte moyenne plus basse), cohérent avec son avantage déjà observé sur PolicyAccuracy. K=32 blunder un peu moins souvent mais plus sévèrement quand ça arrive (d'où sa perte moyenne plus haute malgré moins de blunders). Les deux restent statistiquement proches — pas de signal fort dans un sens ou l'autre, à l'inverse de ce qu'un seul run de 5 positions ou une partie jouée au ressenti peuvent suggérer.
- **Constat plus important que K=8 vs K=32** : les deux modèles blunder sur **~1 coup sur 4** (perte ≥100cp, l'équivalent de perdre une pièce mineure gratuitement) — explique directement le retour "facile à battre" d'Aymeric, indépendamment du choix de K. Confirme (preuve chiffrée, pas juste une hypothèse) que le goulot actuel est le volume/diversité de données (691K positions, un seul joueur), pas la capacité du bottleneck — appuie encore le chantier dataset multi-joueurs comme prochaine étape prioritaire, pas d'autres réglages de K/D isolés.
- **Réutilisable tel quel** pour évaluer un futur modèle v2 (dataset multi-joueurs) contre K=8 — même script, changer `DEFAULT_CHECKPOINTS`.

## Deferred from: Story 9.4 (validation non-régression JAX_DETECTOR) — 2 tests de régression cassés, défaut préexistant non lié à l'Epic 9 (2026-07-29) — 🟡 EN BACKLOG

**Contexte** : Task 4 de la Story 9.4 (ré-exécution de la suite de tests de régression `JAX_DETECTOR`/CenterNet) — 7/9 scripts passent, 2 échouent.

- **Échec identique sur les deux** : `tests/test_centernet_detection_dataset.py` et `tests/test_jax_detector_dataset_tools.py` lèvent `ImportError: cannot import name 'process_detection_dataset_v2' from 'jax_detector_dataset_tools'`. La fonction réelle du module s'appelle `process_detector_dataset` — les deux tests référencent un nom qui n'existe pas.
- **Confirmé non lié à l'Epic 9** : `git diff 1a85f25..049da4c -- dataset_builder/jax_detector_dataset_tools.py` (avant/après toute l'epic) est **vide** — ce fichier n'a pas été touché par les Stories 9.1-9.4. Défaut préexistant (rename non répercuté dans les tests, à une date antérieure non déterminée cette session), pas une régression causée par le domaine échecs.
- **Discussion Aymeric (2026-07-29)** : révèle un problème plus large — des tests peuvent devenir obsolètes silencieusement (référence à une fonction renommée/supprimée) sans que personne ne le remarque avant de les exécuter pour de vrai. Deux pistes explorées et écartées pour un traitement immédiat (toutes deux jugées disproportionnées pour ce soir) :
  1. Relancer un entraînement complet sur un autre domaine (FIGHTERJET_CLASSIFICATION/DETECTION) comme test de validation plus large — écarté : dataset FIGHTERJET encore agrandi depuis, coût élevé pour un gain marginal vu les preuves déjà réunies (Story 2.4 avait déjà tranché ce point : rechargement + comparaison bit-exacte suffit, entraînement complet réservé à Colab/TPU).
  2. Utiliser CIFAR10 comme smoke test générique du pipeline — utile pour valider le tronc commun (`Trainer`/`TaskStrategy`/`main.py`), mais n'aurait pas révélé ce bug précis (spécifique au dataset builder JAX_DETECTOR, hors du chemin CIFAR10).
- **Piste retenue pour plus tard, non instruite techniquement cette session** : un sweep léger qui vérifie que tous les fichiers de test s'importent sans erreur (sans les exécuter en entier) — aurait détecté ce renommage le jour même où il s'est produit plutôt que des mois plus tard.
- **Statut** : ne bloque pas AC2 de la Story 9.4 (aucun lien avec l'Epic 9, prouvé par le diff vide) — la story a été close sans corriger ces 2 tests. **Corrigé le 2026-07-30** : simple renommage de `process_detection_dataset_v2` → `process_detector_dataset` (le vrai nom de la fonction) dans les imports et appels des 2 fichiers de test — pur problème de nom, tous les kwargs utilisés à chaque site d'appel correspondaient déjà exactement à la vraie signature. Les 2 tests passent désormais. Le sweep d'imports plus large (détecter ce genre de dérive automatiquement) reste non instruit, toujours à rattacher à l'audit qualité noyau d'entraînement de Winston si repris.

## Audit qualité noyau d'entraînement — items 3 et 4 clos (2026-07-30)

Suite de l'audit Winston du 2026-07-22 (ci-dessus) :

- **Item 4 (`image_size` obligatoire)** : voir la mise à jour dédiée plus haut dans cette section — remplacé par `input_shape`, générique sur tous les domaines.
- **Item 3 (`KeplerStrategy.compute_loss` sans `**kwargs`)** : corrigé — signature alignée sur la méthode abstraite (`task_strategies.py:17`) et sur `ClassificationStrategy` (même pattern `use_onehot_labels=False, **kwargs`). Vérifié : l'appel existant (`use_onehot_labels=` seul) produit exactement la même loss qu'avant, et un kwarg supplémentaire arbitraire est désormais accepté sans erreur.
- **Item 1, corrigé le 2026-07-30** : `_get_export_path`/`get_training_state_path` étaient déclarées `@abstractmethod` dans `TaskStrategy` alors que les 5 sous-classes (`ClassificationStrategy`, `DetectionStrategy`, `CenterNetDetectionStrategy`, `KeplerStrategy`, `ChessPolicyValueStrategy`) les implémentaient toutes de façon strictement identique (vérifié caractère pour caractère avant de toucher au code, pas supposé). Devenues des défauts concrets dans la classe de base (toujours surchargeables si un futur domaine en a vraiment besoin), les 5 implémentations dupliquées supprimées. Vérifié par exécution réelle sur 3 stratégies différentes (`ClassificationStrategy`/`KeplerStrategy`/`ChessPolicyValueStrategy`) que les chemins générés restent corrects et spécifiques à chaque `dataset_name`. Suite de régression complète (13 fichiers de test) au vert.
- **Item 2 restant** (duplication de l'augmentation de données entre `DetectionDataset`/`CenterNetDetectionDataset`) : laissé en backlog - un vrai refactor de logique touchant le pipeline d'entraînement de deux domaines en production (`FIGHTERJET_DETECTION`, `JAX_DETECTOR`), nécessiterait une vérification pixel par pixel entre ancien/nouveau chemin, pas une retouche à embarquer avant un training long. À reprendre dans une session dédiée.

## Test d'ablation : l'historique (10 plans) sert-il à quelque chose ? (2026-07-29) — 🟢 résultat obtenu, signal réel mais modeste

**Contexte** : suite à la doc `docs/ChessCnnAttentionPolicyValue.md`, remarque d'Aymeric sur son propre choix initial (5 derniers demi-coups, case source/destination seulement) — mon avis avait été "à priori ça ne gêne pas, mais ça ne sert à rien non plus". Testé plutôt que supposé.

- **Changement de code (rétrocompatible, comportement par défaut inchangé)** :
  - `chess_target_encoding.py::encode_position` : nouveau paramètre `include_history: bool = True` — si `False`, retourne `(8, 8, NUM_POSITION_PLANES=19)` au lieu de `(8, 8, NUM_PLANES=29)` (les plans d'historique sont omis, pas mis à zéro).
  - `dataset_builder/chess_pgn_dataset_tools.py::_iter_game_examples`/`build_chess_dataset` : nouveau paramètre `include_history`, forwardé.
  - `data_management.py::ChessPolicyValueDataset` : **bug trouvé et corrigé au passage** — `create_tf_dataset` codait en dur `NUM_PLANES` (29) dans son `output_signature` au lieu de le dériver des données réelles, cassait silencieusement pour tout dataset à un nombre de canaux différent. Nouveau paramètre `num_planes` (défaut `NUM_PLANES`, rétrocompatible), forwardé depuis `config["num_channels"]` dans `get_datasets()`.
  - `dataset_configs.py` : nouvelle entrée `CHESS_NO_HISTORY` (copie de `CHESS`, `num_channels=19`, `output_prefix` distinct, `save_dir` distinct — mêmes hyperparamètres sinon, pour une comparaison isolée sur ce seul facteur).
- **Dataset régénéré** : `chunks/chess_no_history/` (139 chunks, 691 779 positions — même archive Carlsen, mêmes parties, donc comparaison directe valide). **84 Mo → 61 Mo (-27%)** sur disque.
- **Incident de process (transparence)** : le premier essai de "test de fumée" (censé être limité à quelques parties) a en réalité traité l'archive Carlsen complète sans confirmation explicite pour cette exécution précise (`build_chess_dataset` n'a pas de limite de nombre de parties) — signalé à Aymeric immédiatement, le job a été laissé terminer (déjà ~80% fait, aucun risque à le laisser finir) puis regénéré proprement avec le bon `chunk_size` vers l'emplacement définitif.
- **Résultat entraînement réel (15 epochs, mêmes hyperparamètres que la référence K=8/batch=256/lr=8e-4)** :

  | | Avec historique (29 canaux, référence) | Sans historique (19 canaux) |
  |---|---|---|
  | Val PolicyAccuracy finale | 24.43% | 23.68% |
  | Temps total (15 epochs) | ~84 min | ~84 min |

- **Pas de gain de vitesse malgré -27% sur disque** : la mesure préalable ("chargement pur = 52.5% du temps d'epoch, sans aucun calcul modèle") était correcte, mais ce temps semble dominé par un coût **fixe par exemple** (overhead Python/`tf.data` : ouverture fichier, itération, `yield`, batching) plutôt que par le volume de données transporté — réduire la taille des tableaux ne réduit pas le nombre d'opérations. Même famille de leçon que le test K=32 : le goulot de cette pipeline est le nombre d'étapes, pas leur volume.
- **Écart d'accuracy (0.75 point) jugé probablement réel, pas du bruit pur** — contrairement à K=32 (résultat oscillant au-dessus/en dessous de la référence epoch par epoch), "sans historique" est resté systématiquement légèrement en dessous à quasiment chaque epoch du début à la fin, un écart légèrement supérieur au niveau de bruit déjà mesuré avec K=32 (0.62 point, jugé non concluant). Reste un seul run — pas une preuve statistique forte, mais un signal cohérent plutôt qu'une fluctuation aléatoire.
- **Conclusion** : l'historique aide un peu, mais modestement — ne justifie pas de le retirer (pas de gain de vitesse pour compenser), mais confirme aussi que ce n'est pas le levier prioritaire pour améliorer la qualité de jeu (l'écart est petit comparé au problème de fond déjà identifié : volume/diversité de données). Prochaine étape naturelle si le sujet historique est repris : la piste "conv 3D sur un vrai stack de plateaux complets" déjà notée dans `docs/ChessCnnAttentionPolicyValue.md`/l'entrée K=32 ci-dessus, plutôt qu'un encodage source/destination seul.
- **Checkpoint** : `best_model_chess_no_history.pkl` (racine, pas de collision avec `best_model_chess.pkl` — nommage dérivé automatiquement de la clé de config). Non copié dans `chess/model_snapshot_chess.pkl` (reste sur le meilleur résultat global, 24.43%, avec historique).

## Tournoi model1 (Carlsen) vs model2 (Nakamura) + insight architecture : absence de recherche/MCTS (2026-07-29)

**Contexte** : décision de retenir la variante sans historique comme défaut de `chess_game.py` (23.68%, signal modeste mais architecture plus simple - `docs/ChessCnnAttentionPolicyValue.md` mis à jour en conséquence, `chess/model1_snapshot_chess.pkl` = ancien `model_snapshot_chess.pkl`). Question posée par Aymeric : le contenu du dataset d'entraînement (joueur source, volume) influence-t-il réellement la qualité de jeu, ou est-on proche du hasard ? Protocole : entraîner un second modèle ("model2") sur un joueur différent (Nakamura.pgn, remplace Carlsen.pgn dans `/home/aobled/Downloads/tmp_test_PGN/` — 943 250 positions/9756 parties/189 chunks, ~35% de plus que Carlsen, volontairement confondu avec le style du joueur, pas isolé), puis les faire s'affronter sur 100 parties.

- **Nouveaux outils** :
  - `chess/tournament_model_vs_model.py` (nouveau) : tournoi headless (pas de Tkinter, beaucoup plus rapide), réutilise `select_model_move` des deux côtés.
  - `dataset_configs.py::CHESS_NAKAMURA_NO_HISTORY` (nouvelle config, copie de `CHESS_NO_HISTORY`, `decay_steps` recalculé pour le volume Nakamura).
- **Bug méthodologique trouvé et corrigé avant le vrai run** : première version du script utilisait une temperature (tirage aléatoire pondéré sur les coups) pour éviter que deux modèles déterministes ne rejouent exactement la même partie à chaque rencontre à couleurs égales (vérifié empiriquement : 3 répétitions d'une même paire de couleurs → parties strictement identiques, même nombre de demi-coups). **Objection d'Aymeric, juste** : la température pollue justement ce qu'on veut mesurer (le jugement natif/déterministe de chaque modèle). Corrigé avec la méthode standard des vrais tournois de moteurs d'échecs (TCEC/CCRL) : varier la **position de départ** (quelques coups légaux tirés au hasard, aucun modèle impliqué), jamais le jugement des modèles eux-mêmes. Design pairwise : chaque ouverture tirée est rejouée 2 fois (une par couleur), pour neutraliser un biais éventuel de l'ouverture elle-même.
- **Insight architecture, plus important que le tournoi lui-même** : sur le test mécanique (6 parties, modèles partiellement entraînés), toutes les parties finissent en nulle par répétition quintuple ou plafond de sécurité — jamais une victoire nette. Analyse : le plan de répétition (`board.is_repetition(2)`, plan 17, toujours présent) donne au modèle l'info "cette position s'est déjà produite", **mais rien dans l'architecture ne permet de s'en servir pour éviter de la recréer** - decision en une seule passe avant (forward pass), sans recherche/anticipation (pas de MCTS, pas de simulation des conséquences d'un coup avant de le jouer). Avec une politique déterministe (argmax), une position déjà rencontrée reproduit donc mécaniquement le même choix, et la boucle se referme. **Ce n'est pas un problème de capacité (K/D n'y changeraient rien) - c'est une brique entière absente** : AlphaZero (dont s'inspire cette architecture) ne compte jamais sur le réseau de policy seul pour éviter les boucles, c'est MCTS qui explore plusieurs coups à l'avance et écarte les branches stériles. Aggravé par l'imitation pure depuis des parties humaines fortes (Carlsen/Nakamura vs d'autres joueurs forts) : ce genre de boucle contre un adversaire faible/déterministe n'apparaît quasiment jamais dans les données d'entraînement - le modèle n'a jamais "vu" ce cas de figure.
- **Conclusion/next step (v2, architecture)** : ajouter une forme de recherche (MCTS complet, ou même un simple lookahead à 1-2 coups comparant les valeurs des continuations candidates via la tête value) serait le levier le plus structurant pour la qualité de jeu réelle - bien plus que K/D/historique/volume de données, qui n'agissent que sur la qualité du jugement instantané, jamais sur la capacité à planifier. Alternative complémentaire : fine-tuning par self-play avec pénalité explicite sur les nulles/répétitions (RL, pas imitation pure) - plus proche de la méthode AlphaZero originale, mais un chantier bien plus lourd.
- **Décision d'Aymeric (2026-07-29, fin de session)** : chantier échecs mis en pause ici - "les 1ers résultats étaient encourageants mais le gap est encore trop important" (jeu encore faible/imprévisible sans recherche, cf. insight ci-dessus). Retour aux activités BMad du projet principal à la reprise. Training model2 laissé tourner sans supervision, tournoi lancé à la suite - résultat ci-dessous.

### Résultat final (2026-07-30, model2 terminé pendant la nuit)

- **model2 (Nakamura) : 23.66% val PolicyAccuracy** (15 epochs, mêmes hyperparamètres que model1) — quasiment identique à model1 (23.68%, écart de 0.02 point, pur bruit statistique, encore plus proche que l'écart K=8/K=32 ou avec/sans historique déjà mesurés ce soir).
- **Tournoi 100 parties (50 ouvertures aléatoires x 2 couleurs, seed=2026, 1136.8s)** : `_bmad-output/implementation-artifacts/baseline/tournament_model1_vs_model2.json`

  | | Victoires | Dont en Blancs | Dont en Noirs |
  |---|---|---|---|
  | model1 | 0 | 0 | 0 |
  | model2 | 1 | 1 | 0 |
  | Nulles | 99 | - | - |

  **Aucun signal exploitable** — exactement ce qu'annonçait la remarque d'Aymeric en cours de session ("mon test est voué à échouer") : avec deux modèles de qualité quasi identique, la quasi-totalité des parties finit en nulle (répétition quintuple dans l'immense majorité des cas, cf. insight architecture ci-dessus), et 1 victoire sur 100 n'a aucune signification statistique. La seule partie décisive (paire 34, ouverture `f2f4 f7f5 e2e4 h7h6`, model2 Blancs) s'est terminée par un **vrai échec et mat** en 79 demi-coups, pas un artefact - confirme au passage que le mécanisme de fin de partie du script fonctionne correctement de bout en bout.
- **Conclusion sur la question initiale d'Aymeric** ("le dataset a-t-il une influence mesurable ?") : sur ce protocole précis (2 joueurs différents, volumes différents, mais même variante d'architecture sans recherche), **aucune influence détectable** entre Carlsen (691K positions) et Nakamura (943K positions, +35%) — les deux convergent vers un niveau de jeu quasi identique et tout aussi incapable d'éviter les boucles stériles. Cohérent avec l'insight architecture : sans recherche/MCTS, la qualité du jugement instantané (elle-même déjà proche entre les deux, à en juger par PolicyAccuracy) ne suffit de toute façon pas à transformer ce niveau de jeu en victoires exploitables contre un adversaire de force comparable - le facteur limitant n'est pas le dataset, c'est l'absence de la brique de recherche.

## Deferred from: spec-chess-npz-boundary-cleanup, retrait de chess_target_encoding.py (2026-08-01)

**Contexte** : `chess_ai` (split du 2026-07-30) possède désormais la génération réelle des `.npz` échecs et le codec associé — `chess_target_encoding.py` et `dataset_builder/chess_pgn_dataset_tools.py` ont été supprimés de `jax_supervised_training`, qui se réduit au rôle lecture-npz + training. Les 7 constantes qu'ils exportaient (`NUM_MOVES=4672`, `NUM_PLANES=29`, `NUM_POSITION_PLANES=19`, `BOARD_SIZE=8`, `POSITION_KEY="position"`, `POLICY_KEY="policy"`, `VALUE_KEY="value"`) sont devenues des littéraux directement aux points d'usage (`dataset_configs.py`, `task_strategies.py`, `loss_functions.py`, `data_management.py`, `model_library.py`).

- **[Decision] `POSITION_KEY`/`POLICY_KEY`/`VALUE_KEY` codées en dur (littéraux `"position"`/`"policy"`/`"value"`) dans 4 fichiers plutôt que centralisées dans un nouveau module de constantes.** Choix délibéré (acté avec Aymeric en amont du spec) : garder un module réduit aux seules constantes aurait reproduit, à plus petite échelle, exactement le risque de duplication/dérive silencieuse que ce nettoyage cherche à éliminer (même argument que CAP-2 du spec). Vérifié qu'aucune autre entrée de `dataset_configs.py` (CIFAR10, JAX_DETECTOR, etc.) n'importe jamais ses constantes de forme (`num_classes`/`input_shape`) — l'entrée CHESS était l'exception à corriger, pas un précédent à prolonger. À revisiter uniquement si une vraie friction pratique apparaît (typo, duplication gênante constatée) — pas par principe, pas en anticipation.
- **[Gap connu] Plus aucun test de non-régression sur `ChessPolicyValueStrategy`/`ChessPolicyValueDataset` après ce nettoyage.** Le seul test qui les exerçait (`tests/test_chess_task_strategy.py`) dépendait de `build_chess_dataset` (`dataset_builder/chess_pgn_dataset_tools.py`, supprimé) pour générer ses fixtures — supprimé avec lui plutôt qu'adapté (un générateur synthétique inline de remplacement a été envisagé puis écarté par Aymeric : la surface testée sera vraisemblablement remplacée par la future architecture à 2 modèles côté `chess_ai`, investir dans son maintien maintenant serait prématuré). `tests/test_chess_model.py` (modèle seul, `ChessCnnAttentionPolicyValue`) reste vert et couvre toujours shapes/dtypes/bornes value/différentiabilité — mais plus rien ne couvre la strategy/le chargement de dataset côté ce repo. À couvrir par une future epic pilotée par `chess_ai` (probablement à 2 modèles), pas par un patch isolé ici.

## Deferred from: discussion CHESS_LEGAL_MOVES avec Winston (bmad-agent-architect) puis quick-dev (2026-08-02)

- source_spec: none
  summary: Ajouter la tâche "chess_legal_moves" (nouveau modèle sans tête value, loss BCE sigmoid, métrique F1, nouveau loader/stratégie, nouvelle config `CHESS_LEGAL_MOVES`) consommant le dataset déjà généré dans `/home/aobled/Documents/data/chunks/chess_legal_moves/` (`position (8,8,29)` + `legal_mask (4672,)`).
  evidence: Intent initial groupait ce nouveau livrable avec le nettoyage de `dataset_configs.py` (suppression de `CHESS`/`CHESS_NAKAMURA_NO_HISTORY`) — les deux sont des livrables indépendants (l'un n'a aucune dépendance fonctionnelle envers l'autre), Aymeric a choisi de scinder [S] pour traiter le nettoyage d'abord.

- source_spec: `_bmad-output/implementation-artifacts/spec-cleanup-legacy-chess-configs.md`
  summary: Orphelins non nettoyés à la racine du repo (`best_model_chess.pkl`, `best_model_training_state_chess.pkl`, `chess.png`) — sorties d'un entraînement réel sur l'ancienne config `CHESS`, maintenant supprimée, et non couverts par `.gitignore` (qui n'ignore que `checkpoints_*/`, pas les `.pkl`/`.png` à la racine).
  evidence: Repéré par la revue adversariale (Blind Hunter) de ce nettoyage — pas causé par la suppression de la config elle-même (les fichiers existaient déjà, non trackés, avant ce changement), mais la suppression de `CHESS` les rend définitivement orphelins. Risque réel mais mineur : un `git add -A` non attentif les commettrait comme déchet référençant une config qui n'existe plus. Pas traité ici pour rester dans le périmètre strict de ce nettoyage (suppression des entrées de config), pas une passe de hygiène du repo.

- source_spec: `_bmad-output/implementation-artifacts/spec-chess-legal-moves.md`
  summary: `compute_chess_legal_moves_loss` (BCE sigmoid, `CHESS_LEGAL_MOVES`) n'a aucune pondération de classe positive (`pos_weight`) malgré un déséquilibre sévère (~20-40 coups légaux sur 4672, ~0.5-0.9%) — le signal de gradient vers les vrais positifs risque d'être noyé par la masse de négatifs.
  evidence: Repéré par la revue adversariale (Blind Hunter) — pas un bug mathématique, mais un vrai risque que le premier run d'entraînement affiche une loss basse/plate trompeuse sans que le modèle apprenne réellement à détecter les coups légaux. Rien ne signale actuellement que `pos_weight` a été envisagé puis délibérément omis. À vérifier avant d'interpréter les résultats d'un premier run réel (non lancé dans cette spec, test de plomberie uniquement).
- source_spec: `_bmad-output/implementation-artifacts/spec-chess-legal-moves.md`
  summary: `Trainer.train_epoch`/`evaluate` moyennent la métrique retournée par micro-batch (`total_acc / total_microbatches`) — valide pour l'accuracy, mais biaisé pour une métrique non-linéaire comme F1 (moyenne-de-moyennes ≠ F1 réel sur l'ensemble du split).
  evidence: Repéré par la revue adversariale (Blind Hunter) — limitation pré-existante de `Trainer`, non introduite par cette spec (l'accuracy des autres stratégies ne souffre pas de ce biais), mais `ChessLegalMovesStrategy` (`LegalMoveF1`) est la première stratégie du repo où l'écart devient numériquement significatif. Hors périmètre de cette spec (toucher `Trainer` affecterait toutes les stratégies existantes) - à traiter si `LegalMoveF1` doit un jour servir de signal de qualité fiable (pas le cas ici, test de plomberie).
- source_spec: `_bmad-output/implementation-artifacts/spec-chess-legal-moves.md`
  summary: `ChessLegalMovesDataset`/`ChessPolicyValueDataset` (et le code appelant dans `main.py`/`Trainer`) ne gèrent pas les cas `val_split` produisant zéro chunk val, ou un split avec moins d'exemples que `micro_batch_size` (`drop_remainder=True` → zéro batch) — `Trainer.evaluate(None)` ou `next(iter(...))` planteraient avec une erreur peu claire.
  evidence: Repéré par la revue adversariale (Edge Case Hunter) — pattern hérité à l'identique de `ChessPolicyValueDataset` (dupliqué délibérément, pas une régression introduite ici) et non déclenché par la config réelle actuelle (149 chunks, val_split=0.1 → 14 chunks val, largement au-dessus de `micro_batch_size`). Partagé par les deux loaders échecs - à corriger conjointement si jamais rencontré en pratique, pas par un patch isolé sur un seul des deux.
