---
topic: Modèle jax_supervised_training (tronc auto-attention + tête scoring candidats légaux) consommant le dataset spike chess_token_candidate_spike, config dataset_configs.py, loss cross-entropy masquée
updated: 2026-08-13T11:35
---

- (direction) Handoff depuis chess_ai/_bmad-output/specs/spec-chess-token-candidate-poc (SPEC.md + companion token-candidate-dataset-schema.md) : chess_ai a produit et validé le dataset spike (412 574 positions, run 3000 parties seed=42, 2026-08-13, /home/aobled/Documents/data/chunks/chess_token_candidate_spike/chess_token_candidate_spike.npz), et exclut explicitement la définition/l'entraînement du modèle de son propre périmètre (contract-chess-ai-training-interface.md §4). Cette épic jax_supervised_training reprend tout ce que chess_ai a volontairement laissé ouvert : modèle, config, loss.
- (constraint) chess_ai ne définit ni n'entraîne jamais de modèle - cette épic possède entièrement le tronc, le bottleneck, la tête de scoring, la config dataset_configs.py et la loss ; chess_ai ne fournit que le dataset figé (token_position, global_flags, candidate_moves, candidate_mask, candidate_label).
- (capability) CAP-1 : tronc auto-attention pure sur les 64 tokens-case (remplace le tronc CNN existant), bottleneck existant (cross-attention+auto-attention) conservé structurellement, tête de scoring sur les candidats légaux fournis par le dataset (remplace Dense(4672)) - succès = le modèle s'entraîne et converge sans erreur sur le dataset spike.
- (decision) Loss = cross-entropy sur les candidats masqués uniquement : masquage additif -inf sur les slots candidate_mask=0 avant softmax, softmax restreint aux slots candidate_mask=1, label=candidate_label - conséquence directe du format de données (scorer les candidats plutôt que classifier sur 4672), pas un choix ouvert. Formalisé ici en capability (CAP-2), pas laissé en open question.
- (capability) CAP-2 : loss cross-entropy sur candidats masqués (voir decision ci-dessus) - succès = la loss se calcule correctement sur un batch réel (pas de NaN/Inf), comportement vérifié à la main sur un exemple synthétique (candidate_mask connu, label connu).
- (capability) CAP-3 : nouvelle entrée dataset_configs.py consommant le schéma companion adopté (token_position (N,64) int32, global_flags (N,6) float32, candidate_moves (N,50) int32 sentinel -1, candidate_mask (N,50) int8, candidate_label (N,) int32) - succès = la config se charge et un premier batch passe dans le Trainer sans erreur.
- (capability) CAP-4 : comparer la PolicyAccuracy top-1 du nouveau modèle au checkpoint actuel best_model_chess_search_teacher.pkl (28,00%), à parité de volume de dataset et d'epochs - succès = chiffre mesuré et documenté.
- (capability) CAP-5 : mesurer la réduction réelle du nombre de paramètres vs la baseline actuelle, part imputable à l'ancienne tête Dense(4672) explicitement isolée - succès = compte mesuré et documenté. Mesure fraîche contre le checkpoint RÉEL (pas un chiffre recopié d'une doc, voir note discrepancy ci-dessous).
- (capability) CAP-6 : surveiller la convergence train/val de l'auto-attention pure, opérationnaliser la condition d'arrêt anticipé (métrique/seuil/epoch exacts, à fixer dans cette épic) - si décrochage confirmé (écart croissant façon move_token dès epoch 9, ou accuracy plafonnant significativement sous 28%), basculer sur un tronc conv-stem léger devant l'attention - succès = courbes documentées, décision actée explicitement si déclenchée, jamais ignorée silencieusement.
- (note) Incohérence identifiée par Winston dans le SPEC.md chess_ai source : CAP-4 (source) compare l'accuracy au checkpoint RÉEL best_model_chess_search_teacher.pkl (num_bottleneck_tokens=16, token_dim=196, dataset_configs.py:765-766), alors que CAP-5 (source) donne un baseline de paramètres (382 017 dont 299 008 dans Dense(4672)) qui correspond en fait à l'ANCIENNE config par défaut K=8/token_dim=64 (model_library.py:1373), pas au checkpoint réel 28,00%. Les Constraints du SPEC.md source disent aussi garder 'le bottleneck K=8 existant (en pause) inchangé' alors que le checkpoint réel à 28,00% utilise K=16. Aymeric a explicitement choisi de reporter la décision K/token_dim à cette épic plutôt que de la trancher en amont (2026-08-13).
- (question) Quel K (num_bottleneck_tokens) / token_dim le tronc auto-attention doit-il utiliser ? Option A : K=16/token_dim=196 (aligné sur le checkpoint réel 28,00% - seul le tronc CNN->attention varie dans la comparaison CAP-4, CAP-5 recalculé contre les ~1 482 305 params réels). Option B : K=8/token_dim=64 (texte littéral du SPEC.md chess_ai source - mais confond alors 2 variables dans CAP-4, et le baseline CAP-5 382k/299k colle dans ce cas).
- (question) Dimension D de l'embedding token (nn.Embed(13, D)) et de l'embedding positionnel (nn.Embed(64, D)) - laissée ouverte par le companion schema chess_ai.
- (question) Dimension de l'embedding de coup candidat (nn.Embed(NUM_MOVES, D_move) ou équivalent) utilisé pour scorer chaque candidat contre la représentation de position - laissée ouverte par le companion schema chess_ai.
- (question) Nom exact de la nouvelle entrée dataset_configs.py (ex. CHESS_TOKEN_CANDIDATE) - laissé ouvert par le SPEC.md chess_ai source (Open Questions).
- (question) Détail opérationnel de la condition d'arrêt anticipé de CAP-6 (métrique exacte, seuil, epoch de décision) - cadrée en principe par le SPEC.md chess_ai source, à opérationnaliser ici.
- (question) Le format d'export du checkpoint (scores sur candidats variables par position, vs l'actuel vecteur policy fixe (B,4672)) est-il consommable tel quel par l'outillage self-play existant de chess_ai (tournament_model_vs_model.py, contract-chess-ai-training-interface.md #2.3), pour permettre la mesure de référence self-play (CAP-7 du SPEC.md chess_ai source, hors exécution de cette épic) ? Pas tranché - à vérifier avant handoff.
- (constraint) Dataset consommé tel quel (companion schema chess_ai, statut SPIKE/PROVISOIRE) : fichier unique .npz (pas de chunks), token_position (N,64) relatif au joueur au trait, candidate_moves (N,50) jamais réordonné (ordre board.legal_moves naturel), padding sentinel -1, MAX_CANDIDATES=50 (positions au-delà filtrées côté chess_ai, pas tronquées) - le masquage/loss côté modèle doit respecter cet ordre et ce padding tels quels, sans réordonnancement supplémentaire.
- (constraint) Vocabulaire de coup = move_to_index/index_to_move existant (NUM_MOVES=4672, AD-18 hérité) - aucun nouveau schéma d'encodage de coup, aucune réimplémentation indépendante.
- (constraint) Comparaisons CAP-4/CAP-5 à parité de volume de dataset et d'epochs avec le run baseline 28,00% (best_model_chess_search_teacher.pkl) - pas de changement de volume qui invaliderait la comparaison.
- (constraint) Dataset spike hors contrat stable contract-chess-ai-training-interface.md §2.1 tant que non concluant (hérité chess_ai) - ne pas committer la nouvelle entrée dataset_configs.py comme config stable au même titre que CHESS_SEARCH_TEACHER avant verdict CAP-4/5/6.
- (assumption) Risque data-hunger de l'attention pure sans biais de localité (ViT vs CNN à petite échelle) accepté avec garde-fou explicite (CAP-6, condition d'arrêt) plutôt qu'écarté par précaution - hérité de l'assumption chess_ai source.
- (assumption) Embedding positionnel appris (64,D) libre retenu comme option la plus simple à tester en premier ; biais géométrique explicite = repli futur si le résultat déçoit, non testé ici - hérité de l'assumption chess_ai source.
- (assumption) Le dataset .npz déjà généré et vérifié exhaustivement côté chess_ai (412 574 positions, 2026-08-13) est la source de vérité pour cette épic - pas de nouvelle génération prévue, sauf si le volume s'avère être un facteur limitant en cours de run (comme observé pour le spike move_token).
- (note) Non-goals hérités du SPEC.md chess_ai source, toujours valides côté jax_supervised_training : modification/régénération du dataset (propriété chess_ai) ; biais géométrique explicite dans l'embedding positionnel (réserve future) ; entraînement génétique/self-play avec sanction répétition (temps 2, hors scope) ; intégration au contrat stable §2.1 ; refonte du bottleneck K=8/K=16 au-delà de la valeur retenue pour la comparaison CAP-4/5 (pas de recherche d'hyperparamètre bottleneck dans ce spike, une fois la question K/token_dim tranchée).
- (note) Success signal hérité : le spike (temps 1) est concluant sur ses propres termes s'il satisfait CAP-4 (accuracy comparable ou meilleure que 28,00%) et CAP-5 (réduction mesurée des paramètres) sans déclencher la condition d'arrêt anticipé de CAP-6. Le taux de répétition self-play (référence CAP-7 source, hors exécution de cette épic) n'est explicitement pas un critère de succès/échec.
- (note) Companions adoptés retenus : token-candidate-dataset-schema.md (chess_ai, contrat de données - downstream doit le lire pour le loader) et chess-cnn-attention-policy-value.mmd (planning-artifacts, diagramme de la baseline CNN actuelle à comparer/remplacer). SPEC.md chess_ai source listé en sources: (why/capabilities-boundary/constraints déjà absorbés dans ce nouveau SPEC, downstream n'a plus besoin de le relire).
- (event) Self-validate Pass 1 (cohérence) : OK - 6 capacités intent+success, 5 constraints toutes contraignantes (dont l'incohérence K/token_dim explicitement portée en constraint+question plutôt que masquée), 5 non-goals, success signal testable avec la mesure self-play explicitement hors critère, IDs CAP-1..6 stables.
- (event) Self-validate Pass 2 (préservation) : toutes les capacités CAP-3..7 et constraints du SPEC.md chess_ai source pertinentes pour jax_supervised_training retrouvées dans le nouveau SPEC.md (CAP-7 source converti en open question, non un critère de succès, cohérent avec le success signal source). CAP-1/CAP-2 du SPEC.md source (dataset chess_ai) volontairement non dupliqués - hors périmètre de cette épic, restent proprieté chess_ai. La loss (direction forte de la discussion, jamais formalisée côté chess_ai) est formalisée ici en CAP-2, pas laissée en open question. Verdict : spec prêt, 6 open questions et 3 assumptions à revoir avec Aymeric avant implémentation.
- (decision) K/token_dim du bottleneck TRANCHÉ par Aymeric : K=8/token_dim=64 (config par défaut de model_library.py). Décision explicite d'accepter que CAP-4/CAP-5 ne soient PAS une comparaison à variable unique contre le checkpoint 28,00% (K=16/token_dim=196) - 'on repart à neuf', la comparabilité stricte n'est plus recherchée pour ce spike.
- (decision) Conséquence directe de la décision K=8/token_dim=64 ci-dessus : CAP-4 (accuracy vs 28,00%) et CAP-5 (réduction params vs baseline) redeviennent des mesures de référence informationnelles, plus un critère de comparaison contrôlée à variable unique - le Success signal est reformulé en conséquence (mesuré/documenté, pas 'pass/fail' strict contre le chiffre 28,00%).
- (decision) Dimension D des embeddings token (nn.Embed(13,D)) et positionnel (nn.Embed(64,D)) : D=64, alignée sur token_dim=64 du tronc/bottleneck (décision Winston, délégation explicite d'Aymeric 'je te fais confiance') - évite une couche de projection supplémentaire entre les embeddings d'entrée et le bottleneck qui consomme déjà token_dim=64 en KV.
- (note) CORRECTIF Winston : proposition initiale nn.Embed(NUM_MOVES=4672, D_move) pour représenter chaque candidat rejetée par Aymeric - réintroduit exactement la structure dimensionnée sur 4672 que CAP-5 cherche à éliminer, juste déplacée côté entrée. Vérifié sur le .npz réel (chess_token_candidate_spike.npz) : candidate_label ∈ [0,50) (index de slot, PAS un index move_to_index) - l'espace de sortie à 50 était déjà correct côté CAP-2/loss, l'erreur portait uniquement sur la représentation d'ENTRÉE de chaque candidat.
- (decision) Représentation d'un coup candidat DÉCOMPOSÉE, jamais via une table nn.Embed(4672,-) : move_to_index encode index=from_square×73+move_type (chess_ai/chess_target_encoding.py:243, move_type∈[0,73) - 0-63 type direction/distance dame, 64-72 sous-promotions). from_square=index//73 réutilise LA MÊME table d'embedding positionnel (64,D=64) déjà dans le tronc (poids partagés, 0 param supplémentaire). move_type=index%73 utilise une nouvelle petite table nn.Embed(73, D_type=32) - 73×32=2 336 params, indépendante de 4672. Slot -1 (padding) non décodé, candidate_mask suffit.
- (decision) Nom exact de l'entrée dataset_configs.py : CHESS_TOKEN (décision Aymeric, pas CHESS_TOKEN_CANDIDATE proposé initialement par chess_ai).
- (decision) Condition d'arrêt anticipé de CAP-6 : mécanisme standard patience/nombre d'epochs configuré (même pattern que les autres configs échecs, ex. patience=8) - PAS de détection sur mesure d'un décrochage vs une baseline CNN, cohérent avec la décision 'on repart à neuf' (K=8/token_dim=64, pas de comparaison contrôlée). L'idée de bascule vers un tronc conv-stem léger (SPEC.md chess_ai source) reste une piste de repli documentée mais n'est pas un mécanisme actif de ce spike.
- (decision) Compatibilité de checkpoint avec tournament_model_vs_model.py (chess_ai) : AUCUNE compatibilité requise pour ce spike (décision Aymeric). Le nouveau format de sortie (scores sur 50 candidats, pas un vecteur policy fixe (B,4672)) est fondamentalement différent - l'adaptation de l'outillage self-play côté chess_ai est un travail futur séparé, explicitement hors du périmètre de cette épic. Aucune rétrocompatibilité avec les .pkl existants n'est demandée.
- (event) Re-dérivation SPEC.md après résolution des 6 open questions avec Aymeric (K=8/token_dim=64, embeddings D=64, décomposition candidat from_square+move_type au lieu d'une table 4672, CHESS_TOKEN, arrêt anticipé standard, aucune compat checkpoint). Open Questions vidée, section omise.
- (event) Self-validate Pass 1 (cohérence) : OK - 6 capacités intent+success, 6 constraints toutes contraignantes (dont la décomposition from_square+move_type qui exclut explicitement une table 4672 côté entrée), 7 non-goals, success signal testable et explicitement reformulé en mesures de référence (pas pass/fail) suite à la décision K=8/token_dim=64. IDs CAP-1..6 stables.
- (event) Self-validate Pass 2 (préservation) : toutes les décisions actées du memlog (batch de résolution 2026-08-13) retrouvées dans SPEC.md - aucune perte. Verdict : spec prêt, 0 open question, 2 assumptions restantes (data-hunger sans garde-fou comparatif, dataset .npz vérifié comme source de vérité).
