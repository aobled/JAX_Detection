---
topic: Input tokenisé (64 tokens/case + embedding positionnel) et sortie scoring sur candidats légaux, côté chess_ai uniquement
goal: Spike temps 1 : dataset + encodage prêts pour une épic jax_supervised_training séparée qui teste tronc auto-attention + tête de scoring candidats
updated: 2026-08-13T10:55
---

- (direction by user) Espace de sortie fixe 4672 classes identifié comme problème (78% des 382k params du modèle dans Dense(4672)), input CNN aussi remis en cause — discussion tenue dans docs/chess-architecture-v2-hypotheses.md (2026-08-13)
- (decision) Bottleneck K=8 (cross-attention + auto-attention, model_library.py) reste inchangé structurellement pour ce spike, en pause — seul ce qui l'alimente change
- (decision) Vocabulaire de tokens input = 13 entrées statiques par case (ami-{Roi,Dame,Tour,Fou,Cavalier,Pion} + ennemi-{idem} + case vide), encodage RELATIF au joueur au trait (pas blanc/noir absolu) pour préserver la normalisation déjà en place dans chess_target_encoding.py
- (constraint) Drapeaux d'état globaux (trait, 4 droits de roque, répétition) restent des scalaires hors-token, comme aujourd'hui — pas assez d'entrées pour justifier un embedding dédié
- (constraint) Règles dynamiques/dérivées (ex. roque interdit par échec courant) ne sont JAMAIS encodées dans l'input — délégué entièrement à python-chess via la génération de candidats (AD-18), jamais réimplémenté
- (decision) Embedding positionnel : table apprise (64, D) libre, une par case — option la plus simple retenue en premier ; biais géométrique explicite (distance/diagonale partagée, lié à AD-23 différé) resterait à instruire plus tard si besoin
- (decision by user) Tronc : auto-attention pure dès le premier étage (pas de CNN), remplace le tronc CNN actuel
- (assumption) Risque data-hunger connu de l'attention pure sans biais de localité (ViT vs CNN à petite échelle, ImageNet-1k) accepté avec un garde-fou explicite plutôt qu'écarté par précaution — le plateau (64 cases) est bien plus petit qu'une image naturelle, donc pas d'obstacle de principe démontré, juste un risque à surveiller
- (decision) Condition d'arrêt anticipé : si convergence train/val de l'auto-attention pure est nettement pire que la baseline CNN à budget de données égal (écart croissant façon move_token dès epoch 9, ou accuracy qui plafonne significativement sous 28%), basculer sur un tronc conv-stem léger devant l'attention
- (decision by user) Sortie : remplacer Dense(4672) par un score sur les candidats de board.legal_moves, réutilisant move_to_index/index_to_move tel quel comme vocabulaire de coup (AD-18) — aucun nouveau schéma d'encodage de coup
- (decision by user) MAX_CANDIDATES=50 (couvre la quasi-totalité des positions réelles, 20-40 typiques, la queue à 218 est négligeable), padding + masque additif -inf avant softmax, pattern standard type masque d'attention
- (constraint) chess_ai ne définit ni n'entraîne jamais de modèle (contract-chess-ai-training-interface.md §4) — model_library.py, tronc auto-attention et tête de scoring vivent exclusivement côté jax_supervised_training, épic séparée dans une session séparée (même patron que spec-chess-move-token-poc CAP-3), confirmé explicitement avec l'utilisateur le 2026-08-13
- (constraint) Dataset spike hors du contrat stable contract-chess-ai-training-interface.md §2.1 tant que non concluant — tooling autonome, même patron que le spike move-token et le POC génétique
- (decision) Professeur = chess_search.py profondeur 12 (même profondeur que CHESS_SEARCH_TEACHER existant) pour rester comparable aux baselines déjà mesurées (28,00% PolicyAccuracy, 78,0%/94,0% répétition self-play)
- (capability) CAP-1 chess_ai : encoder une position en tokens (64 IDs entiers 0-12 + drapeaux globaux scalaires) — round-trip vérifiable
- (capability) CAP-2 chess_ai : générer le dataset spike (position tokenisée + candidats légaux paddés MAX_CANDIDATES=50 + masque + label professeur) frais et seedé, script autonome
- (capability) CAP-3 jax_supervised_training (session séparée) : tronc auto-attention + bottleneck K=8 inchangé + tête de scoring candidats, s'entraîne sans erreur sur le dataset spike
- (capability) CAP-4 : PolicyAccuracy top-1 comparable ou meilleure que la baseline teacher 28,00%, à parité dataset/epochs
- (capability) CAP-5 : réduction mesurée du nombre de paramètres vs baseline actuelle (382k dont 299k dans Dense(4672))
- (capability) CAP-6 : convergence train/val surveillée, sert de déclencheur à la condition d'arrêt anticipé (bascule conv-stem)
- (capability) CAP-7 : mesure de référence self-play (nouveau modèle vs lui-même, 100 parties, protocole exact tournament_model_vs_model.py déjà utilisé pour teacher-vs-teacher/move_token-vs-move_token) — non un critère de succès/échec, sert de point zéro pour un futur temps 2 génétique
- (note) Non-objectif explicite : le taux de répétition en self-play n'est pas jugé ici — le modèle n'a reçu aucun signal lié à l'issue de partie à ce stade, le juger là-dessus reproduirait la confusion déjà nommée pour move_token
- (note) CHESS_LEGAL_MOVES (F1=0,86, overlay écarté en 2026-08-02) devient sans objet comme direction produit séparée : la légalité est garantie par construction (candidats générés par python-chess) plutôt que devinée par un modèle séparé
- (note) Non-goal : définition/entraînement du modèle — épic jax_supervised_training séparée
- (note) Non-goal : intégration au contrat stable contract-chess-ai-training-interface.md §2.1 à ce stade
- (note) Non-goal : régénération du dataset à pleine échelle
- (note) Non-goal : biais géométrique explicite dans l'embedding positionnel (réserve future, pas ce spike)
- (note) Non-goal : training génétique/self-play avec sanction sur répétition (temps 2, hors scope) — rappel : une pénalité de répétition en fitness a déjà été rejetée deux fois dans le POC génétique précédent, à réexaminer pas reconduire automatiquement
- (note) Non-goal : refonte du bottleneck K=8 (en pause, dépend du résultat de ce spike)
- (question) Nom exact de la nouvelle entrée dataset_configs.py côté jax_supervised_training (ex. CHESS_TOKEN_CANDIDATE) — à fixer dans l'épic séparée, pas ce spec
- (event) Spec dérivé du memlog, prêt pour self-validate
- (decision) Politique MAX_CANDIDATES=50 précisée en écrivant le companion schema : positions à plus de 50 coups légaux sont FILTRÉES (non écrites dans le dataset), jamais tronquées avec réordonnancement — un ordre truqué (ex. coup professeur toujours en tête) laisserait le modèle deviner le label par sa position plutôt que par son contenu. Ordre des candidats = ordre naturel board.legal_moves, jamais modifié. Padding sentinel = -1 (hors [0, NUM_MOVES), sans ambiguïté même sans lire le masque).
- (event) Self-validate — Pass 1 coherence: OK (7 capabilités intent+success, 9 constraints toutes contraignantes, 6 non-goals, success signal testable avec CAP-7 explicitement hors critère, IDs stables). Pass 2 preservation: toutes les décisions actées du memlog retrouvées dans SPEC.md/companion ; seule exception volontaire (wrapper-only, non bloquante) — l'observation 'CHESS_LEGAL_MOVES overlay devient sans objet' reste dans le memlog/source (chess-architecture-v2-hypotheses.md) mais n'est pas dupliquée dans le kernel, aucune capability n'en dépend. Verdict : spec prêt.
- (decision) CORRECTIF (auto-detecte en lisant dataset_builder/chess_search_teacher_dataset_tools.py avant d'ecrire le generateur) : le professeur doit etre STOCKFISH profondeur 12, pas chess_search.py. CHESS_SEARCH_TEACHER (baseline 28,00%) a abandonne chess_search.py le 2026-08-07 (2.44 pos/s mono-processus, ~84h a l'echelle) au profit de Stockfish (moteur natif, plus rapide ET plus fort). SPEC.md initial (erreur de cette session) disait 'chess_search.py profondeur 12' — corrige partout en 'Stockfish profondeur 12', seul choix qui reste reellement comparable a la baseline existante.
- (event) CAP-1 implemente et teste : encode_position_tokenized/decode_tokenized_position/encode_legal_candidates ajoutes a chess_target_encoding.py, 9 tests unitaires (test_chess_target_encoding_tokenized.py) tous verts (round-trip position de depart/apres coups/roque/droits de roque perdus/partie aleatoire 60 coups, encodage relatif ami/ennemi verifie explicitement, filtrage >MAX_CANDIDATES verifie sur la position record a 218 coups). Suite existante (test_chess_move_token_encoding.py, test_chess_bottleneck_genetic.py, 19 tests) toujours verte - aucune regression.
- (event) CAP-2 implemente et smoke-teste : chess_token_candidate_spike_dataset.py cree (structure portee mecaniquement de chess_move_token_spike_dataset.py deja valide - mono+parallele, seede, checkpoints, merge). Smoke test reel (2 parties, mono-processus, Stockfish teacher_depth=12) : 348 positions generees, 0 filtree, .npz relu et verifie exhaustivement (13 assertions par position sur les 348 - dtypes, shapes, plage des IDs token/coup, coherence masque/label/padding) - toutes passees.
- (event) Perimetre chess_ai de ce spec (CAP-1, CAP-2) considere complet et verifie a petite echelle. Reste : un run a plus grande echelle si Aymeric le souhaite avant handoff, puis CAP-3+ (jax_supervised_training, epic separee, hors scope de cette session par decision explicite du 2026-08-13).
- (event) Run a l'echelle lance (2026-08-13) : 3000 parties, seed=42, n_workers=7 (defaut, 16 coeurs), teacher_depth=12, arriere-plan, output /home/aobled/Documents/data/chunks/chess_token_candidate_spike/chess_token_candidate_spike.npz. Decision Aymeric : commencer simple a 3000, augmenter le volume une fois la boucle complete validee (meme prudence que le spike coup-token, qui avait demarre a 3000 avant d'etendre a 10000).
- (event) Run 3000 parties termine (2026-08-13, ~1h00) : 412 574 positions gardees, 1275 terminales ignorees, 3732 filtrees (>MAX_CANDIDATES=50 coups legaux, soit 0,89% des positions non-terminales - mesure, PAS 'proche de zero' comme suppose dans les Assumptions du spec, a corriger). Verification exhaustive vectorisee sur les 412 574 positions : dtypes/shapes conformes au schema, IDs token dans [0,13), global_flags binaires, coherence masque/padding/label sur 100% des lignes, invariant 'exactement 1 roi par camp' verifie sur 100% des lignes. Validation croisee inattendue : 412574+3732=416306, EXACTEMENT le total de positions non-terminales du 1er run du spike coup-token (meme seed=42, memes parametres generate_selfplay_positions, memes 3000 parties) - confirme independamment l'absence de bug de generation cote auto-jeu, les deux scripts partagent le meme flux RNG/positions en amont malgre un encodage totalement different en aval.
- (note) Correction Assumptions : le taux de filtrage MAX_CANDIDATES=50 mesure (0,89%) est faible mais pas negligeable au sens strict - a garder en tete si un futur run a plus grande echelle veut estimer combien de positions seront perdues (proportion stable attendue, pas un artefact de ce run precis).
