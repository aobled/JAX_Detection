---
topic: Généraliser la sélection automatique de compute_dtype (précision mixte interne des couches) par backend matériel détecté (bfloat16 sur TPU, float32 sur GPU/CPU), disponible à tout type de modèle plutôt qu'à ChessMoveTokenTransformer seul
updated: 2026-08-17T12:37
---

- (direction) Aymeric demande de généraliser compute_dtype (2026-08-17, en clôturant la branche chess Epic 11/CHESS_TOKEN_1_MOVE) : sujet identifié comme reporté 'à une future epic' (sprint-status.yaml:396, epic 11, status backlog), transverse au projet (pas spécifique aux échecs).
- (note) État actuel : compute_dtype est une string codée en dur par config (dataset_configs.py:966, 'bfloat16' fixe sur CHESS_MOVE_TOKEN uniquement), forwardée conditionnellement à model_kwargs si présente dans la config (main.py:154-160). Seule ChessMoveTokenTransformer (model_library.py:1130) déclare/consomme ce champ (dtype des couches Dense/Attention, poids maîtres/checkpoint restent float32). Mécanisme distinct et indépendant du dtype global d'entrée (main.py:31-41, float16 sur TPU ET GPU, casts les données pas le calcul interne des couches).
- (decision) compute_dtype doit être dérivé automatiquement du backend détecté au runtime (jax.default_backend(), même détection que main.py:31-41), pas lu depuis une string codée en dur dans la config du dataset — décision actée par Aymeric.
- (decision) Règle de dérivation : bfloat16 si backend=='tpu', float32 sinon (GPU et CPU). Confirmé factuel côté TPU (natif bfloat16, float32 dégrade fortement, diagnostic réel documenté 2026-08-10/11). Côté GPU, float32 est un choix de PRUDENCE PROJET (aucune mesure de gain faite, 2 crashs GPU locaux déjà documentés ce projet) et non une limite matérielle absolue (GPU récents supportent bfloat16/float16 via Tensor Cores) — activer bfloat16/float16 sur GPU est explicitement hors scope, décision distincte future.
- (capability) CAP-1 Sélection automatique par matériel : le point d'entrée d'entraînement dérive compute_dtype du backend détecté au lieu de le lire dans la config du dataset. Succès : lancer n'importe quelle config sur TPU applique bfloat16 au calcul interne des couches concernées sans clé compute_dtype dans la config ; le même lancement sur GPU/CPU applique float32.
- (capability) CAP-2 Mécanisme générique, pas chess-only : le calcul de compute_dtype par backend est centralisé (pas dupliqué par domaine), et au moins un modèle non-échecs existant (CNN classification, ex. CIFAR10/FIGHTERJET_CLASSIFICATION) peut déclarer/consommer compute_dtype sans logique de détection matérielle propre à sa classe. Succès : test réel CIFAR10 (local, GPU/CPU) + test réel FIGHTERJET_CLASSIFICATION (Colab, TPU) démontrent le bon dtype appliqué selon le matériel, sans régression sur les autres domaines (échecs, détection).
- (constraint) Non-régression obligatoire sur CHESS_MOVE_TOKEN, seul consommateur actuel de compute_dtype (comportement bfloat16/TPU inchangé après généralisation, checkpoint-compatible).
- (constraint) Les poids 'maîtres' (checkpoint) restent float32 dans tous les cas — seule la dtype de CALCUL des couches (Dense/Attention) varie ; pattern déjà établi par ChessMoveTokenTransformer à préserver, pas réinventer.
- (constraint) Ne doit pas casser un modèle qui ne déclare pas/ne consomme pas compute_dtype — le forwarding généralisé ne doit pas planter (TypeError kwarg inattendu) sur un modèle Flax n'ayant pas ce champ ; a un impact direct sur la conception du point d'injection commun (get_model/factories).
- (non-goal) Ne pas activer/tester bfloat16 ou float16 sur GPU dans ce spec (décision distincte, mesurée séparément plus tard).
- (non-goal) Ne pas toucher au mécanisme séparé de cast de l'ENTRÉE (dtype global main.py:31-41, float16 sur TPU et GPU) — sujet distinct, hors scope.
- (non-goal) Ne pas étendre compute_dtype à tous les modèles existants dans ce spec — rendre le mécanisme générique/disponible suffit ; l'adoption modèle par modèle peut suivre après validation. Les deux modèles touchés par les tests demandés (CIFAR10, FIGHTERJET_CLASSIFICATION) sont l'exception nécessaire pour valider CAP-2, pas un rollout complet.
- (non-goal) Pas de nouveau domaine/dataset créé par ce spec.
- (assumption) CIFAR10 (modèle sophisticated_cnn_32_plus) et FIGHTERJET_CLASSIFICATION (modèle sophisticated_cnn_128_lite) ne déclarent aujourd'hui aucun champ compute_dtype — les rendre compatibles fait partie du travail de ce spec (pas seulement le point d'injection central), sinon les tests demandés ne peuvent pas s'exécuter.
- (question) Le point d'injection central doit-il forwarder compute_dtype à TOUT modèle inconditionnellement (obligeant chaque classe modèle à déclarer le champ, quitte à l'ignorer) ou seulement aux modèles qui déclarent explicitement le supporter (registre/introspection, forwarding conditionnel comme aujourd'hui) ? Impacte directement l'ampleur du changement sur les modèles CNN existants (CIFAR10/FIGHTERJET) — décision d'architecture, pas tranchée par Aymeric à ce stade.
- (question) Faut-il retirer la string 'compute_dtype': 'bfloat16' codée en dur de la config CHESS_MOVE_TOKEN (dataset_configs.py:966) une fois la dérivation automatique en place, ou la laisser en filet de sécurité/documentation ? Non tranché.
- (decision) Plan de validation demandé par Aymeric : (1) test rapide en local sur CIFAR10 (GPU/CPU) — vérifie que le backend non-TPU résout bien float32, aucune régression ; (2) test via Colab sur FIGHTERJET_CLASSIFICATION (TPU) — vérifie que bfloat16 est bien appliqué sur un modèle non-échecs en conditions matérielles réelles.
- (note) Précédent直接 comparable pour la rigueur de validation attendue : AD-29 (Epic 11, CHESS_MOVE_TOKEN) — un cast de précision incorrect avait silencieusement corrompu des tokens (float16 sur des ids > 2048) ; validé à l'époque par un test qui inspecte réellement le dtype/les valeurs après passage dans le pipeline, pas seulement par lecture de code. Même standard attendu ici.
- (note) Correction (coquille d'encodage sur l'entrée précédente) : précédent directement comparable pour la rigueur de validation attendue = AD-29 (Epic 11, CHESS_MOVE_TOKEN) — un cast de précision incorrect avait silencieusement corrompu des tokens (float16 sur des ids > 2048), validé à l'époque par un test qui inspecte réellement le dtype/les valeurs après passage dans le pipeline, pas seulement par lecture de code. Même standard attendu ici.
- (event) SPEC.md dérivé (create). Self-validate pass 1 (cohérence, règles 1-6+8) : OK, 2 capabilities (CAP-1, CAP-2) chacune intent+success, 4 constraints qui contraignent réellement, 4 non-goals, success signal testable (2 exécutions réelles), aucun ID réutilisé, prose sans décoration.
- (event) Self-validate pass 2 (préservation) : chaque entrée du memlog (direction, notes, décisions, capabilities, constraints, non-goals, assumption, questions) retrouvée dans SPEC.md — aucun contenu porteur perdu, aucun drop wrapper-only à consigner.
- (decision) Résolution OQ1 (Aymeric, 2026-08-17) : forwarding UNCONDITIONNEL, pas conditionnel/registre. Si backend détecté == TPU, compute_dtype=bfloat16 s'applique quel que soit le modèle — chaque classe modèle doit structurellement déclarer/consommer le champ (pas un opt-in par introspection). Conséquence directe : la contrainte anti-crash ('ne doit pas planter sur un modèle qui ne déclare pas compute_dtype') se satisfait en ADAPTANT chaque modèle plutôt qu'en gardant certains modèles hors mécanisme.
- (decision) Résolution OQ2 (Aymeric, 2026-08-17) : retirer la string codée en dur 'compute_dtype': 'bfloat16' de la config CHESS_MOVE_TOKEN (dataset_configs.py:966) une fois la dérivation automatique en place — confirmé, c'était déjà l'intention de base d'Aymeric. La détection runtime devient la source unique, plus de littéral par config.
- (direction) Aymeric route explicitement la conception du point d'injection (comment chaque modèle adopte compute_dtype) vers bmad-architecture, pas décidée ici. Séquencement demandé : (1) étudier/produire d'abord l'impact concret sur CIFAR10 (sophisticated_cnn_32_plus) comme cas de référence adapté, (2) SEULEMENT ENSUITE discuter de la généralisation du concept à l'ENSEMBLE des modèles du projet (au-delà des 2 modèles déjà dans le scope de validation de ce spec, CIFAR10+FIGHTERJET_CLASSIFICATION). La généralisation totale à tous les modèles reste donc hors du scope immédiat de CE spec (non-goal inchangé), mais est actée comme la trajectoire cible voulue par Aymeric, à traiter en second temps avec Winston.
- (event) SPEC.md re-dérivé (update) après décisions Aymeric 2026-08-17 : OQ1/OQ2 résolues et déplacées vers une section 'Decisions', constraint forwarding mise à jour (unconditionnel), non-goal rollout complet clarifié (trajectoire cible actée, séquencement CIFAR10 d'abord puis bmad-architecture pour la généralisation). Self-validate : cohérence OK (plus d'open questions actives), préservation OK (aucune perte, la vieille formulation OQ1/OQ2 remplacée par leur résolution).
- (event) Update déclenché par la finalisation de la spine bmad-architecture (architecture-compute-dtype-hardware-2026-08-17/ARCHITECTURE-SPINE.md, status: final). L'audit brownfield de cette spine (lecture réelle des 12 factories de MODELS, model_library.py) a renversé la résolution OQ1 de ce SPEC — voir décision ci-dessous.
- (decision) RENVERSEMENT d'OQ1 (Aymeric, via coaching bmad-architecture, 2026-08-17) : le forwarding UNCONDITIONNEL décidé précédemment ('pas de registre/introspection') est abandonné au profit d'un forwarding par INTROSPECTION (main.py vérifie qu'un paramètre nommé explicitement compute_dtype existe dans la signature de la factory cible avant d'injecter). Raison : audit concret montrant que 6 des 12 modèles de MODELS planteraient immédiatement (aucun **kwargs, aucun champ compute_dtype) et 3 autres (aircraft_detector_unet/centernet/centernet_lite) avaleraient la valeur SILENCIEUSEMENT (bug pré-existant déjà documenté dans le code : **kwargs accepté en façade mais jamais transmis au constructeur) — contredisant directement l'objectif de non-régression de CAP-2. La décision OQ1 originale reste visible dans l'historique de ce memlog ; cette entrée la supersede explicitement, ce n'est pas une correction silencieuse.
- (decision) Fermeture de la faille laissée par l'ancienne formulation 'unconditionnel' : la SEULE façon sanctionnée pour un modèle d'être exempté de compute_dtype dérivé du matériel est de ne pas déclarer le paramètre nommé (mécanisme d'introspection). Un flag de config par dataset reste interdit dans tous les cas, y compris présenté comme une 'exception' ponctuelle — ça reproduirait exactement l'anti-pattern que la dérivation matérielle unique (ex-OQ1 partie 'pas de string par config') visait à éliminer.
- (decision) Scope CAP-2 confirmé concrètement (ex-assumption levée) : exactement 2 modèles adaptés dans ce chantier — CIFAR10 (sophisticated_cnn_32_plus) ET FIGHTERJET_CLASSIFICATION (sophisticated_cnn_128_lite), pas juste 'au moins un'. Les deux sont nécessaires pour que le plan de test du SPEC (local + Colab TPU) valide réellement quelque chose : ce sont deux classes top-level SÉPARÉES malgré des sous-modules partagés (SeparableConv/SEBlock/SpatialAttention).
- (constraint) compute_dtype s'applique uniquement aux couches nn.Conv/nn.Dense (matmul lourd) — jamais à nn.BatchNorm/nn.LayerNorm/nn.Embed. Ratifie le principe déjà établi par ChessMoveTokenTransformer (seules les couches matmul lourdes bénéficient réellement d'un calcul en précision réduite ; pas une question de sécurité numérique). Détail décidé pendant la spine (AD-4), pas dans la résolution OQ1 initiale.
- (constraint) La logique de validation existante de create_chess_move_token_transformer (string->jnp.dtype, model_library.py:1218-1221) N'EST PAS retirée/centralisée — préservée telle quelle, couverte par un test de régression existant (test_compute_dtype_factory_rejects_unknown_string) qui teste l'appel direct hors main.py. La résolution centralisée (ex-OQ2) s'applique seulement aux NOUVELLES factories (CIFAR10, FIGHTERJET).
- (constraint) Nouvelle exigence (surfacée par la Reviewer Gate de la spine) : tout modèle adoptant compute_dtype doit avoir un test dédié qui vérifie que le dtype est RÉELLEMENT observé (paramètres/sortie diffèrent sous bfloat16 vs float32) — pas seulement que l'appel de factory ne lève pas d'erreur. Même rigueur que le test existant test_compute_dtype_bfloat16_params_and_output_stay_float32. Un paramètre nommé mais jamais réellement transmis au nn.Module reproduirait le bug aircraft_detector_* un niveau plus bas, silencieusement.
- (note) Compte corrigé (était approximatif) : 9 modèles (pas 10) restent hors scope/différés - aircraft_detector_unet/centernet/centernet_lite, chess_cnn_attention_policy_value/legal_moves, chess_token_candidate_model, chess_token_one_move_model, kepler_1d_cnn, sophisticated_cnn_128_plus. Non-goal 'pas de rollout complet' inchangé sur le fond, juste chiffré précisément désormais.
- (direction) ARCHITECTURE-SPINE.md (architecture-compute-dtype-hardware-2026-08-17) référencée comme companion ADOPTÉ (pas spec-authored, propriété de bmad-architecture) - AD-1 à AD-7 gouvernent désormais l'implémentation, ce SPEC n'a pas besoin de dupliquer leur contenu.
- (event) SPEC.md re-dérivé (update, 2026-08-17) suite au renversement d'OQ1 par la spine bmad-architecture. Self-validate pass 1 (cohérence) : OK — CAP-1/CAP-2 intent+success, IDs préservés, 6 constraints qui contraignent réellement, 4 non-goals, success signal testable. Pass 2 (préservation) : chaque entrée du memlog (renversement OQ1 tracé sans écraser l'historique, fermeture de la faille opt-out, scope 2 modèles confirmé, restriction Conv/Dense, préservation validation ChessMoveTokenTransformer, nouvelle exigence de test par modèle, compte 9 modèles corrigé, companion ARCHITECTURE-SPINE.md ajouté) retrouvée dans SPEC.md ou son frontmatter — aucune perte.
- (event) Implémentation (bmad-quick-dev, 2026-08-17) : trou réel trouvé dans le mécanisme AD-3 (introspection cible aussi create_chess_move_token_transformer, mismatch type string vs jnp.dtype déjà résolu -> TypeError). Fix minimal appliqué (accord Aymeric), détail complet dans le memlog de la spine architecture-compute-dtype-hardware-2026-08-17 et le Spec Change Log de _bmad-output/implementation-artifacts/spec-compute-dtype-hardware.md. SPEC.md/ARCHITECTURE-SPINE.md pas re-dérivés pour ce delta mineur (comportement/tests existants préservés à 100%, changement additif) - à re-dériver seulement si Aymeric le souhaite.
- (decision) AMENDEMENT (2026-08-17, post-rollout groupe 1) : la contrainte 'compute_dtype uniquement Conv/Dense, jamais BatchNorm/LayerNorm' est levée pour BatchNorm/LayerNorm — dtype=compute_dtype s'applique désormais aussi à ces deux (jamais Embed, inchangé). Raison : revue Blind Hunter a montré empiriquement que ne pas le faire réinitialise la chaîne de précision réduite à float32 à chaque bloc de normalisation, videant une partie du gain visé. Vérifié sûr (force_float32_reductions protège la réduction interne indépendamment de dtype=, confirmé empiriquement pour BatchNorm ET LayerNorm). Détail complet et raisonnement dans le memlog de la spine (architecture-compute-dtype-hardware-2026-08-17), AD-4 amendé là-bas. Processus : implémentation directe via bmad-quick-dev, pas de nouvelle session bmad-architecture dédiée (choix Aymeric, vérification empirique déjà faite en amont).
