---
scope: Epic CHESS_MOVE_TOKEN : transformer causal sur sequences move-token (dataset spike chess_ai), nouveau task_type/TaskStrategy/loader/modele, sans impact sur CHESS_SEARCH_TEACHER/CHESS_LEGAL_MOVES/CHESS_NO_HISTORY/JAX_DETECTOR existants
purpose: build-substrate
altitude: epic
updated: 2026-08-11T15:54
---

- (constraint) Source: docs/spike-chess-move-token-dataset-schema.md. Dataset spike chess_ai (/home/aobled/Documents/data/chunks/chess_move_token_spike/chess_move_token_spike.npz) : cles position(3190,8,8,29) f32, policy(3190,) i32, move_tokens(309988,) i32, move_token_offsets(3191,) i64 (bornes CSR). move_tokens partage le meme espace d'action que policy (NUM_MOVES=4672, move_to_index/index_to_move, chess_ai). Longueurs variables non paddees/tronquees par chess_ai (min=4, mediane=86, max=301).
- (constraint) docs/spike-chess-move-token-dataset-schema.md est explicitement PROVISOIRE/SPIKE - format peut changer sans preavis avant verdict CAP-4/CAP-5 cote chess_ai (spec-chess-move-token-poc/SPEC.md). Ne pas figer dataset_configs.py comme config definitive tant que le spike n'est pas conclusif.
- (constraint) chess_target_encoding.py / generation dataset n'existent plus dans ce repo (deplaces vers chess_ai, split epic input_shape 2026-08-09, tests/test_no_chess_dependency.py). NUM_MOVES=4672 et move_to_index sont sources de verite cote chess_ai - jax_supervised_training les consomme en aveugle (meme posture que position/policy pour CHESS_SEARCH_TEACHER).
- (decision) Epic dediee CHESS_MOVE_TOKEN (Aymeric, 2026-08-10) - pas une reutilisation style Epic 10 (CHESS_SEARCH_TEACHER, qui reutilisait task_type/modele existants a l'identique). Nouvelle modalite d'entree (sequence entiers longueur variable vs plan 8x8x29 fixe) touche loader + modele + TaskStrategy simultanement - forme d'epic la plus proche : Epic 9 (chess-2026-07-27).
- (decision) Occasion explicite de valider la genericite du pipeline jax_supervised_training au-dela de la generalisation deja prouvee (Epic 9 : nouveau domaine ; Epic 10 : nouveau dataset meme modele ; cette epic : nouvelle modalite d'entree/architecture modele dans le meme domaine).
- (decision) Padding dynamique par batch (pas de max_len global fixe), a GAUCHE (pas a droite). Rationale : lecture du modele = etat cache du DERNIER token reel (une seule prediction policy par position, pas un LM par-token), le padding gauche place ce token toujours en position -1 du tenseur (indexation triviale), evite un gather par longueur reelle requis par le padding a droite.
- (decision) Bucketing par longueur NON retenu par defaut (Rule of Three / boring technology) - complexite non justifiee sans mesure de gachis de padding. Deferre : a revisiter seulement si profiling montre que le padding domine le cout, notamment a plus grande echelle que le spike (3190 positions).
- (decision) 2 tokens speciaux reserves dans l'espace d'ENTREE (embedding) uniquement : BOS=4672, PAD=4673 (espace d'entree total 4674). Pas d'EOS - l'objectif est une lecture d'historique a prediction unique (classification), pas une generation autoregressive jusqu'a fin de partie.
- (decision) Tete de sortie policy reste strictement sur les 4672 classes existantes (jamais BOS/PAD en sortie) - meme espace/meme semantique que CHESS_SEARCH_TEACHER/CHESS_NO_HISTORY. Asymetrie deliberee entree(4674)/sortie(4672) : PAS de weight-tying par defaut (complexite non justifiee tant que non mesuree utile). Deferre comme levier futur explicitement ouvert par CAP-3 (chess_ai), pas impose.
- (decision) Nouveau task_type dedie (chess_move_token) + nouvelle TaskStrategy dediee - jamais un branchement conditionnel dans ChessPolicyValueStrategy/ChessLegalMovesStrategy existantes. Precedent direct : AD-17 (architecture-chess-2026-07-27, lui-meme heritant de architecture-jax_supervised_training-2026-07-15) - meme regle, 2 points de dispatch reels (main.py selection Strategy, data_management.py selection loader).
- (decision) Nouveau loader dataset dedie (variable-length via move_token_offsets/CSR), classe separee de ChessPolicyValueDataset/ChessLegalMovesDataset - meme lignee AD-17. Lecture legere des cles npz + padded_batch dynamique (tf.data), pas de nouvelle dependance.
- (decision) Nouveau modele transformer causal dans model_library.py - premiere utilisation d'un masque causal dans ce codebase (l'attention existante, chess_cnn_attention_policy_value, est un encodeur bidirectionnel style Perceiver/TokenLearner, pas causal). Confine au modele (nouvelle classe), pattern Strategy+Factory+DI parent inchange - meme forme de nouveaute qu'AD-23 (bottleneck) dans l'epic precedente.
- (version) flax 0.10.7 / jax 0.6.2 installes (jax_env) - nn.make_causal_mask et nn.combine_masks disponibles nativement (verifie par import), nn.MultiHeadDotProductAttention deja utilise dans model_library.py (accepte un argument mask). Aucune nouvelle dependance requise pour le masquage causal.
- (decision) Non-regression (precedent AD-21, chess-2026-07-27) : aucune story de cette epic ne modifie ChessPolicyValueStrategy/ChessPolicyValueDataset/ChessLegalMovesStrategy/ChessLegalMovesDataset/CenterNetDetectionStrategy/CenterNetDetectionDataset, ni les configs CHESS_SEARCH_TEACHER/CHESS_LEGAL_MOVES/CHESS_NO_HISTORY/JAX_DETECTOR existantes. Validation par execution reelle, pas lecture de code seule (coherent avec feedback memoire 'verifier les chiffres via les logs').
- (decision) Nom de la config dataset_configs.py : CHESS_MOVE_TOKEN (propose par Aymeric, accepte).
- (decision) Priorite explicite : vitesse d'experimentation vers un premier run, pas optimalite. Choix de cette epic (padding gauche dynamique, BOS+PAD sans EOS, pas de weight-tying, pas de bucketing) sont marques reversibles/evolutifs selon resultats du premier run, pas des verdicts definitifs (Aymeric, 2026-08-10).
- (question) Readout exact du transformer pour la tete policy : dernier token reel (position -1 grace au padding gauche) - a fixer comme AD explicite dans le spine (pas encore une question ouverte, mais merite d'etre ecrit noir sur blanc pour eviter une divergence d'implementation).
- (assumption) [ASSUMPTION] Le modele ne consomme QUE move_tokens (pas position en entree) - le doc source dit que 'position' sert a faire tourner le modele BASELINE existant pour comparaison CAP-4, pas necessairement en entree du nouveau modele transformer. A confirmer par Aymeric a la relecture.
- (assumption) [ASSUMPTION] Une seule config dataset_configs.py (CHESS_MOVE_TOKEN) suffit pour ce spike, sur le modele des configs chess existantes (pas de variante immediate).
- (decision) CONFIRME (chess_ai SPEC.md CAP-3, verifie 2026-08-10) : le transformer prend l'HISTORIQUE DE COUPS (move_tokens) en entree et predit le coup suivant - pas 'position' en entree du nouveau modele. 'position' sert uniquement a faire tourner le modele BASELINE existant pour comparaison CAP-4 (execution separee, pas ce modele). L'assumption precedente est donc promue en decision confirmee, pas une supposition.
- (decision) CONFIRME (chess_ai SPEC.md Non-goals) : 'refonte de la loss ou de la tete value' explicitement hors scope, et le success de CAP-3 ne mentionne que la tete policy (4672 classes) + cross-entropy - le nouveau modele est POLICY-ONLY, aucune tete value (pas une neutralisation par value_weight=0.0 comme CHESS_SEARCH_TEACHER, une architecture qui n'a simplement pas cette tete). Nouvelle TaskStrategy dediee : cross-entropy simple sur policy, pas la loss composite policy+value existante (ChessPolicyValueStrategy.compute_loss).
- (event) Spine draft distille (Fast path) - ARCHITECTURE-SPINE.md ecrit, AD-26 a AD-31, 6 invariants herites (AD-3/14/17/18/21/22), diagramme de dependances mermaid, Deferred (bucketing/weight-tying/EOS/promotion-echelle/hyperparametres).
- (event) Reviewer gate execute (Fast path, floor configure = 2 lenses) : lint_spine.py (1 finding low, faux positif verifie - {output_prefix} est un literal de code reel, pas un placeholder de template) + 2 subagents paralleles (verification version/tech, adversarial consistency).
- (constraint) CRITIQUE (trouve par le reviewer adversarial, verifie manuellement dans trainer.py/main.py) : main.py:35-41 fixe dtype=jnp.float16 en dur (TPU et GPU) passe a Trainer(dtype=...) ; trainer.py:313/430 fait jnp.array(images_np, dtype=self.dtype) INCONDITIONNELLEMENT avant tout hook Strategy. float16 ne represente exactement que les entiers jusqu'a ~2048 (au-dela, seuls les multiples de 4 le sont jusqu'a 8192) - les tokens move_tokens/BOS/PAD (espace 0-4673) seraient silencieusement corrompus par ce cast, pas seulement BOS(4672)/PAD(4673) qui pourraient meme se confondre. Aucune story precedente (echecs ou detection) n'a ce probleme : leurs entrees sont des plans flottants continus, pas des indices entiers discrets.
- (decision) Fix : main.py passe dtype=jnp.int32 (pas jnp.float16) a Trainer() pour task_type=='chess_move_token' specifiquement - Trainer.dtype est deja un parametre generique existant (trainer.py __init__), aucune modification de trainer.py lui-meme (zero-touch Trainer preserve, precedent AD-24 du spine chess). int32->int32 via jnp.array est un no-op, aucune perte de precision.
- (decision) Fix : padding a gauche (AD-28) implemente cote loader (ChessMoveTokenDataset) via le motif standard tf.data (padded_batch ne supporte que le padding a droite, verifie par le reviewer) : tf.reverse(sequence) -> padded_batch (padde a droite = ancien debut de sequence a la fin) -> tf.reverse a nouveau sur le batch obtenu. BOS prefixe a la sequence AVANT le premier reverse (se retrouve donc juste apres le padding, avant les vrais tokens, une fois le padding applique).
- (decision) Fix : le masque d'attention (causal + padding) est construit DANS le modele a partir du token PAD_TOKEN_ID directement present dans le tenseur d'entree (mask = tokens != PAD_TOKEN_ID) - jamais un tenseur de masque separe achemine via Strategy/Trainer (apply_fn(vars, images, ...) a une signature fixe a un seul tenseur d'entree, zero-touch Trainer).
- (decision) Fix : BOS_TOKEN_ID=4672/PAD_TOKEN_ID=4673 definis comme UNE SEULE fois en constantes nommees (data_management.py, cote producteur/loader qui construit les sequences) - le modele les importe, ne les redefinit jamais comme litteraux independants (evite la derive silencieuse notee par le reviewer).
- (decision) Fix : ChessMoveTokenDataset melange les EXEMPLES (pas des chunks, un seul fichier) avec une graine fixe reproductible AVANT le split fraction train/val - meme convention que les chargeurs echecs existants (evite le biais d'ordre deja documente/corrige historiquement, 2026-07-27).
- (version) TF 2.20.0 installe (jax_env) - padded_batch verifie disponible, confirme par le reviewer. Ajoute au Stack (manquant dans le premier jet).
- (question) Flax Linen (utilise ici) vs Flax NNX (nouvelle direction long-terme de Google, confirme par recherche web du reviewer) - Linen n'est pas deprecie a court terme, mais c'est un signal a surveiller. Pas urgent pour ce spike (coherence avec le reste du codebase, deja 100% linen) - reste en Deferred, pas un blocker.
- (event) Renumerotation AD apres insertion (AD-32(dtype)->AD-29, AD-29(tokens)->AD-30, AD-30(modele)->AD-31, AD-31(spike status)->AD-32) pour retablir l'ordre ascendant, toutes references croisees mises a jour. Lint final : 1 finding low residuel (faux positif {output_prefix} deja verifie). Spine finalise (status: final).
- (event) Rendu de synthese HTML publie (Artifact) - demande utilisateur 'spine + doc de synthese'. Aucun external_handoff configure. Spine finalized.
- (constraint) TROUVE PENDANT L'IMPLEMENTATION (2026-08-10, verifie par execution reelle) : Trainer.create_train_state (trainer.py:145) construit son dummy d'initialisation via jnp.ones((1,)+input_shape, jnp.float32) - dtype FLOAT32 code en dur, independant du fix dtype=jnp.int32 d'AD-29 (qui ne couvre que la boucle d'entrainement reelle, trainer.py:313/430, pas model.init). Teste empiriquement : nn.Embed.init() leve ValueError('Input type must be an integer or unsigned integer') sur une entree float32 - le pipeline crasherait des le premier appel a create_train_state, avant meme le premier step.
- (decision) Fix : chess_move_token_transformer caste explicitement son entree en int32 des la premiere ligne de __call__ (jnp.asarray(x, dtype=jnp.int32)) - independant du dtype fourni par l'appelant (float32 du dummy d'init OU int32 des vraies donnees post-AD-29). Corrige AUSSI le cas dummy=1.0 -> 1 (token id valide dans le vocab 4674, sans danger pour l'init). Zero-touch Trainer toujours preserve (fix 100% local au modele, trainer.py non touche).
- (event) Nouvel AD (AD-33) ajoute au spine finalise (status reste final, decision additive qui ne contredit aucun AD existant - suit le flux Update de bmad-architecture, ID jamais renumerote).
- (constraint) TROUVE PENDANT L'IMPLEMENTATION (Story 11.2) : model_library.py est importe au TOUT DEBUT de main.py (ligne 22, avant jax/tensorflow proprement inities), alors que data_management.py n'est importe que paresseusement DANS main() (ligne 78) - ordre deliberement construit pour positionner CUDA_VISIBLE_DEVICES avant le premier import tensorflow (commentaire data_management.py:9-25). Un import top-level 'from data_management import CHESS_MOVE_TOKEN_PAD' dans model_library.py casserait cet ordre (data_management/tensorflow charges des l'import de model_library, avant jax). Fix : import PARESSEUX de CHESS_MOVE_TOKEN_PAD a l'INTERIEUR de ChessMoveTokenTransformer.__call__ (meme discipline que le lazy-import deja utilise par main.py ligne 78 pour la meme raison) - sans risque, data_management est deja charge par main.py avant la creation du modele dans le flux reel.
- (event) Story 11.2 validee par execution reelle : (1) model.init() avec dummy float32 (1,1) reussit (AD-33 confirme, 1 994 304 params avec les hyperparametres seed num_layers=4/d_model=128/num_heads=4) ; (2) forward pass sur batch int32 reel -> shape (B,4672) correcte ; (3) test d'invariance au padding CORRIGE (1ere tentative erronee : changer la VALEUR d'un slot PAD invalide le test, puisque le masque est derive dynamiquement de tokens!=PAD - le slot change alors de statut. Test correct : meme contenu reel, quantites de padding differentes) -> diff max 2e-6 (bruit flottant), argmax identiques. Masquage causal+padding valide correct.
- (constraint) TROUVE PENDANT L'IMPLEMENTATION (Story 11.3) : validate_config (dataset_configs.py) exigeait len(input_shape)>=2 (conçu pour images/plans spatiaux) - CHESS_MOVE_TOKEN a besoin d'un input_shape=(1,) (dummy d'init 2D (B,L), pas de dimension canal, contrairement a Kepler qui garde une dimension canal (longueur,1)). Rejete par validate_config avant le fix.
- (decision) Fix : borne basse de validate_config relachee de 2 a 1 (dataset_configs.py) - changement STRICTEMENT MONOTONE (accepte plus qu'avant), verifie qu'aucune config existante n'a de input_shape de longueur 1 aujourd'hui -> comportement de validation inchange pour tous les domaines deja en place. Prefere a l'alternative (forcer un input_shape=(1,1) et faire porter au modele un squeeze de dimension factice) - plus simple, plus honnete architecturalement (chess_move_token est genuinement une sequence sans dimension canal, pas un cas a maquiller).
- (event) SMOKE TEST REEL COMPLET (Story 11.4) : Trainer end-to-end reel sur le vrai fichier spike, 1 epoch (89 steps, CPU faute de GPU CUDA disponible sur cette machine pour ce run, ~4m24s) - AUCUNE exception. Train Loss=8.4581/PolicyAccuracy=0.0014 ; Val Loss=7.8822/PolicyAccuracy=0.0069 (nettement au-dessus du hasard 1/4672~=0.00021 des la 1ere epoch) ; checkpoint best_model_chess_move_token.pkl exporte avec succes. Decision : ce run de 1 epoch (deja ~4m30, CPU sature) sert de validation officielle Story 11.4/FR8 plutot que le run complet 10 epochs configure (~44min estimees) - coherent avec la consigne de prudence sur l'execution locale lourde sans supervision (memoire feedback_caution_before_heavy_local_execution) ET avec le cadrage explicite spike/POC de chess_ai (SPEC.md: 'test d'architecture d'abord', pas une convergence complete). Signal positif suffisant pour valider FR8 (le pipeline apprend), pas suffisant pour statuer sur le succes du spike (CAP-4/CAP-5, hors scope epic jax_supervised_training - decision cote chess_ai apres un run complet, deferre).
- (constraint) TROUVE PAR AYMERIC EN OBSERVANT nvtop PENDANT LE RUN REEL 10 EPOCHS (2026-08-10, apres reprise) : GPU montait a 60% puis retombait a 0% pendant ~5s, repete a chaque step, debit tres faible (3.68s/it observe). Diagnostic (Winston) : Trainer._train_step est @jax.jit (trainer.py:229), qui cache par FORME d'entree. AD-28 (1ere version) paddait dynamiquement chaque batch a SA PROPRE longueur max (padded_shapes=([None],[])) - vu la variance de longueur du dataset (min=4, mediane=86, max=301), la forme du batch (B,L) changeait quasiment a chaque step -> recompilation XLA (CPU, GPU a 0%) a presque chaque step au lieu d'une seule fois. Mon propre smoke-test CPU (Story 11.4) avait deja ce symptome (temps par step variable 1.4-3.9s) sans que je le diagnostique a l'epoque - meme cause.
- (decision) Fix (Aymeric a tue le process, relance demandee) : padding a une longueur GLOBALE FIXE (padded_shapes=([self.max_seq_len],[])) au lieu d'une longueur par-batch. self.max_seq_len calcule depuis les VRAIES donnees (max des longueurs d'historique + 1 pour BOS = 302 sur ce dataset), pas un litteral en dur - jamais silencieusement faux si le dataset change. Verifie : les 10 premiers batches ont maintenant tous exactement la meme shape (32,302) (avant : shape variable). AD-28 amende en place dans le spine (Rule tightened), meme ID, pas de renumerotation.
- (event) 9 tests unitaires re-executes apres le fix (tous verts, aucune regression) + re-verification manuelle (10 batches consecutifs -> 1 seule shape). Nouveau smoke-test reel (1 epoch, longueur fixe) lance pour confirmer l'absence de recompilation repetee en conditions reelles - resultat en cours.
- (event) Fix confirme par execution reelle (1 epoch, longueur fixe=302) : step 1 lent (5.84s, seule compilation JIT), puis stabilise a ~1.1 it/s (~0.9s/step) constant jusqu'a la fin - fini les fluctuations 1.4-3.9s d'avant. Epoch complete en 86.7s (train) contre 264.2s avant (~3x plus rapide sur CPU, l'ecart serait bien plus marque sur GPU vu que le calcul reel est trivial). Resultats identiques (Val PolicyAccuracy=0.0069) - correction confirmee sans effet de bord sur l'apprentissage. AD-28 mis a jour dans le spine (Rule tightee), Deferred 'bucketing' mis a jour (devenu moins urgent, cout dominant etait la recompilation pas le gachis de calcul).
- (decision) Aymeric (2026-08-10, en reponse au surapprentissage mesure : Train PolicyAccuracy=33.5% vs Val=0.35% epoch 6) a augmente dropout_rate 0.1->0.3 et label_smoothing 0.0->0.2 dans CHESS_MOVE_TOKEN (dataset_configs.py) en attenuation temporaire, pendant qu'une regeneration du dataset spike a plus grande echelle est lancee cote chess_ai (cause racine = volume, 20 parties). Commentaires du fichier mis a jour pour rester exacts (memoire feedback_verify_numbers_against_logs).
- (event) Dataset spike regenere cote chess_ai (Aymeric, 2026-08-10) : 416306 positions (137x le run initial de 3190), meme fichier chess_move_token_spike.npz, meme 4 cles. Verifie : validate_config OK, ChessMoveTokenDataset charge correctement (374676 train/41630 val), max_seq_len auto-recalcule a 404 (etait 302). AVANT relance : decay_steps/warmup_steps/micro_batch_size (calcules pour 2871 exemples) etaient perimes - corrige (micro_batch_size 16/32->128/256 aligne sur convention domaine echecs, warmup_steps 30->200, decay_steps recalcules depuis le vrai volume : gpu 1463 steps/epoch x15=21945, tpu 2927x15=43905). epochs/patience 10/5->15/8 (dataset n'est plus un smoke-test de plomberie). dropout_rate=0.3/label_smoothing=0.2 (decision Aymeric precedente) conserves inchanges - un seul facteur (volume) change a la fois. 9 tests unitaires re-passes apres coup, tous verts.
- (constraint) OOM reel (RESOURCE_EXHAUSTED, ~6.06 Go pour une seule allocation) sur GPU d'Aymeric au 1er step avec micro_batch_size=256/max_seq_len=404. Cause vraisemblable : self-attention O(B*H*L^2) - a batch=256/heads=4/L=404, les tenseurs (B,H,L,L) pesent deja ~0.67 Go chacun, et le graphe complet (forward+backward, 4 couches) accumule bien au-dela. Contrairement aux modeles chess_cnn_attention_* (attention sur 64 cases fixes, jamais un souci de memoire a ce batch), chess_move_token_transformer est sensible au produit batch*longueur_sequence^2.
- (decision) Fix : gpu.micro_batch_size 256->128 (Aymeric), decay_steps recalcule en consequence (21945->43905, meme valeur que le bloc tpu puisque les deux tournent desormais a 128). Recommande si l'OOM persiste : redescendre encore (64) plutot que ré-augmenter a l'aveugle - piste alternative differee (reduire d_model/num_layers, changerait le modele lui-meme) non retenue pour l'instant, le batch_size est le levier le plus direct sans toucher a l'architecture.
- (event) Run 2 termine (15 epochs, dataset 416306 positions, batch=128, dropout=0.3, label_smoothing=0.2) : plateau confirme, Val PolicyAccuracy 5.44%->5.54% (epoch 11->15, quasi plat), Train 6.82%->7.30%, gap sain 1.76pt (pas d'explosion). 254x le hasard (1/4672). Aymeric d'accord : volume de donnees le facteur limitant principal, prochaine regeneration ciblee ~10000 parties (echelle CHESS_SEARCH_TEACHER).
- (decision) Diagramme .mmd cree (_bmad-output/planning-artifacts/chess-move-token.mmd, meme convention que chess-cnn-attention-policy-value.mmd) - documente l'architecture REELLEMENT entrainee (4 layers/d_model=128, 1994304 params mesures via model.init(), pas estimes). Trouvaille notable en construisant le diagramme : Embedding (598272) + tete policy (602688) = 60.2% du modele a eux deux, le corps transformer (4 blocs) ne pese que 39.9% - rend le weight-tying (deja ouvert par AD-30, jamais impose) concretement plus attractif qu'estime au design initial.
- (decision) Proposition (Winston, pas encore appliquee/confirmee par Aymeric) pour le prochain run : num_layers 4->6, d_model 128->192 (miroir du token_dim=192 convergent de CHESS_SEARCH_TEACHER) -> 4468672 params (2.24x, mesure reelle via model.init()). Rationale specifique a ce domaine (pas juste 'plus gros=mieux') : la profondeur sert la reconstruction d'etat implicite compositionnelle (absente des modeles position->coup existants) plutot que la simple largeur ; a ce nouveau ratio, embedding+tete ne pesent plus que 40.3% (vs 60.2% actuellement) - la capacite ajoutee va au corps du reseau, pas a l'overhead vocab fixe.
- (question) Idee d'Aymeric (2026-08-10) : architecture(s) specialisee(s) par phase de partie (ouverture/milieu/fin), chacune avec un espace de sortie/objectif different - l'ouverture n'a pas besoin de l'espace complet 4672 coups, la fin de partie a besoin d'un objectif different (conclure/eviter le nul, pas juste imiter le coup du professeur). Winston : PAS un cul-de-sac - precedent reel dans les moteurs classiques (livres d'ouverture, tablebases de fin de partie), argument solide sur l'objectif d'entrainement en finale (imitation supervisee peut ne pas etre le bon signal pour CAP-5/nulles-par-repetition). Premature avant d'avoir pousse le modele unique plus loin (donnees+capacite) - recommande un diagnostic peu couteux d'abord (PolicyAccuracy decoupee par numero de coup/phase sur le modele ACTUEL) avant d'investir dans 3 architectures/datasets separes. Logue en backlog (sprint-status.yaml), pas traite cette session (Aymeric : 'avancons deja sur le modele actuel').
- (event) Aymeric confirme tout : num_layers=6/d_model=192 applique a dataset_configs.py (CHESS_MOVE_TOKEN), tpu.micro_batch_size 128->256 (a tester Colab), gpu.micro_batch_size reste 128 (seule valeur validee sans OOM). decay_steps (43905) marque explicitement PROVISOIRE dans le code (commentaire dedie) - depend du volume reel du prochain dataset regenere (~10000 parties cible, pas encore genere). validate_config OK, params reels mesures via model.init() config production = 4468672 (2.24x), forward pass verifie batch=128 ET 256. Diagramme chess-move-token.mmd mis a jour (E4 ADOPTE pour Run 3, E5 ajoute pour le contexte batch/dataset). Architecture par phase de partie confirmee en backlog, pas prioritaire.
- (event) Run 3 prepare (2026-08-11) : dataset regenere cote chess_ai recupere (170989716 octets, conforme a l'estimation 170 Mo). Mesure REELLE (pas estimee) via le motif de reset des longueurs d'historique dans le .npz : 9980 parties, 1393040 positions, 139.58 positions/partie - quasi-parite avec CHESS_SEARCH_TEACHER (10000/1402252). 2e crash GPU local rapporte par Aymeric (modele plus gros desormais, 6/192) -> entrainement deplace sur TPU Colab. gpu.micro_batch_size 128->64, tpu conserve a 256. decay_steps recalcules depuis le volume reel mesure : gpu 293835 (19589 steps/epoch x15), tpu 73455 (4897x15). Comment PROVISOIRE retire du code (valeurs desormais reelles). validate_config OK, 9 tests unitaires OK, chess-move-token.mmd mis a jour (E5).
- (event) Aymeric confirme epochs 15->25 (dataset_configs.py, CHESS_MOVE_TOKEN) - alignement complet sur CHESS_SEARCH_TEACHER (epochs=25/patience=8) maintenant que le volume est a parite. decay_steps recalcules en consequence : gpu 293835->489725 (19589 steps/epoch x25), tpu 73455->122425 (4897x25). validate_config OK, 9 tests unitaires OK. Config CHESS_MOVE_TOKEN prete pour le Run 3 sur TPU Colab.
- (decision) Precision mixte TPU implementee (2026-08-11, model_library.py::ChessMoveTokenTransformer) : nouveau champ compute_dtype (defaut float32, retrocompatible) applique via dtype= sur nn.Dense/nn.MultiHeadDotProductAttention (calcul), param_dtype JAMAIS touche (poids stockes restent float32 - compat checkpoint intacte, verifie). force_fp32_for_softmax=True fixe explicitement sur l'attention (defaut flax=False, verifie par introspection) - softmax en fp32 par prudence sur L=404. Sortie policy_logits recastee explicitement en float32 avant retour (jamais de logits en precision reduite vers compute_chess_policy_loss). Config CHESS_MOVE_TOKEN : compute_dtype='bfloat16' (chaine, convertie par create_chess_move_token_transformer - jamais un objet jnp.dtype dans dataset_configs.py). Scope strictement contenu a ChessMoveTokenTransformer (classe dediee, aucun autre modele ne peut etre affecte).
- (event) 12 tests unitaires (9 existants + 3 nouveaux specifiques precision mixte : parametres/sortie restent float32, gradients finis, factory rejette une chaine invalide) tous verts. Non-regression confirmee sur 42 tests echecs/detection existants (aucun autre modele touche par construction - compute_dtype est un champ local a ChessMoveTokenTransformer, pas une modification partagee).
- (question) Aymeric a signale l'overhead de dispatch par step (~303ms/step observe) comme piste distincte du dtype. Vrai levier connu (regrouper plusieurs steps d'entrainement dans une seule boucle jax.jit/lax.scan pour amortir la latence hote<->device sur N steps au lieu d'un aller-retour Python par step) - mais touche trainer.py (code PARTAGE par tous les domaines, pas seulement chess_move_token). PAS implemente cette session par prudence explicite (Aymeric : 'attention a ne pas degrader les performances' des autres modeles) - logue en backlog, necessiterait sa propre discussion de conception (ex. flag opt-in par config) avant toute implementation, contrairement au compute_dtype qui est sans risque car contenu a une seule classe.
- (event) Reprise reelle testee (Aymeric, 2026-08-11) : resume depuis checkpoint epoch7 (0-indexed, affiche epoch 8) vers le code bfloat16 - AUCUNE regression/discontinuite (predit correctement via lecture code checkpoint_manager.py::resume_training + verification qu'aucun parametre de schedule/architecture n'avait change depuis le lancement de Run 3). Off-by-one confirme visible (epoch 8 rejouee) mais SANS effondrement : Val 11.68%->12.06% (continue la tendance, delta +0.38pt coherent avec le ralentissement deja observe 7->8). Vitesse bfloat16 mesuree : 3.83 it/s vs ~3.30 avant = 1.16x (plus modeste que l'estimation 1.3-1.8x, mais reel et positif, coherent avec la mise en garde sur l'overhead de dispatch/pipeline non reduit par le dtype). Val epoch8 (rejouee) = 563x le hasard, 2.18x le plateau final de Run 2 (5.54%).
- (event) VERDICT CAP-4/CAP-5 (Aymeric, 2026-08-11) - 4 tournois de 100 parties chacun joues cote chess_ai (meme infra que le POC genetique, meme champ 'reason' par partie), tous lus/analyses directement depuis tournament_result.json : 1) teacher vs teacher (self-play, protocole propre) : 78.0% de TOUTES les parties, 92.9% des NULLES, par repetition quintuple (16 decisives/84 nulles, 78 par repetition/6 checkmate/insuffisance materiel 4/pat 2). 2) move_token vs move_token (self-play, meme protocole) : 94.0% de toutes les parties, 100.0% des nulles, par repetition (6 decisives toutes checkmate/94 nulles toutes repetition). 3) teacher vs move_token (tournoi croise, confondu par l'ecart de force) : teacher 32 victoires (100% checkmate) / move_token 0 victoire / 68 nulles (59 repetition, 9 pat) - 59% de toutes les parties en repetition. 4) Baseline POC genetique (run-results.md, population=6 derivee de teacher, 120 parties) : 72.5% de toutes les parties, 91.6% des nulles, par repetition. COMPARAISON PROPRE retenue (1 vs 2, meme protocole exact, meme session, aucun confondant de force) : move_token repete PLUS que teacher (94.0% vs 78.0%, +16.0pt de toutes les parties ; 100.0% vs 92.9% des nulles) - CAP-5 ECHOUE NETTEMENT, dans le sens oppose a l'objectif (reduire la repetition).
- (event) VERDICT FINAL EPIC/SPIKE (Aymeric, 2026-08-11) : les DEUX conditions du succes signal (chess_ai SPEC.md spec-chess-move-token-poc) echouent - CAP-4 (accuracy comparable/meilleure) : NON, plafond ~14-15% (epoch 17/25, tendance a la stagnation + gap train/val croissant) contre 28.00% teacher, a volume de donnees pourtant a parite (1 393 040 vs 1 402 252 positions) et capacite superieure (4 468 672 vs 1 482 305 params, 3.0x). CAP-5 (baisse mesurable de la repetition) : NON, degradation nette (+16pt vs teacher en comparaison propre). Chemin de cloture deja prevu par le spec chess_ai lui-meme applique : 'cloture documentee - meme sort que le POC genetique bottleneck (negatif mais documente, pas un echec de session)'. Aymeric d'accord avec cette lecture ('je te rejoins completement. C'est un echec').
- (event) Hypothese mecanique avancee (Winston) pour expliquer POURQUOI move_token repete plus, pas seulement moins precis : mode de degenerescence connu des modeles sequentiels autoregressifs entraines par imitation/teacher-forcing (repli sur la repetition de motifs recents quand le modele manque de confiance sur le prochain token - documente en generation de texte greedy) - ici la 'sequence' modelisee est litteralement l'historique de coups, donc ce mode de degenerescence se manifeste directement comme des coups repetes. Coherent avec l'hypothese initiale (reconstruction d'etat implicite = tache plus dure) mais va plus loin : biais STRUCTUREL vers la repetition propre a l'architecture sequence-de-coups, pas seulement un deficit de precision. Hypothese non testee formellement (pas de mecanisme d'attribution/interpretabilite mis en oeuvre), a prendre comme lecture qualitative plausible, pas une preuve.
- (event) Epic 11 (CHESS_MOVE_TOKEN) close (Aymeric, 2026-08-11). Cloture documentee : chess_ai/docs/chess_ai_global_conclusions.md (synthese format hypothese/observation -> conclusion, croise HANDOFF.md/POC genetique/spike move-token, verdict CAP-4/CAP-5 negatif documente, 4 pistes futures dont architecture par phase et algorithme genetique reformule). Reference chess_ai/docs/index.md non ajoutee (edition annulee par Aymeric - a refaire plus tard si souhaite). Item explicitement reporte a une future epic : generaliser compute_dtype/precision mixte (introduit ici sur ChessMoveTokenTransformer) aux autres modeles du domaine echecs/detection - PAS fait cette session (scope volontairement limite a une seule classe). Prochaine reprise jax_supervised_training annoncee sur des sujets d'architecture de code, pas ce domaine echecs specifiquement.
