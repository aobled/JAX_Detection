---
scope: Epic échecs : brancher policy/value sur Trainer/TaskStrategy, zéro régression JAX_DETECTOR
purpose: build-substrate
altitude: epic
updated: 2026-07-27T18:46
---

- (constraint) Paradigme hérité, inchangé : Strategy+Factory+DI (main.py/Trainer/TaskStrategy/model_library.get_model()), de architecture-jax_supervised_training-2026-07-15 (lui-même de architecture-JAX_Detection-2026-07-12). Échecs s'insère comme nouvelle stratégie/classe/config, pas une exception au pattern.
- (constraint) AD-14 (parent architecture-jax_supervised_training-2026-07-15) hérité : entraînement reste modulaire, jamais unifié. Échecs s'entraîne comme config/stratégie séparée, comme tous les domaines existants.
- (constraint) AD-17 (parent 2026-07-15) hérité, directement contraignant ici : nouveau format de tâche = nouvelle classe TaskStrategy dédiée + nouvelle classe data loader dédiée, jamais une branche conditionnelle sur une classe existante. Le littéral task_type échecs doit être défini une seule fois et référencé identiquement aux 3 points de dispatch existants : task_strategies.py, data_management.py, main.py.
- (constraint) AD-18 (parent 2026-07-15) hérité, directement contraignant ici : le format d'échange producteur/consommateur (ici : encodage de position en planes façon AlphaZero, produit par le builder dataset PGN, consommé par le data loader/TaskStrategy échecs) est défini par un module ou une paire de fonctions encode/decode partagées — jamais réimplémenté indépendamment de chaque côté.
- (constraint) AD-3 [ADOPTED] (parent 07-12 via 07-15) hérité : tout chargement de checkpoint applique le fallback de chemin 3 niveaux + réinit des batch_stats manquants — s'applique au modèle échecs comme aux autres.
- (event) Vérification brownfield sur le code actuel (main.py:121-166 dispatch task_type, task_strategies.py 4 classes Strategy, data_management.py 3 classes loader, model_library.py get_model/MODELS registry) : pattern AD-17 toujours en place, dispatch à 3 points confirmé (main.py, task_strategies.py, data_management.py). Nouveaux modèles récents (aircraft_detector_centernet_lite, sophisticated_cnn_128_lite) confirment que le registry model_library accepte de nouvelles entrées sans friction.
- (assumption) AD-20 (parent 07-15) protège l'ANCIEN pipeline segmentation (FIGHTERJET_DETECTION), pas JAX_DETECTOR — JAX_DETECTOR n'a encore aucun AD de non-régression dédié puisque 07-15 est l'epic qui l'a créé. La contrainte dure du PRD échecs (JAX_DETECTOR zéro impact) nécessite donc un AD NOUVEAU dans ce spine, pas un héritage. À confirmer que cette lecture est correcte.
- (decision) Double loss policy+value : zero-touch sur Trainer, à l'image exacte de compute_centernet_loss (loss_functions.py:547) — compute_loss retourne UNE loss pondérée policy_weight*policy_loss + value_weight*value_loss. Détail policy_loss/value_loss séparé PAS visible dans le log live Trainer (Loss=.../metric=... par epoch) — exposé uniquement en fin d'entraînement via generate_reports() (hook déjà existant par stratégie). Aucune modification de trainer.py.
- (decision) primary_metric_name de la stratégie échecs = accuracy policy (top-1 coup prédit == coup joué), optimization_mode='max'. Cohérent avec l'objectif Maia (imiter le coup humain). La value head est entraînée (contribue à la loss) mais ne gate pas la sauvegarde du best model.
- (decision) Encodage policy = espace de coups fixe façon AlphaZero (vecteur de taille fixe), avec masquage explicite des coups illégaux. Résout Open Question #2 du PRD (impact direct sur faisabilité 'sans modification Trainer' — confirmé sans impact, sortie modèle reste un array de taille fixe, cohérent avec le pattern générique).
- (decision) Construction des tokens du bottleneck = requêtes apprises type Perceiver/TokenLearner (cross-attention CNN-features -> K tokens appris), pas du pooling par groupe de canaux. Résout Open Question #1 du PRD.
- (decision) Biais géométrique dans l'attention (Maia-3 style) : PAS implémenté cette epic, attention 'nue' (standard) pour commencer. Explicitement conservé comme piste d'itération future, pas abandonné. Résout Open Question #3 du PRD.
- (version) flax.linen 0.10.7 déjà présent dans l'environnement (vérifié) — fournit nn.MultiHeadDotProductAttention nativement. Aucune nouvelle dépendance externe requise pour l'auto-attention (Perceiver-tokens + attention entre tokens). Première utilisation d'attention transformer-style dans le codebase (l'attention existante — SpatialAttention/SE — est du gating CNN, pas du self-attention multi-têtes).
- (decision) Pas de masquage des coups illégaux cette epic — repoussé au futur projet d'intégration chess_game.py (sélection de coup à l'inférence). Entraînement = cross-entropy simple contre le coup joué sur l'espace de coups fixe, sans masque. Aymeric a soulevé le cas des coups spéciaux (prise en passant) : couvert par construction, puisque les labels viennent de python-chess qui gère déjà la légalité/les règles spéciales lors du rejeu PGN — le modèle apprend juste à imiter l'index de coup encodé, pas à connaître les règles lui-même. L'exact schéma d'encodage coup->index (roque, promotion, en passant) est un détail d'implémentation du module encode/decode partagé (AD-18), pas un nouvel AD.
- (decision) Métadonnées dataset (cadence, ECO) : entièrement ignorées cette epic, ni capturées ni filtrées. Format d'exemple = position + policy target + value target uniquement. Risque accepté : si besoin plus tard, re-parsing du PGN brut nécessaire.
- (decision) task_type échecs = 'chess_policy_value' (littéral unique, référencé identiquement à main.py, task_strategies.py, data_management.py per AD-17 hérité).
- (decision) Nouvel AD-21 confirmé (premier ID libre après le parent 07-15 qui va jusqu'à AD-20) : non-régression JAX_DETECTOR, formulé sur le modèle d'AD-20 (parent) mais Binds=JAX_DETECTOR (au lieu de FIGHTERJET_DETECTION/ancien pipeline, non couvert par AD-20). Validé par exécution/baseline diff (FR-7 PRD).
- (decision) model_name échecs = 'chess_cnn_attention_policy_value' (model_library.py, registry MODELS). dataset_configs.py entrée = 'CHESS' (convention majuscule existante : JAX_DETECTOR, KEPLER, FIGHTERJET_DETECTION).
- (event) Coaching terminé, toutes les décisions structurantes actées. Passage à la distillation du spine.
- (event) Spine distillé : Design Paradigm, 4 Inherited Invariants (AD-3/14/17/18 du parent 07-15), 5 AD propres (AD-21 non-régression JAX_DETECTOR, AD-22 policy sans masquage, AD-23 bottleneck Perceiver-style, AD-24 TaskStrategy loss composite zero-touch Trainer, AD-25 format dataset minimal), diagramme de dépendances, Consistency Conventions, Stack (aucune nouvelle dépendance), Structural Seed, Deferred (biais géométrique, masquage, métadonnées, schéma d'encodage exact, K tokens, intégration chess_game.py, déploiement)
- (event) Reviewer Gate complet : lint_spine.py propre, 2 réconciliations (PRD, spine parent) + rubric walker + version-check + adversariale, toutes revenues. Fixes appliqués : NFR-2 (pas de moteur externe) ajouté à AD-25, correction '3 points de dispatch' hérités du parent → 2 réels (task_strategies.py ne dispatche pas), chemin dataset_builder/ corrigé (était à la racine), JAX_KEPLER (était KEPLER), value head précisée scalaire/tanh/MSE (AD-24), source unique taille espace policy num_classes=NUM_MOVES réutilisant model_kwargs existant (AD-22, ferme F1 adversarial), source unique poids/sous-loss via loss_params (AD-24, ferme F3), propriétaire unique du signe value = producteur (AD-25, ferme F4), règle policy non-filtrée par résultat ancrée à AD-25 (FR-3), Deferred enrichi (K token sourcing caveat, calibrage batch/epoch).
- (event) spine finalized
