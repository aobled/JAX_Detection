---
scope: Généraliser compute_dtype (dtype de calcul interne des couches Dense/Attention) : dérivation automatique depuis le backend matériel détecté (bfloat16 si TPU, float32 sinon), forwarding unconditionnel à tout modèle, retrait de la string codée en dur sur CHESS_MOVE_TOKEN. Séquencement voulu par Aymeric : impact concret sur CIFAR10 (sophisticated_cnn_32_plus) comme cas de référence d'abord, généralisation à l'ensemble des modèles du projet discutée ensuite.
purpose: Trancher le point d'injection (comment un modèle Flax adopte compute_dtype sans dupliquer la détection matérielle par classe) et son impact concret sur CIFAR10, pour ensuite décider si/comment étendre aux autres modèles du projet. Livrable : la spine seule (pas de rendu additionnel), consommée ensuite par bmad-spec pour mettre à jour SPEC-compute-dtype-hardware.
altitude: feature (une spec unique, spec-compute-dtype-hardware, pas une spine projet entier)
updated: 2026-08-17T08:57
---

- (direction) Input = SPEC-compute-dtype-hardware (spec + memlog), déjà résolu : forwarding unconditionnel par backend (pas de string par config), retrait prévu du littéral CHESS_MOVE_TOKEN, séquencement CIFAR10 (sophisticated_cnn_32_plus) comme cas de référence AVANT généralisation à tous les modèles. Hérité tel quel, non re-décidé ici.
- (note) Investigation brownfield (model_library.py) : SophisticatedCNN32Plus (CIFAR10) compose 3 sous-modules PARTAGÉS avec les autres CNN du projet (SophisticatedCNN128Plus/128Lite) — SeparableConv, SEBlock, SpatialAttention (lignes 18-83) — aucun n'accepte aujourd'hui de champ compute_dtype. ~15 appels nn.Conv/nn.Dense/nn.BatchNorm/nn.LayerNorm dans le seul SophisticatedCNN32Plus. Contrairement à ChessMoveTokenTransformer (seul précédent, aucune composition de sous-module), le mécanisme doit donc se propager à travers des nn.Module imbriqués partagés par plusieurs modèles — cas non couvert par le précédent Epic 11.
- (note) Risque de crash confirmé par lecture du code : get_model(model_name, **kwargs) -> MODELS[model_name](**kwargs) où MODELS pointe vers des factory functions à signature FIXE (ex. create_sophisticated_cnn_32_plus(num_classes=2, dropout_rate=0.0), aucun **kwargs catch-all). Un forwarding vraiment inconditionnel de compute_dtype depuis main.py vers TOUS les modèles casserait immédiatement (TypeError kwarg inattendu) tout modèle dont la factory ne déclare pas encore ce paramètre — soit la quasi-totalité aujourd'hui (CIFAR10 excepté, une fois adapté).
- (note) Recherche web (2026-08-17) confirmée : la pratique standard flax.linen.BatchNorm calcule ses statistiques en float32 EN INTERNE quel que soit le dtype de calcul passé (dtype=bfloat16 n'affecte que la sortie/le calcul, pas l'accumulation des stats, param_dtype par défaut float32) — donc dtype=self.compute_dtype peut s'appliquer UNIFORMÉMENT à Conv/Dense/BatchNorm/LayerNorm sans exclusion spéciale pour BatchNorm/LayerNorm, contrairement à une inquiétude initiale de stabilité numérique. Sources : apxml.com/courses/advanced-jax (mixed precision JAX), flax.readthedocs.io (BatchNorm API), github.com/google/flax/discussions/3987.
- (note) Impact réel du big-bang (les 12 entrées de MODELS, model_library.py:1750-1763) audité par lecture de code : (A) chess_move_token_transformer — DÉJÀ compatible. (B) chess_cnn_attention_legal_moves/policy_value, chess_token_candidate_model, chess_token_one_move_model, kepler_1d_cnn — factory transmet **kwargs TEL QUEL au nn.Module, mais le nn.Module lui-même ne déclare pas compute_dtype -> crasherait quand même (TypeError) un niveau plus bas, factory seule insuffisante. (C) aircraft_detector_unet/centernet/centernet_lite — factory accepte **kwargs mais NE LE TRANSMET JAMAIS au constructeur (déjà documenté comme piège connu dans le code, ex. num_classes silencieusement perdu pour centernet) -> compute_dtype serait avalé SILENCIEUSEMENT, aucun crash, aucun effet, faux sentiment que ça marche. (D) sophisticated_cnn_128_plus/128_lite/32_plus (dont CIFAR10) — AUCUN **kwargs, signature fixe -> crash immédiat et bruyant.
- (note) Conclusion de l'audit : un big-bang inconditionnel aujourd'hui ne serait pas un simple ajout de champ sur chaque factory — il exigerait de toucher/auditer les 11 modèles restants MAINTENANT (dont 3, les aircraft_detector_*, échoueraient SILENCIEUSEMENT si oubliés, pire qu'un crash). Contredit directement le séquencement 'CIFAR10 seul aujourd'hui' déjà acté. Présenté à Aymeric pour arbitrage final avec ce chiffrage concret (pas hypothétique).
- (decision) Résolution Q1 (Aymeric, 2026-08-17) : introspection automatique (inspect.signature) retenue pour MAINTENANT. Seul CIFAR10 (sophisticated_cnn_32_plus) adapté dans ce chantier ; les 11 autres modèles restent inchangés (non cassés, introspection ne leur transmet rien) ; big-bang explicitement différé à une décision future, modèle par modèle.
- (decision) Résolution Q2 (Aymeric, 2026-08-17) : résolution string->jnp.dtype centralisée dans main.py (une fois), retirée de create_chess_move_token_transformer (model_library.py:1218-1221) qui n'est appelée que via get_model (vérifié par grep, aucun autre appelant direct).
- (note) Écart trouvé entre le succès signal du SPEC (test Colab FIGHTERJET_CLASSIFICATION/TPU) et le scope 'CIFAR10 seul' : FIGHTERJET_CLASSIFICATION utilise sophisticated_cnn_128_lite (dataset_configs.py, model_name), PAS sophisticated_cnn_32_plus (CIFAR10). SophisticatedCNN128Lite (model_library.py:212) est structurellement analogue à SophisticatedCNN32Plus (mêmes sous-modules partagés SeparableConv/SEBlock/SpatialAttention, même pattern Conv/BatchNorm/Dense) mais reste une classe top-level SÉPARÉE avec ses propres appels de couches à instrumenter individuellement. Si seul sophisticated_cnn_32_plus est adapté, le test Colab TPU sur FIGHTERJET_CLASSIFICATION ne validerait RIEN de réel côté bfloat16 (son modèle resterait float32 en dur) — à trancher avec Aymeric.
- (decision) Résolution scope FIGHTERJET (Aymeric, 2026-08-17) : sophisticated_cnn_128_lite (FIGHTERJET_CLASSIFICATION) adapté DANS ce même chantier, en plus de sophisticated_cnn_32_plus (CIFAR10) — même patron mécanique, aucune nouvelle décision de conception. Scope final de cette spine : 2 modèles adaptés (32_plus, 128_lite) + les 3 sous-modules partagés qu'ils composent (SeparableConv/SEBlock/SpatialAttention) + le point d'injection central (main.py). Les 10 autres entrées de MODELS restent inchangées.
- (decision) dtype=self.compute_dtype s'applique UNIFORMÉMENT à tous les appels nn.Conv/nn.Dense/nn.BatchNorm/nn.LayerNorm dans les classes adaptées et leurs sous-modules — pas d'exclusion spéciale pour BatchNorm/LayerNorm (confirmé par recherche web : Flax calcule déjà les stats BatchNorm en float32 en interne quel que soit dtype). SeparableConv/SEBlock/SpatialAttention reçoivent compute_dtype comme nouveau champ, threadé depuis la classe parente à chaque instanciation (ex. SeparableConv(48, (3,3), compute_dtype=self.compute_dtype)).
- (constraint) Compatibilité checkpoint préservée par construction : compute_dtype est un champ de configuration statique du nn.Module (dataclass field, pas un nn.Param), donc absent du pytree des paramètres — ajouter ce champ (avec défaut float32) aux classes adaptées ne change ni la forme ni la structure des poids existants. Même garantie déjà établie par le précédent ChessMoveTokenTransformer (dataset_configs.py:960-961).
- (note) Enveloppe opérationnelle (déploiement/environnements) : aucun changement — TPU via Colab, GPU/CPU en local, détection au cold-start (main.py:31-41) déjà en place et réutilisée telle quelle, pas une nouvelle dimension pour cette spine.
- (event) Reviewer Gate exécutée : lint_spine.py clean (0 finding) ; 4 revues parallèles (réconciliation spec, rubric walker, vérification web, adversariale). 2 findings CRITIQUES confirmés par lecture directe du code : (1) AD-2 tel que rédigé cassait tests/test_chess_move_token_model.py::test_compute_dtype_factory_rejects_unknown_string (L257-265, appelle create_chess_move_token_transformer directement avec une string invalide et attend un ValueError explicite) — la suppression littérale du bloc getattr/raise (model_library.py:1218-1221) viole AD-7 (non-régression). (2) AD-3 (introspection) contredit explicitement et sans le dire la décision datée d'Aymeric dans SPEC.md/memlog ('forwarding UNCONDITIONNEL... pas d'opt-in par registre/introspection', OQ1 2026-08-17) — le spec source doit être mis à jour pour refléter ce renversement légitime (motivé par l'audit brownfield : le forwarding littéralement inconditionnel casserait 6 modèles sans **kwargs ni champ compute_dtype, contredisant le propre objectif de non-régression du spec pour CAP-2).
- (decision) Fix F1 (AD-2) : NE PAS retirer la validation existante de create_chess_move_token_transformer (model_library.py:1218-1221) — elle reste telle quelle, couvre les appels directs/hors main.py (déjà testée). AD-2 recentré : la résolution centralisée dans main.py s'applique aux NOUVEAUX adoptants (CIFAR10/FIGHTERJET et au-delà), qui reçoivent un jnp.dtype déjà résolu sans avoir besoin de leur propre validation — pas de retrait rétroactif sur le précédent existant.
- (decision) Fix F2/F3 (AD-3) : ajout explicite dans la spine que AD-3 renverse la résolution OQ1 du SPEC ('unconditionnel, pas d'introspection'), avec la justification concrète (audit brownfield : 6 modèles casseraient sinon). SPEC.md à mettre à jour via bmad-spec après cette spine pour rester synchronisé (offert à Aymeric au closing). Fermeture explicite de la faille F3 : la SEULE façon sanctionnée pour un modèle d'être exempté de compute_dtype dérivé du matériel est de ne pas déclarer le paramètre nommé (mécanisme AD-3) — un flag de config par dataset reste interdit même présenté comme une 'exception', pas seulement comme cas général (renforce AD-1).
- (decision) Fix F4 (AD-3) : nommer le paramètre ne suffit pas à prouver qu'il est réellement consommé (reproduirait le piège aircraft_detector_* un niveau plus bas, silencieusement). Ajout à AD-3 : tout modèle adoptant compute_dtype doit avoir un test dédié qui vérifie que le dtype est RÉELLEMENT observé (paramètres/sortie diffèrent sous bfloat16 vs float32) — même patron que le test existant test_compute_dtype_bfloat16_params_and_output_stay_float32 (tests/test_chess_move_token_model.py:207) — pas seulement l'absence d'erreur à l'appel.
- (decision) Fix F2/rubric (AD-4) : lecture de la docstring précédente (model_library.py:1107-1123, ChessMoveTokenTransformer) — la raison réelle de l'exclusion de nn.LayerNorm/nn.Embed n'est PAS un risque numérique mais 'seuls Dense/Attention bénéficient réellement d'un calcul en précision réduite' (ops non compute-bound). AD-4 réécrite pour ratifier ce principe exact plutôt que l'application uniforme initiale : compute_dtype s'applique aux appels nn.Conv/nn.Dense (ops matmul lourdes) où qu'ils apparaissent, y compris dans les sous-modules partagés — jamais à nn.BatchNorm/nn.LayerNorm/nn.Embed. Portée élargie au-delà des 2 classes ciblées (résout aussi F5 : la règle devient générique par TYPE de couche, pas scopée à des noms de classe, donc s'applique automatiquement à tout futur adoptant sans réécrire l'AD).
- (decision) Fix F6 : ajout à Consistency Conventions — le défaut propre d'un NOUVEAU factory (utilisé si appelé hors du chemin main.py) doit déjà être un jnp.dtype résolu (ex. jnp.float32), jamais une string. Ne s'applique pas rétroactivement à create_chess_move_token_transformer (défaut string préservé, cf fix F1).
- (decision) Fix mineurs : (1) Deferred corrigé '10 autres modèles' -> '9 autres modèles' (12 total - 2 touchés - 1 déjà adopté = 9, recompté). (2) Citation ajoutée pour la base factuelle de AD-4 (docstring model_library.py:1107-1123 comme source primaire in-repo ; recherche web complémentaire : flax.readthedocs.io, apxml.com/courses/advanced-jax, github.com/google/flax/discussions/3987 pour le mécanisme force_float32_reductions de BatchNorm, désormais accessoire puisque AD-4 exclut BatchNorm/LayerNorm par principe, pas seulement par sécurité). (3) main.py:154-160 (ancien bloc de forwarding conditionnel par config) explicitement nommé comme code mort à retirer, ajouté au Structural Seed.
- (event) spine finalized — 7 AD (AD-1..AD-7), tous les findings critiques/high de la Reviewer Gate corrigés (F1 AD-2 préserve le précédent testé, F2/F3 AD-3 documente explicitement le renversement d'OQ1 + ferme la faille opt-out, F4 AD-3 exige un test d'effet réel, F2-rubric/F5-adversarial AD-4 réécrite par TYPE de couche (Conv/Dense uniquement) ratifiant le principe réel du précédent, généralisée à tout futur adoptant). Findings mineurs corrigés (compte Deferred, code mort main.py:154-160 nommé). lint_spine.py clean après corrections. status: final. Prochaine étape recommandée : bmad-spec pour re-dériver SPEC-compute-dtype-hardware (documenter le renversement d'OQ1), puis implémentation (bmad-quick-dev ou bmad-dev-story).
- (event) Implémentation (bmad-quick-dev, 2026-08-17) a trouvé un trou réel dans AD-2/AD-3 non anticipé par la Reviewer Gate : l'introspection pure-par-nom (AD-3) cible AUSSI create_chess_move_token_transformer (paramètre nommé compute_dtype préexistant), qui recevrait le jnp.dtype déjà résolu de main.py au lieu de la string qu'attend sa validation interne (getattr(jnp, <dtype-object>, None) lève TypeError, pas None) — cassait tout run réel de CHESS_MOVE_TOKEN (modèle clos Epic 11, risque pratique faible, bug réel). Fix minimal appliqué avec accord Aymeric : la factory accepte str (chemin historique inchangé) OU jnp.dtype déjà résolu (nouveau chemin), les 2 tests existants restent verts. AD-2/AD-7 amendés en conséquence (la factory n'est plus strictement 'inchangée octet pour octet' mais son comportement/tests existants sont 100% préservés, seule une tolérance de type est ajoutée).
